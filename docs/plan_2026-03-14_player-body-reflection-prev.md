# プレイヤー体表示とミラーハウス反射対応 計画

更新日: 2026-03-14

## プロンプト
今現在の実装で、ゲーム開始時に、プレイヤーに対してキャラクターID（少なくともNPCには割り振られているはずの、キャラ画像を表示するなどに使うID）は割り振られていますか？

なるほど。
これから追加したいと思っているのは、以下の機能です。
- プレイヤーにも、キャラ画像を割り振る
- カメラを下に向けると、キャラ画像の下半身が映るようにする
- ステージ上の鏡や水たまり（未実装）に、プレイヤーキャラの画像が映るようにする
そのためにどのようなプロパティや、実装が必要か、検討してください。

指示を追加します。
鏡と水たまりのテスト用に、arena.jsonをコピーし、「アリーナ（ミラーハウス）」ステージを追加してください。
その新規ステージ以外には、まだ鏡と水たまりは実装しないでください。

下半身レイヤーについて、
下を向くと足先だけが見えるような見え方でした。
顔の位置にカメラがあると考えると、下を向くと胸のあたりから足先までが見えるのが自然です。
遠近感も付けて、レイヤーの見え方を調整してください。

いいえ、見え方がおかしいです。
カメラを下に向けたときに、胸元（キャラ画像の上から2/3程度）が画面下辺いっぱいに映り、そこから足先（キャラ画像の下辺）が画面中央くらいに向かって遠くなっていくようにしてください。
また、下半身レイヤーが見え始めるようカメラの傾きは、ほぼ下限界ギリギリに近づくまでは見えないようにしてください。

前よりは良くなりましたが、まだ調整したいです。

カメラ角度と足先の関係について：
カメラを下に向けていった時、カメラに見えている範囲に、プレイヤーが立っている座標＝カメラの真下の床が映り始めたら、そこに足先（画像の下辺）が来るようにしてください。
更にカメラを下に向けていくと、画像が画面下辺から徐々に上に上がってくるようなイメージで、最終的には画面中央に足先、画面下辺に胸元がくるようにしてください。

胸元の見え方について：
胸元（キャラ画像の上から2/3程度）と指示しましたが、まだ低すぎます。腰より下しか画面に見えていません。もっと画像の上の方まで下半身レイヤーにしてください。
もし、腰より下しか画面に見えていない理由がカメラの位置が低いからであれば、カメラの位置をもう少し上げて

良くなってきました。が、まだ調整したいです。
- 切り抜き位置をもっと上まで上げてください。
- 下半身レイヤーが、カメラを下に向けるほど透明度が下がって見えるようになりますが、透明度は最初から0でいいです。また、台形の変形は今よりも緩めてください（なしにはしない）。さっき指示したように、画面下から画像がスライドインしてくるイメージにしてください。

台形変形よりも、スライドインで演出してほしいんです。まだ台形変形が強いし、スライドインも全然していません。

スライドインのイメージは近づきました。
ただ、台形の角度が強すぎて不自然に見えます（カメラ向き変更にともなう角度変更ではなく、レイヤーそのものの基本角度のこと）。元の長方形に近い角度にしてください。
また、切り抜き位置をもう少し下げてください。0.35にしてみてください。

カメラを下方に向けたときの、プレイヤーのスプライト画像の見え方を自然に調整したいです。
現在の表示よりも、少し足先（画像の下辺）に向かっての傾きを強めて、遠くにある感を強めてください。

もう少し遠くにある感を出してください

足先をもう少し遠くにしてください。胸元は今のままでいいです

今、足先（画像の下辺）はカメラ座標の真下に合わせてあると思います。
これを、下半身レイヤー表示時のみ、足先の位置を画面で見たときに上に少し移動させてほしいです。

もう少しだけ足先を上にずらし、胸元を画面内に表示できるようにしてください

下半身レイヤーの胸元のオフセットがおかしいです。カメラを真下に向けた時、胸元（画面下辺）が見切れてしまっています。

逆です！
さっきよりも見切れがひどくなってしまいました。
胸元の画像を（画像の上側）をもっと表示できるようにオフセットを調整してください。

もう少し、下半身レイヤー全体を画面上方向にずらして表示してください。
胸元のオフセットも、見きれないように同時に調整してください。

だーかーらー
見切れないように画像のオフセットも調整してっていいましたよね。
胸元が見切れているのでもっと表示してください。

今の足先の位置と、胸元（画像上部）のオフセットは固定したまま、
下半身レイヤーを画面手前（下側）に縦方向の比率を伸ばしてください。下方向に引っ張り伸ばして、画面下側に空白ができないようにしてください。

足先が消えちゃってますよ。

全くもっておかしなことになっています。
画像が2枚に分かれて、変な方向で結合されています。

初心に返ってください。
私が求めている「下半身レイヤーの表示方法」の設計を、もう一度問うてください。

案2です。

下辺を100px分下に「比率を伸ばして」ください。画像のオフセットは変えないでください。

「下」の意味が違って伝わっています。
「画面下辺」は、「下を向いたカメラの手前側」であり、「ステージの絶対座標でいう上方向」です。これは分かりますか？
なので「下辺を100px分下に「比率を伸ばして」ください」という指示に従うと、画面下部の隙間がなくなるはずなのです。しかし隙間がたっぷり残っています。

また、「100px分伸ばす」のような当座の仕組みで解決しないでください。画像をどのように表示するかの仕組み全体を調整して、私の指示を守るようにしてください。

おお、かなり良くなってきました。
もうほんの少し、画面下側に伸ばしてください。

いや、やっぱりやめておきます。

別件で、カメラを下にゆっくり向けていくと、ある角度でいきなり下半身レイヤーの足先が表示される境界線があります。
いきなり出すにしても画面外で画像を出してからスライドインしてくるようにしてほしいです。

なんかある境界線でいきなりヌッとスライドインしてくる感じがあります。
変な補完はせずに、直線的に画面内に出てくるようにしてほしいです。

PLEASE IMPLEMENT THIS PLAN:
# プレイヤー体表示とミラーハウス反射対応 計画

## 要約
- 定義方式は `zone` ではなく、`水たまり = env拡張`、`鏡 = decals拡張` を採用する。`zone` はゲーム進行注釈なので見た目用の反射面には使わない。
- プレイヤー表示は二系統に分ける。通常プレイでは見やすさ優先の一人称下半身レイヤーを使い、反射面には既存のプレイヤースプライトをワールド体として映す。
- 鏡と水たまりの配置データは新規ステージ「アリーナ（ミラーハウス）」にのみ追加する。既存ステージ JSON には追加しない。

## 主要変更
- プレイヤー描画
  - プレイヤーの正本データは現状の `playerVoiceId` / `playerPortraitDirectory` / `playerState` を継続利用する。
  - 表示先を `反射用ワールド体` と `一人称下半身レイヤー` に分離する。
  - 反射用ワールド体は既存 `playerAvatar` を流用し、通常プレイ中は主カメラから隠して反射カメラにだけ見せる。
  - 一人称下半身レイヤーは専用 plane とし、既存の立ち絵スプライトシートの現在セルを再利用して下半身だけを切り出して表示する。
  - 下半身レイヤーはカメラ pitch に応じて表示・位置・フェードを制御し、実姿勢の厳密再現より見やすさを優先する。
  - `playerAvatar` と下半身レイヤーは同じ state / cellIndex 更新経路を共有し、状態差分を持たせない。

- 反射面
  - 反射は scene 全体を少しくすませて映す planar reflection で実装する。
  - 水たまり用に床反射 RTT を 1 枚持ち、複数水たまりで共有する。
  - 鏡は壁沿い矩形のみ対応し、鏡面ごとに個別 mirror RTT を持つ。
  - 反射キャプチャ用カメラを追加し、主カメラの位置・角度・FOV を同期する。
  - 反射 RTT は `renderSprites = true` を有効化し、NPC とプレイヤーのスプライトも反射に含める。
  - 反射面マテリアルは `tint` `amount` `blur` を持ち、少しくすんだ色味を表現する。

- ステージ定義
  - `StageSurfaceStyle` に反射設定を追加する。対象は `enabled` `tint` `amount` `blur`。
  - `decals` を型付きにし、壁矩形鏡を表せる `reflective.kind = "mirror"` を追加する。
  - 水たまりは `semantics.channels.env` の新記号で定義し、対応する `generationRules.env.<symbol>.floor.reflection` に反射設定を書く。
  - 鏡は `decals` の `face: "wall"` `type: "rect"` `wallDir` と反射設定で定義する。
  - `arena.json` を複製して新規ステージ `arena_mirror_house.json` を作り、`meta.description` を「アリーナ（ミラーハウス）」にする。
  - ステージカタログに `arena_mirror_house` を追加する。
  - 既存ステージ JSON は変更しない。鏡・水たまりのデータは新規ステージにだけ入れる。

- ステージ生成
  - 床本体マテリアルは従来のまま維持し、水たまりは薄い overlay plane を重ねて反射を載せる。
  - 鏡は wall decal から専用 plane を生成する。
  - 反射面・反射 RTT・専用マテリアルを `StageParts` の管理対象へ追加し、dispose 対象に含める。
  - 反射データが無いステージでは従来経路のまま動作し、追加描画物を作らない。

## ステップ
- [x] 既存 `docs/plan.md` を退避し、今回タスク用の計画へ切り替える
- [x] ステージJSON型とステージカタログを拡張し、`arena_mirror_house` を追加する
- [x] プレイヤー表示を反射用ワールド体と一人称下半身レイヤーへ分離する
- [x] 水たまり・鏡の反射面生成と反射カメラ同期を実装する
- [x] `npm run build` でビルド確認を行う
- [x] 実装結果を `docs/plan.md` の結果へ反映する

## 結果
- `src/main.ts` でプレイヤー表示を分離し、通常プレイ中は `playerAvatar` を反射専用レイヤへ退避しつつ、カメラ前方に一人称下半身レイヤーを重ねるようにした。下半身レイヤーは既存の立ち絵スプライトシートから現在セルの下側だけを切り出して使い、視線を下げるほどフェードインする。
- `src/main.ts` に反射専用カメラと反射面同期処理を追加し、ステージが持つ反射面メッシュへ `MirrorTexture` を割り当てるようにした。水たまりは共通 RTT、鏡は面ごとの RTT を使い、どちらも少しくすんだ色味で scene 全体を反射する。
- `src/world/stageJson.ts` / `src/world/stage.ts` / `src/world/stageContext.ts` を拡張し、`env` の床反射設定と `decals` の壁鏡設定を読んで、反射面メッシュを生成・破棄できるようにした。
- `public/stage/arena_mirror_house.json` を追加し、`src/world/stageSelection.ts` から選択できるようにした。鏡と水たまりのデータはこの新規ステージだけに定義し、既存ステージ JSON には追加していない。
- `docs/plan.md` 旧版は `docs/plan_2026-03-11_no-gun-touch-brainwash-reload-prev.md` へ退避した。
- `npm run build` は成功した。ゲーム内の見た目確認は未実施。
- 一人称下半身レイヤーの切り抜き位置を上げ、レイヤー自体のサイズ・前後距離・傾きを視線角度に応じて変えるようにした。これにより、下を向いたときに胸元から足先まで見えやすくなり、上半身ほど遠く見える構図へ寄せた。
- 一人称下半身レイヤーを、胸元側を手前・足先側を奥に置く台形メッシュへ変更した。可視化開始角度もほぼ真下近くまで遅らせ、胸元が画面下辺いっぱいに入り、足先が画面中央方向へ遠ざかる見え方になるよう調整した。
- 一人称下半身レイヤーの足先位置を、カメラ FOV から求めた「真下の床が見え始める角度」に連動するよう変更した。そこから真下へ向けるほど足先が画面下辺から中央へ上がり、胸元は画面外下から下辺へせり上がる構図にした。腰しか見えない問題は切り抜き開始位置が低すぎたことが原因だったため、カメラ高さは変えずに切り抜き範囲を上方へ広げた。
- 一人称下半身レイヤーの切り抜き開始位置をさらに上げ、不透明度は出現直後から固定にした。台形の変形量も弱めて、遠近感は残しつつ、画面下から画像がそのままスライドインしてくる印象を強めた。
- 一人称下半身レイヤーの補間を「上下のスライド量」と「台形変形量」で分離し、台形側はごく弱く、上下移動側を強くした。これにより、画面下から画像がせり上がってくる見え方を主にしつつ、遠近感はわずかに残す調整へ変更した。切り抜き位置もさらに上げて、胸元寄りまで見える範囲を広げた。
- 一人称下半身レイヤーの基本台形角度をさらに弱め、長方形に近い見え方へ寄せた。切り抜き開始位置は `0.35` に調整し、表示範囲を少し下げてバランスを取り直した。
- `src/main.ts` の `develop` マージ競合は、反射カメラ同期 `syncReflectionCamera` とタイトル画面の可変カメラ高さ同期 `syncTitleCameraHeight` を併存させる形で解消した。これにより、`develop` 側のカメラ高さ可変設定を維持したまま、現ブランチ側の反射描画同期も残している。
- 一人称下半身レイヤーの胸元側をわずかに手前かつ広め、足先側をわずかに奥かつ細めに再調整し、台形補間の効きも少しだけ強めた。これにより、下を向いたときに画像下辺へ向かう遠近感が現状より自然に強く出るようにした。
- 一人称下半身レイヤーの遠近差をさらに一段だけ広げ、胸元側を少しだけ手前かつ広め、足先側を少しだけ奥かつ細めに再調整した。台形補間の効きもわずかに増やし、スライドイン主体の印象を残したまま、足先がより遠くにある見え方へ寄せた。
- 一人称下半身レイヤーの胸元側は維持したまま、足先側だけをさらに奥かつ細めに調整した。これにより、画面下辺から中央へ向かう足先の遠さだけを追加で強め、胸元の見え方は前回のバランスを保つようにした。
- 一人称下半身レイヤー表示時のみ、足先側の画面内Y位置を少し上方へシフトした。胸元側や遠近感の定数は維持しつつ、足先だけが画面上でわずかに上に見えるように調整した。
- 一人称下半身レイヤーの足先側をさらに少しだけ上方へ寄せ、胸元側もわずかに画面内へ入るように `screenY` を再調整した。遠近感や幅・奥行きはそのままに、表示範囲だけを上にずらして収まりを整えた。
- 一人称下半身レイヤーの胸元側 `screenY` が下がり過ぎていたため、胸元側だけを少し上へ戻した。これにより、カメラを真下へ向けたときでも画面下辺の胸元が見切れにくいように調整した。
- 胸元側の前回調整は逆効果だったため、胸元側 `screenY` を反対方向へ振り直した。これにより、胸元画像の上側がより表示され、真下視点でも画面下辺で見切れにくい方向へ再調整した。
- 一人称下半身レイヤー全体の `screenY` をさらに上方へ寄せ、足先側はそのまま画面上方向へ、胸元側は見切れ対策として少し強めに上方補正した。これにより、レイヤー全体が上に移動しつつ、胸元の画像上側も前回より画面内に収まりやすくした。
- 胸元の見切れは画面位置だけでなく切り抜き量も原因だったため、`firstPersonBodyCropTopRatio` を `0.3` へ下げて画像上側を多く残しつつ、胸元側 `screenY` もさらに上方へ補正した。これにより、胸元画像の上側が前回より多く表示されるように調整した。
- 足先位置と胸元側の上方向切り抜きは維持したまま、画像下側だけを少し詰めて同じ表示高さへ引き伸ばすように変更した。これにより、下半身レイヤーの見た目を下方向へ伸ばし、画面下側の空白が出にくいように調整した。
- 前回の下側切り抜きは足先まで削ってしまっていたため取りやめ、足先行を固定したままその下にだけ伸びる帯を追加するジオメトリへ変更した。これにより、足先を残したまま画面下側の空白だけを埋める方向へ修正した。
- 下半身レイヤーの表示方式は「案2」を採用し、「1枚の画像を1つの四角形で表示し、軽い台形変形だけを許可する」方針へ整理し直した。これに合わせて分割メッシュを撤去し、単一台形の下辺をやや下げる調整へ戻した。
- 単一台形の上辺や画像オフセットは維持したまま、下辺だけを 100px 分だけ下方向へ伸ばす調整を追加した。これにより、画像をずらさずに縦方向の比率だけを下側へ広げるようにした。
- 「画面下辺」は胸元側の近辺である前提へ表示ロジックを整理し直し、`px` 加算で下辺を動かす処理は撤去した。代わりに、足先側を基準に胸元側だけを比率で引き伸ばす方式へ変更し、画像オフセットを変えずに画面下側の隙間を埋める方向へ修正した。
- 単一台形の胸元側伸長率を `1.28` へわずかに上げ、画面下辺側へほんの少しだけ追加で伸ばした。表示方式や画像オフセットはそのままで、比率だけを微調整した。
- 下半身レイヤーの出現境界は維持したまま、表示直後だけ全体へ下方向のオフセット補間を加えるようにした。これにより、足先が境界線でいきなり画面内へ出るのではなく、いったん画面外下に置かれてからスライドインする見え方へ調整した。
- 下半身レイヤーの出現直後だけ別系統で加えていた下方向オフセット補間は撤去し、開始位置そのものを下げたうえで可視化量に対して終始直線補間する方式へ戻した。これにより、境界線を越えた直後の「ヌッ」とした不自然な追い付きがなくなり、画面外下からそのまま直線的にスライドインする見え方へ調整した。
