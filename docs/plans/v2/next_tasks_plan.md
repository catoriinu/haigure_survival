# HAIGURE SURVIVAL v2 PR #41以降の実行ロードマップ 計画

更新日: 2026-07-20

## プロンプト

直近のB02学校の実ゲーム読込テストで今後のプランを大きく変更した。次の作業としてB03とT04を、可能な範囲で同時並行に進めたい。同時並行できない場合は、どちらを優先すべきか判断する。

今後のプランを、一ブランチで完結できる作業単位へ分割し、並行できる作業は並行で進める方針で練り直す。

リーズニングエフォートのUltraを使った方がよいタスクと、Ultraでなくてもよいタスクを分ける。Ultraを使わない場合も、推奨モデルとMedium、Highなどの推奨リーズニングを記載する。

追加指示原文:

> なんだったら、ここで新しく今後のプランを練り直しても構いません。その場合も、一ブランチでできる作業単位でありつつ、並行でできる作業は並行でやるっていう方針で今後のタスクを練り直してみてください。

> 後でできれば、えっと、リーズニングエフォートのウルトラを使った方がいいタスクと、別にそうじゃないタスクを分けてほしいです。そうじゃない場合、リーズニングのレベルがあの、高ければ高いほどいいのか、それとも、ま、ソロは使うつもりだけど、標準や高いぐらいで済むレベルなのかっていうのもちょっと、おすすめのモデルとリーズニングエフォートっていうのもちょっと書いておいてください。

> わかりました。その方針で行きたいので、今後の用のプランファイルに書き出しておいてください。で、次以降はそのタスクをもとに、進めていきたいので、常に見るようにしておいてください。

> あ、あと、えっと、もうプルリク20、いや41はデベロップにマージをしました。なので、えっと、一番上のタスクはもう終わってるという前提で次進んでください。

> あと結局どのプラン、どのタスクワリアっていうの、タスクと、どれだったら、えっと、並行作業可能かっていうのを分かるようにしてほしいです。

今回の追加承認内容:

- T05を、全ビットモード共通の飛行・高さ安全を担当するT05-1と、完成戦闘・全光線・ゲームシステムを担当するT05-2へ分割する。
- T05-1では天井と屋外の高さ安全、`bit_window`／`bit_roof`固定経路の実飛行、特殊接続へ入る前のカーペット解除を実装する。T05-2では戦闘モード、全光線、集合、警報、公開処刑を実装する。
- 屋上は窓の設置対象外であるだけで、ゲーム、索敵、経路、戦闘の対象には含める。屋外と屋上はビット専用・双方向必須の`bit_roof`固定経路で接続する。
- `bit_window`と`bit_roof`はどちらも`LNK_*`で表し、`PRT_*`は使用しない。
- B03-0開始時は、`docs/plans/v2/T04/plan.md`の「B03-0へのビット通過窓・屋上接近引き渡し契約」を必ず読む。

承認済み方針:

- PR #41を`develop`へ統合した後、最新`develop`から後続タスクを開始する。
- B03とT04の全工程を同時並行にはしない。B03の設計確定と、学校資産を編集しないT04の高さ付きNavMesh基盤だけを並行する。
- 学校の`.blend`とGLBは常に単一担当とし、B03完了後にT04の実学校複数階統合を行う。
- 2階を三年生、3階を二年生、4階を一年生の教室とする。
- B03で2～4階の廊下、教室区画、扉、階段を通行可能にする。家具、小物、材質、最適化は後続のB03-2で仕上げる。
- B03-0でビット通過窓と屋上接近用固定経路の位置・本数・見た目をユーザー承認し、B03-1で承認済みの開口、Collider、`LNK_*`を学校資産へ追加する。
- T05はT05-1、T05-2の順に直列実行し、飛行・高さ安全の完成前に戦闘システム統合へ進まない。
- 以後の各タスクは、`docs/plan.md`、本ファイル、対象タスクの個別計画、`docs/branch_strategy.md`を必ず読む。

## ステップ

- [x] G0: PR #41のレビュー、必要な手動確認、修正を完了し、`develop`へマージする
- [x] T04-2Aを専用ブランチで実装し、B03-0への引き渡し契約と専用fixtureを確定する
- [x] B03-0を専用ブランチとworktreeで開始し、確定済みのT04引き渡し契約を設計正本として読む
- [x] B03-0: T04の引き渡し契約を読み、窓配置、ビット通過窓、屋上接近用`LNK_*`、各階用途、通行可能範囲、造形密度、共通部品、マテリアル方針を設計確定する
- [x] T04-2A: 学校`.blend`、GLB、NavMeshバイナリを変更せず、高さ付きNavMesh問い合わせを専用fixtureで実装する
- [x] T04-2AとB03-0をPull Requestで`develop`へ統合する
- [x] B03-1: 最終窓、壁、階段、手すり、屋上、プール、トイレ、2～4階の通行可能な構造、`NAV_*`生成元、承認済みの`bit_window`／`bit_roof`用`LNK_*`を仕上げる
- [ ] B03-2: 機能Objectの名前・位置・開口寸法を維持し、教室内装、家具、小物、共通部品、マテリアル、GLB容量、1080p表示を仕上げる
- [ ] T04-2B: B03最終GLBからNavMeshを再ベイクし、NPCの複数階移動、実窓・屋上接続分類、学校回帰試験を統合する
- [ ] T05-1: 全ビットモード共通の天井・屋外高さ安全、`bit_window`／`bit_roof`固定経路の実飛行、特殊接続前のカーペット解除を統合する
- [ ] T05-2: gun所持NPCとビットの完成戦闘モード、通常・固定・トラップ・動的ビームを含む全光線、集合、警報、公開処刑を3D空間へ統合する
- [ ] T06: 学校全域、音声、水中速度、全ゲームシステム、資源ライフサイクルを統合する
- [ ] T07: 性能、回帰試験、Web・Electron、仕様書を最終化する
- [ ] 各タスク完了時に対象個別計画と`docs/plan.md`の結果を更新する

## タスク単位と依存関係

| 順序 | タスク | 推奨ブランチ | 依存 | 主な所有範囲 | 並行可否 |
|---|---|---|---|---|---|
| 0 | G0 PR #41統合 | 完了 | なし | PR #41の現差分 | 2026-07-19完了 |
| 1A | B03-0 設計確定 | `codex/v2-b03-design-freeze` | G0 | B03計画、T04引き渡し契約に基づく窓・`LNK_*`設計資料、承認記録 | 2026-07-20完了・統合済み |
| 1B | T04-2A 高さ付きNav基盤 | `codex/v2-t04-height-nav-core` | G0 | `navigationWorld`、`navigationAgent`、T04専用fixture、T04計画 | 2026-07-20完了・統合済み |
| 2 | B03-1 建築仕上げ | `codex/v2-b03-architecture` | B03-0、T04-2A | 学校`.blend`、GLB、窓、建築形状、`NAV_*`生成元、`bit_window`／`bit_roof`用`LNK_*`、確認画像 | 2026-07-20実装完了・Draft PR工程 |
| 3 | B03-2 内装・最適化 | `codex/v2-b03-interiors` | B03-1 | 同じ学校`.blend`、GLB、内装、共通部品、材質、最適化。機能Objectは維持 | 単独 |
| 4 | T04-2B 学校複数階統合 | `codex/v2-t04-school-multifloor` | T04-2A、B03-2 | NavMeshバイナリ、カタログハッシュ、実学校NPC回帰、実窓・屋上接続分類 | 単独 |
| 5 | T05-1 ビット飛行・高さ安全 | `codex/v2-t05-bit-flight-safety` | T04-2B | 全ビットモード共通の天井・屋外高さ安全、`bit_window`／`bit_roof`固定経路実飛行、カーペット解除 | 単独 |
| 6 | T05-2 戦闘システム | `codex/v2-t05-combat-systems` | T05-1 | gun所持NPC・ビット戦闘、全光線、集合、警報、公開処刑 | 単独 |
| 7 | T06 学校全体統合 | `codex/v2-t06-school-integration` | B03-2、T05-2 | 学校全域、音声、水中、ゲーム進行、資源破棄 | 単独 |
| 8 | T07 性能・回帰・仕様書 | `codex/v2-t07-regression-docs` | T06 | 性能、回帰、最終仕様書 | 単独 |

## 並行実行Wave

| Wave | 同時に開始するCodexタスク | worktree・ブランチ条件 | 完了後に開始できるタスク |
|---|---|---|---|
| 0 | G0 PR #41統合 | 完了済み。`origin/develop`の`bd58b84` | Wave 1 |
| 1 | B03-0とT04-2A | 並行実装・`develop`統合とも完了 | B03-1（完了） |
| 2 | B03-1だけ | 学校`.blend`とGLBの単一担当worktreeで実装完了。Draft PR工程 | B03-1のPR統合後にB03-2 |
| 3 | B03-2だけ | B03-1マージ後の最新`develop`から学校資産を引き継ぐ | T04-2B |
| 4 | T04-2Bだけ | B03最終GLBを入力とし、NavMesh、カタログ、Runtime回帰を担当する | T05-1 |
| 5 | T05-1だけ | T04-2Bマージ後の最新`develop`。学校バイナリは変更せず、確定済み`LNK_*`を使う | T05-2 |
| 6 | T05-2だけ | T05-1マージ後の最新`develop`。学校バイナリは変更せず、完成戦闘とゲームシステムを担当する | T06 |
| 7 | T06だけ | B03-2、T04-2B、T05-1、T05-2がマージ済みの最新`develop` | T07 |
| 8 | T07だけ | T06マージ後の最新`develop` | v2リリース準備 |

## 並行作業可否マトリクス

`可`は別Codexタスク・別worktreeで同時実行できる組み合わせ、`不可`は依存関係またはファイル所有が重なるため直列にする組み合わせである。

| タスク | B03-0 | T04-2A | B03-1 | B03-2 | T04-2B | T05-1 | T05-2 | T06 | T07 |
|---|---|---|---|---|---|---|---|---|---|
| B03-0 | - | 可 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 |
| T04-2A | 可 | - | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 |
| B03-1 | 不可 | 不可 | - | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 |
| B03-2 | 不可 | 不可 | 不可 | - | 不可 | 不可 | 不可 | 不可 | 不可 |
| T04-2B | 不可 | 不可 | 不可 | 不可 | - | 不可 | 不可 | 不可 | 不可 |
| T05-1 | 不可 | 不可 | 不可 | 不可 | 不可 | - | 不可 | 不可 | 不可 |
| T05-2 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 | - | 不可 | 不可 |
| T06 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 | - | 不可 |
| T07 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 | 不可 | - |

Wave 1のB03-0とT04-2Aは`develop`へ統合済みで、Wave 2のB03-1も実装・検証を完了した。B03-1のPull Requestを`develop`へ統合した後、次はWave 3のB03-2だけを開始する。その他のタスクでも読み取り専用の調査、テスト実行、レビューは補助的に並行できるが、別の成果ブランチとして同時実装しない。

## ファイル所有規則

- B03-1とB03-2だけが学校`.blend`とGLBを編集する。同時に複数ブランチから編集しない。
- B03-0は学校バイナリを編集せず、T04計画の「B03-0へのビット通過窓・屋上接近引き渡し契約」に従って`bit_window`と`bit_roof`の位置・本数・見た目を承認する。
- B03-1は最終形状に一致する`NAV_*`生成元、承認済みの窓開口・Collider・`LNK_*`、T05-2に必要な承認済み`MRK_*`／`VOL_*`を学校資産へ含めるが、NavMesh RuntimeとNPCロジックは変更しない。
- B03-2は`LNK_*`、Collider、`MRK_*`、`VOL_*`の名前・位置と窓開口寸法を変更せず、内装、材質、容量、表示だけを仕上げる。
- T04-2Aは学校`.blend`、GLB、`.navmesh.bin`、`stageCatalog.ts`の学校ハッシュを変更しない。
- T04-2BはB03最終GLBを入力として`.navmesh.bin`を再生成し、カタログハッシュとRuntime回帰を更新する。学校`.blend`またはGLBの形状修正が必要になった場合は、T04ブランチへ混在させずB03修正ブランチへ戻す。
- `bit_window`と`bit_roof`は`LNK_<id>_A/B`だけで表し、`PRT_*`を追加しない。両接続はビット専用・双方向必須とし、屋上をゲーム、索敵、経路、戦闘の対象から除外しない。
- T05-1は確定済み`LNK_*`を消費するRuntime飛行・高さ安全だけを所有し、学校`.blend`、GLB、NavMeshバイナリ、カタログハッシュを変更しない。
- T05-2は戦闘状態、全光線、集合、警報、公開処刑のRuntimeだけを所有し、学校バイナリを変更しない。不足する意味Objectが判明した場合はT05-2へ混在させず、B03資産修正を先に`develop`へ統合してNavMeshとハッシュを再検証する。
- 並行担当は`docs/plan.md`と本ファイルを編集しない。全体計画は統合担当だけが更新する。
- 各担当は自分の個別計画だけを1ステップごとに更新する。

## モデルとリーズニングエフォート

リーズニングは高ければ常に良いとは限らない。複雑なアルゴリズム、複数システム統合、原因が複数候補にまたがる不具合では高い設定を使う。仕様確定後の造形、機械的検証、文書整理ではMediumまたはHighを使い、時間と使用量を抑える。

| タスク | 推奨モデル | 推奨リーズニング | Ultra判断 | 代替 |
|---|---|---|---|---|
| G0 PR #41最終統合 | GPT-5.6 Sol | High | 通常は不要。全差分の再監査をやり直す場合だけUltra候補 | Sol Extra High |
| B03-0 設計確定 | GPT-5.6 Sol | High | 不要。ユーザー承認と設計比較を重視する | Sol Medium |
| T04-2A 高さ付きNav基盤 | GPT-5.6 Sol | Ultra | 推奨。重複床、階段、誤投影、到達可能性を同時に扱う | Sol Extra High |
| B03-1 建築仕上げ | GPT-5.6 Sol | High | 不要。仕様確定後のBlender作業と視覚検証が中心 | Sol Medium |
| B03-2 内装・最適化 | GPT-5.6 Sol | High | 通常は不要。配置中心ならMediumへ下げてよい | Sol Medium |
| T04-2B 学校複数階統合 | GPT-5.6 Sol | Ultra | 推奨。実GLB、NavMesh、NPC、窓、回帰を横断する | Sol Extra High |
| T05-1 ビット飛行・高さ安全 | GPT-5.6 Sol | Ultra | 推奨。全モードの天井・屋外高さ制約と特殊接続遷移を同時に扱う | Sol Extra High |
| T05-2 戦闘システム | GPT-5.6 Sol | Ultra | 推奨。戦闘状態と複数の光線・集合・警報が相互作用する | Sol Extra High |
| T06 学校全体統合 | GPT-5.6 Sol | Ultra | 推奨。多数のサブシステムと資源ライフサイクルを統合する | Sol Extra High |
| T07 性能・回帰 | GPT-5.6 Sol | High | 原因不明の複合ボトルネックだけUltra候補 | Sol Medium |
| T07 文書整理 | GPT-5.6 Terra | Medium | 不要。読取、結果整理、仕様書反映が中心 | Sol Medium |

CodexアプリのUltraは、対応モデルで最大推論を使用し、環境と指示が許す場合は並列委譲にも向く。サブエージェントを使用する場合でも、書込みはファイル所有が分離できる場合だけとし、学校バイナリ資産は常に単一担当とする。

## 各タスク開始時の必読順序

1. `docs/plan.md`
2. `docs/plans/v2/next_tasks_plan.md`
3. 対象タスクの`docs/plans/v2/<ID>/plan.md`
4. `docs/branch_strategy.md`
5. 対象範囲に関係する3D資産・Runtime仕様書と先行タスク結果

B03-0に限り、上記に加えて`docs/plans/v2/T04/plan.md`の「B03-0へのビット通過窓・屋上接近引き渡し契約」を設計開始前に必ず読む。

開始時は`date`、`git status`、`git branch`、`git log`、`git worktree list`、`git fetch origin --prune`を実行し、最新`origin/develop`から専用ブランチとworktreeを作る。未マージの依存ブランチから派生させない。

## 結果

2026-07-19、PR #41以降の実行順を再編した。B03全体とT04全体の完全並行は行わず、B03-0設計確定とT04-2Aコード専用技術検証だけを並行する。学校バイナリ資産はB03-1、B03-2の順で単一担当が仕上げ、T04-2Bは完成したGLBからNavMeshと実学校回帰を統合する。T05は、全ビットモード共通の天井・屋外高さ安全、`bit_window`／`bit_roof`固定経路実飛行、カーペット解除を担当するT05-1と、完成戦闘、全光線、集合、警報、公開処刑を担当するT05-2へ分割し、この順で直列実行する。屋上は窓の設置対象外に限り、ゲーム、索敵、経路、戦闘の対象として維持する。特殊接続は`PRT_*`を使わず、ビット専用・双方向必須の`LNK_*`とする。各タスクの推奨モデルとリーズニングを定め、T04-2A、T04-2B、T05-1、T05-2、T06はGPT-5.6 SolのUltraを推奨し、造形、性能確認、文書整理はMedium～Highを基本とした。PR #41は2026-07-19に`develop`へマージされ、`origin/develop`の`bd58b84`で統合を確認した。T04-2Aは専用ブランチで実装・検証を完了し、T04計画の引き渡し契約をB03-0へ渡せる状態にした。次はB03-0で窓と屋上接続の位置・本数・見た目を設計し、ユーザー承認後に固定する。B03-1はB03-0の承認とT04-2Aの`develop`統合後に開始する。

2026-07-20、T04-2AはPR #43で`develop`へ統合済みとなり、B03-0も承認済み設計の確定を完了した。B03-0では`D01`～`D10`、修正版W02-R1の81窓・開放率40.7%・体育館南面完全窓なし、W07案3の`bit_roof`2経路、T04契約、B03-P小物22種の利用先、設計図10枚を確定した。学校`.blend`、GLB、NavMesh、ゲームコードは変更していない。B03-0のPull Requestを`develop`へ統合した後、次はB03-1だけを開始する。

同日、B03-0統合後のB03-1で学校を768 Object／674 Meshへ更新し、81地点を左右2枚引違いユニット2～4組の連続窓帯として実装した。片側開放羽33件、閉じた羽の`COL_ActorOnly_WindowFixed_*` 33件、`bit_window` 33組、`bit_roof` 2組、2～4階と屋上の`NAV_*`生成元、全屋内天井Collider、`VOL_PoolWater`を含む。生成・GLB監査、連続2回生成一致、7枚の1080p画像、GLB単体ブラウザ表示、通常・T01～T04ビルドに合格し、NavMesh、B03-Pライブラリ、`src/`の不変を確認した。次はB03-1のPull Requestを`develop`へ統合後、B03-2で機能Objectを維持したまま内装・材質・最適化を仕上げる。
