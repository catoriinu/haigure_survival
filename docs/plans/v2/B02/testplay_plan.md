# HAIGURE SURVIVAL v2 B02＋T03 実ゲーム読込・テストプレイ 計画

更新日: 2026-07-19

## プロンプト

# HAIGURE SURVIVAL v2 B02＋T03 実ゲーム読込・テストプレイ引き継ぎ

### 手動テスト後の追加指示

- 階段を上がると速度が急落し、操作が効かないまま非常に遅く上がり続ける。表示は段のまま、移動とカメラは連続スロープとして滑らかに昇降させる。
- ビットのビームを壁で遮り、将来の窓だけは透過させる。
- ビットの通常索敵は既存の扇形視野と距離に加え、壁・障害物越しには標的を取得しない。窓越しは視認可能とする。
- アラート経由の標的共有は壁越しでも維持する例外とする。ただしビーム自体は壁で止める。
- アラート標的や窓越し標的へ直進すると障害物へ衝突する場合、最短経路で壁を回り込む。洗脳されたNPCも学校1階では同様に対応し、階段・複数階はT04へ残す。
- 階段など同色面の境界が分かりにくい点は、B03で修正予定なら今回は変更しなくてよい。
- 修正は機能単位でコミットを分け、pushは後でまとめて行う。

### 2026-07-19 3D空間基盤への方針変更

- V2ではJSON文字マップとセルナビゲーションを完全に廃止し、GLBの3D形状、3Dマーカー、3Dボリューム、NavMeshをもとにすべての処理を行う。
- スポーン、NPC・ビット移動、視線、ビーム、集合、禁止領域、境界を3D空間問い合わせへ書き換える。TypeScriptにはステージIDと資産URLなど非空間情報だけを保持する。
- 既存8ステージは一旦V2の対象外とし、互換処理を作らない。将来それぞれをGLB・NavMesh化して学校と同じ契約へ再導入する。
- 当面は学校だけを通常ゲームの対象とし、B02/T03テスト用に追加したJSON、全面床セル、セルBFSとその修正コミットを取り下げる。
- これまでT04～T06へ残していた恒久基盤の境界を見直し、本ブランチで学校の3D空間基盤と必要なNPC・ビット・通常ビーム・視線まで移行する。
- 階段の同色面表現は引き続きB03対象とし、今回の基盤移行では表示材質を変更しない。

### 2026-07-19 3D学校の第2回手動テスト指摘

- 主玄関、渡り廊下、体育館出入口では段差へ乗り上げられない、または挟まって移動不能になることがある。表示段を維持し、地面と床の間へ不可視の連続スロープを設ける。
- 移動不能時に`境界外移動の巻き戻し後の足元位置がBND_Stageの外側です。`が発生した。校庭南側でも塀の手前に見えない壁があり、体育館舞台脇階段では幅方向の片側だけガクついて登頂できない。
- 校舎3階段は1階天井との干渉を解消して吹き抜けにする。折返し後を閉鎖する不可視Colliderは、2階・3階内部が未実装であることを表す既知の制限として維持する。
- 階段下倉庫は最大視点高でも通常歩行可能にする。1階天井下面をZ＝3.0m、2階床面をZ＝3.6mとし、その間を厚さ0.6mの天井・床構造帯として、階段と踊り場を再設計する。
- 校舎全体を一様に1.2倍へ拡大する案は採用しない。
- gun持ちNPCの発射・モード遷移、ビットの完全な戦闘状態機械はT05で実装し、BGM・SE・VOICEのV2接続はT06で実装する。今回の移動修正へ仮実装を追加しない。
- 未洗脳時の照準を非表示にし、操作説明を従来のWASD・Shift表記へ戻してマウス説明を削除する。
- 男女トイレの扉表示を削除する。北側校舎北側出入口と体育館西側の扉は、常時開放状態が明確になる位置へ退避させる。
- 階段の手すり形状と同色面の判別性はB03の受入条件として記録し、今回の衝突修正と分離する。
- 修正は機能単位で検証して個別コミットし、push・PR作成は行わない。

### 2026-07-19 3D学校の第3回手動テスト指摘

- 北側校舎南出入口の段差へ上がる時と、渡り廊下へ側面から乗る時に、`境界外移動の巻き戻し後の足元位置がBND_Stageの外側です。`が再発する。体育館出入口を含む床高差には、地面から床へ続く不可視の連続スロープを不足なく設ける。
- 北側校舎南出入口へ、常時開放状態の扉と表示用段差を追加する。出入口段差は幅だけを可変とし、蹴上、踏面、材質、不可視連続スロープの断面を全箇所で共通化する。
- 男女トイレは今回、薄い仕切り壁と個室扉を備えた3個室ずつのブロックアウトを追加する。男子トイレの小便器本体はB03へ残し、3基分の設置スペースと通路幅だけを確保する。
- 校舎3階段の下段ランプから中間踊り場へ移る直前に速度低下がないか、形状接続とT03接地・段差処理の双方から確認し、再現する場合だけ最小修正する。
- 問題は機能単位で検証して個別コミットし、push・PR作成は行わない。

### 2026-07-19 3D学校の第4回手動テスト指摘と4階化

- 北側校舎南出入口には、要求済みの常時開放扉と表示用ステップが実際の入口位置へ見える状態で必要である。前回の完了記録だけを根拠にせず、ゲーム画面で存在を確認する。
- 北側校舎南出入口の段差へ上がる時と、渡り廊下へ東側から乗る時に、コンソール例外が表示されなくても移動不能になる。添付スクリーンショットの位置・方向を再現点とする。
- 地面と床の間には不可視の連続スロープを設ける。渡り廊下の東側と西側はどちらも表示段を置かず、透明な移動Colliderだけにする。
- 負荷上問題がなければ、北側校舎と西側校舎を3階建てから4階建てへ拡張する。上層階内装の今回範囲は、実装前に確認する。
- 1階の普通教室3室は、北側と中央の2室を同じ区画を使う特別教室へ変更し、南側1室だけを普通教室として維持する。
- 北側校舎北西階段を屋上まで接続し、屋上出口には小部屋状の階段室と常時開放扉を設ける。
- 屋上の1階男女トイレ直上位置へ、同じ平面寸法の男女更衣室2室をブロックアウトする。
- 緊急の出入口・渡り廊下修正と4階化は別コミットに分け、push・PR作成は行わない。
- 実ゲームで例外を捕捉したまま無視・継続してはならない。更新処理で例外が起きた場合は必ずコンソールへ出し、ゲーム更新を停止して原因を確認できる状態にする。

## 目的

T03までのゲーム実装と、B02で完成・developへマージ済みの学校ブロックアウトを実ゲームへ読み込み、プレイヤーが校舎1階、校庭、渡り廊下、体育館を歩いて縮尺・衝突・階段移動をテストプレイできる状態を作る。

ユーザーが手動テストで見つけた問題は、このタスク専用ブランチ内だけで再現、修正、再検証する。2026-07-19の追加指示により、B03の表示仕上げは先取りしない一方、T04の3D空間基盤、NPC・ビットNavMesh移動、通常ビーム・視線遮蔽は本タスクへ統合する。

## Git・作業場所

- このタスクは独立worktreeを新設せず、メインworktree `D:\Users\draft\create\fps_survival20251226`を使用する。
- メインworktreeの`develop`はPR #40取込後の`8b25790efbd9afbd151fa3081a8f0faf095b5df5`までpull済みで、開始時点ではクリーンである。
- 最初に`date`、`git status`、`git branch`、`git log`、`git worktree list`を実行し、日時、既存変更、現在位置、最新develop、旧B02/T03 worktreeとの分離を確認する。
- メインworktreeがクリーンな`develop`であることを確認後、最新developから`codex/v2-b02-t03-gameplay-test`を作成して切り替える。
- 既に同名ブランチが存在する場合は勝手に削除・上書きせず、状態を確認してユーザーへ報告する。
- 旧B02ブランチ、T03旧ブランチ、Codexが先に作った停止済みテストworktree、他タスクのworktree・作業ファイルには触れない。
- T02、T03、B02のコミットをcherry-pickしない。最新developを正本とする。
- ユーザーの明示指示なしにコミット、push、Pull Request作成を行わない。

## 最初にUTF-8で最後まで読むファイル

- `docs/plan.md`
- `docs/branch_strategy.md`
- `docs/spec_stage_assets_v2.md`
- `docs/spec_stage_json.md`
- `docs/spec_technical.md`
- `docs/plans/v2/T01/plan.md`
- `docs/plans/v2/T02/plan.md`
- `docs/plans/v2/T03/plan.md`
- `docs/plans/v2/B01/approved_layout.md`
- `docs/plans/v2/B01/b02_handoff.md`
- `docs/plans/v2/B02/plan.md`
- `docs/plans/v2/B03/plan.md`
- `docs/plans/v2/T04/plan.md`
- `docs/plans/v2/T05/plan.md`
- `docs/plans/v2/T06/plan.md`

リポジトリ内の`AGENTS.md`は探さず、自動適用されたグローバル指示に従う。

## 計画管理

- 本テスト専用の計画を`docs/plans/v2/B02/testplay_plan.md`へ作成する。
- ユーザーの今回の指示をプロンプト欄へ反映する。
- 1ステップごとに計画を更新し、問題、再現手順、原因、修正、検証結果を記録する。
- B02の完成結果を書き換える必要がある場合は、B02計画にも最新結果だけを追記する。

## 確定状態

- T03までdevelopへマージ済みで、プレイヤーの高さ、重力、接地、段差、斜面、階段ランプ移動が実装済みである。
- B02はPR #40でdevelopへマージ済みである。
- B02成果物:
  - `assets/blender/v2/B02/b02_school_blockout.blend`
  - `public/stage-assets/v2/B02/b02_school_blockout.glb`
  - `docs/plans/v2/B02/images/`
- 現テストブランチのB02 Blenderは457 Object／432 Mesh、GLB出力は434 Objectである。内訳は`VIS_*`274件、`COL_*`147件、`NAV_*`8件、`META_Stage`1件、`MRK_*`1件、`VOL_*`2件、`BND_Stage`1件である。
- 学校1階、校庭、渡り廊下、体育館が今回のテスト対象である。
- 学校専用V2通常入口は接続済みである。2階・3階内部、B03ローポリ仕上げ、正式な複数ステージ選択は未実装である。

## 実装・検証範囲

1. 最新developで次を基準検査する。
   - `npm run build`
   - `npm run build:t01`
   - `npm run build:t02`
   - `npm run build:t03`
2. B02 GLBを、T03の実運用プレイヤー移動で読み込める学校専用TypeScriptカタログへ接続する。
3. JSONへ空間情報を持たせず、GLB内の3Dマーカー・ボリュームと事前ベイクNavMeshを`StageSpatialContext`へ接続する。
4. 学校GLB、3D NavMesh、スポーン、ステージ境界、衝突、視線・ビーム遮蔽を接続する。
5. Web版を実際に起動し、ユーザーが手動でテストプレイできるURL、操作方法、確認観点を提示する。
6. 自動確認だけで完了にせず、ユーザーの手動プレイ結果と修正指示を待つ。
7. 指摘された問題を専用ブランチ内で修正し、同じ場所・操作で再検証する。

## 最初のテスト項目

- 主玄関付近へ正しい縮尺と床高でスポーンできる
- 主玄関、北側出入口、副玄関の段差と扉を通行できる
- 普通教室3室、特別教室、廊下、トイレ前、階段前を歩ける
- 教室の前後引き戸開口を通行でき、壁をすり抜けない
- 北西・北東・南西階段を踊り場まで上り下りできる
- 階段ランプ、踊り場、閉鎖コライダー、転落防止壁が機能する
- 階段下倉庫予定の開放部へ入れる
- 校庭、開放渡り廊下、体育館へ連続して移動できる
- 体育館舞台脇、舞台階段、舞台、体育倉庫出入口を確認できる
- 外周塀、正門、東側通用口をすり抜けない
- 壁、床、天井へカメラやプレイヤーが不自然に食い込まない
- 床抜け、浮き、急落、見えない段差、進行不能、Z-fighting、欠落壁がない
- 1080p既定設定で重大な描画低下や読込エラーがない
- 学校の破棄・再読込でGLB、NavMesh、イベント購読などの資源が残留しない

## 今回は不具合扱いしない既知の未実装

- B03のローポリ仕上げ、机、椅子、ロッカー、小物
- B03の壁の実窓開口、透明ガラス、窓枠
- T05で完成させる固定光線、トラップ光線、動的ビーム、特殊ビット編隊と全ゲームシステムの3D統合
- T06のB03完成資産との最終統合、全機能・性能・資源ライフサイクル検証
- 2階・3階内部
- 窓ガイドが現時点で平面パネルであること

ただし、B02/T03だけで成立すべきプレイヤー移動、GLB表示、縮尺、1階衝突、階段、床高の問題は今回の修正対象とする。

## Blender資産を修正する場合

- Blenderを触る前に`bpy.data.filepath`、`bpy.data.is_dirty`、オブジェクト一覧を読み取り、ユーザーへ提示する。
- 現在開いているファイルが旧B02 worktree側、または未保存変更ありの場合、上書き、保存、破棄、初期化をせず停止して確認する。
- Blender変更が必要になった場合は、最初にメインworktreeの現テストブランチ側`assets/blender/v2/B02/b02_school_blockout.blend`へ「名前を付けて保存」し、現在パスがこの作業ブランチ側へ変わったことを確認してから編集する。
- T01、旧B02ブランチ、旧B02 worktreeの`.blend`・GLBを上書きしない。
- 修正後は`.blend`、GLB、確認画像、オブジェクト監査、T01非変更を再検証する。
- 同一B02バイナリを他セッションと並行編集しない。

## 問題修正方針

- 問題ごとに、場所、再現操作、期待結果、実際の結果を記録する。
- 原因がBlender形状、GLB書き出し、3D資産分類、NavMesh、T03プレイヤー移動、`StageSpatialContext`のどこにあるか切り分ける。
- セル互換やJSONフォールバックを追加せず、学校の3D空間基盤として必要な最小責務だけを実装する。
- 修正後は該当箇所、学校全体、T01～T03のうち3D基盤に残る検証を回帰確認する。既存8ステージはV2回帰対象に含めない。
- UTF-8（BOMなし）、`git diff --check`、括弧対応、ビルド、Git差分を最終確認する。

## 最初の進め方

まずGitと指定文書を確認し、過去の学校JSON・セル接続を安全に取り下げる。GLB意味オブジェクト契約、Recast事前ベイク、`StageSpatialContext`と`NavigationWorld`を実装し、学校だけを通常ゲームへ接続する。その後、ユーザーがプレイできるURLと操作方法を提示し、手動テスト結果を待つ。

## ステップ

- [x] 日時、Git状態、ブランチ、履歴、worktree分離、最新`develop`を確認する
- [x] 最新`develop`から`codex/v2-b02-t03-gameplay-test`を作成する
- [x] 指定された15文書をUTF-8で最後まで読む
- [x] T02/T03のローダー、プレイヤー移動、検証入口とB02 GLB構造を調査し、最小接続方法とT06境界を確定する
- [x] 最新develop基準の`npm run build`、`build:t01`、`build:t02`、`build:t03`を実行する
- [x] （廃止済み旧セル接続の履歴）学校テスト専用JSON、通常ゲームのテスト限定入口、スポーン、平面ナビゲーション、境界を実装する
- [x] （廃止済み旧セル接続の履歴）学校読込、実プレイ開始、既存ステージ切替、資源破棄を自動・実ブラウザで検証し、詳細な歩行・階段確認を手動テストへ引き継ぐ
- [x] （廃止済み旧セル接続の履歴）UTF-8（BOMなし）、JSON、TypeScript構文・括弧、`git diff --check`、Git差分を確認する
- [x] 初回Web版を起動し、URL、操作方法、手動テスト項目をユーザーへ提示する
- [x] 初回のユーザー手動テスト結果を受け取り、問題ごとに再現・原因・修正・再検証を記録する
- [x] 手動テスト指摘を調査し、階段はT03接地補正、ビームと索敵はGLB壁未接続、AI迂回は学校ナビゲーション全面床が原因と切り分ける
- [x] 学校テスト入口を単独ビルド確認し、機能単位でコミットする
- [x] 階段斜面上の停止・逆方向入力・速度低下を修正して検証する
- [x] JSON・セル依存の限界を調査し、V2では学校の3D空間基盤へ全面移行する方針を確定する
- [x] 学校テストJSON、セルナビ、セルBFSの暫定コミットを安全なrevertコミットで取り下げる
- [x] GLB意味オブジェクト、3Dマーカー・ボリューム、NavMesh派生物の資産契約を確定する
- [x] Recast NavMeshの生成・バイナリ往復・3D経路を小型検証する
- [x] 学校の階段・舞台斜面コライダーをNavMesh入力に使える閉じた外向きMeshへ正規化する
- [x] 北側出入口の0.30m箱段差コライダーを外側地面から1階床へ続く連続スロープへ置き換える
- [x] 学校GLBへ`META_*`、`MRK_*`、`VOL_*`、`BND_*`、`NAV_*`を追加し、事前ベイクNavMeshの代表10経路を検証する
- [x] 学校用`StageSpatialContext`、`NavigationWorld`、TypeScriptカタログを実装する
- [x] セル非依存の共通3D経路追従とVolume内NavMesh生成位置を実装する
- [x] 学校専用V2プレイヤーランタイムを新設し、通常ゲーム入口を切り替える
- [x] プレイヤー、NPC、ビット、通常ビーム、ビット視線を3D空間基盤へ移行する
- [x] V2実行経路からステージJSON、`GridLayout`、`FloorCell`、セル座標処理を削除する
- [x] 全ビルドとBlender・GLB・NavMesh監査を最終確認する
- [x] 第2回手動テスト指摘を再現し、南塀と舞台階段の幅方向差を`BND_Stage`内部判定、出入口停止を段差形状、踊り場停止を天井干渉へ切り分ける
- [x] `BND_Stage`の単一Ray偶奇判定を閉Mesh用の安定した内部判定へ置き換え、南塀・舞台階段・表面・代表内外点を回帰検証する
- [x] 主玄関、渡り廊下、体育館校庭側出入口の衝突面を連続ランプ化し、同じ面からNavMeshを再ベイクする
- [x] 1階天井下面Z＝3.0m、2階床面Z＝3.6m、階段下有効高2.3mを前提に、3階段と吹き抜けを再構築する
- [x] 男女トイレ扉を削除し、北側出入口と体育館西側の扉表示を常時開放位置へ修正する
- [x] 未洗脳時の照準を非表示にし、操作説明を既存のWASD・Shift表記へ戻す
- [x] 戦闘・音声・階段手すり・複数階ナビゲーションの後続範囲をB03・T04・T05・T06へ記録する
- [x] 全ビルド、実ブラウザ、Blender・GLB・NavMesh、UTF-8、括弧、Git差分を再検証する
- [x] 第3回手動テスト指摘について、現ブランチ、Blender保存状態、出入口形状、境界巻き戻し処理、階段ランプ接続を再監査する
- [x] 境界外巻き戻し時は再接地せずフレーム開始時の高さ状態を復元し、再同期時のX/Zも固定して回帰検証する
- [x] 北側校舎南、渡り廊下側面、体育館出入口へ共通断面の表示段差・常時開放扉・不可視連続スロープを整備する
- [x] 男女トイレへ薄壁の3個室ずつを追加し、男子小便器3基分の空間を確保する
- [x] 校舎3階段の踊り場直前速度を計測し、接地斜面移動と水平慣性の原因を修正する
- [x] GLB・NavMeshを再生成し、機能単位のコミット、全ビルド、実ブラウザ、資産・UTF-8・Git監査を行う
- [x] 第4回指摘を現Blenderと添付画面で再監査し、入口たたきの誤転用、東側ランプ欠落、西側表示段混入を原因として記録する
- [x] 北側校舎南出入口へ開放扉と共通表示ステップを正しい位置で復元する
- [x] 渡り廊下東西側へ表示を持たない不可視連続ランプを追加する
- [x] 北側校舎南出入口と渡り廊下東西両方向の横乗りをGLBと実`playerController`で自動検証する
- [x] 緊急修正後のGLB・NavMesh・T02～T04・通常ゲームを検証して独立コミットする
- [x] V2実ゲームの例外経路を監査し、握りつぶしを除去してコンソール通知と停止を独立コミットする
- [x] 4階化の寸法、Object再利用、描画負荷、境界、屋上導線を監査する
- [ ] 4階化前に、中央・北教室の統合方法と上層階の今回通行範囲をユーザーへ確認する
- [ ] 北側校舎・西側校舎の4階外形、北西階段の屋上導線、屋上階段室と更衣室2室、1階教室用途変更を実装する
- [ ] 4階化後のGLB・NavMesh・全ビルド・実ブラウザ・通常ゲーム性能を検証して独立コミットする
- [ ] 3D移行後の学校をユーザーが再テストし、結果を記録する

## 結果

- 問題41（自動再検証済み・手動再テスト待ち）: スクリーンショットのRuntime座標`(-0.412, -0.124, -8.283)`はBlender約`(1.648, 33.132, -0.496)`で、渡り廊下ではなく北側校舎南面Y＝32.5mの開口直内側だった。この真の開口X＝0.15～5.4mには扉、表示段、Rampが一件もなく、足元は床・地面より下かつ`BND_Stage`下端の約0.004m上まで落ちていた。前回の`VIS_NorthSouthEntryStep01/02`と`COL_NorthSouthEntryRamp`は実際には渡り廊下西側X＝37.4～39.4mへ置かれ、東側X＝43.4～45.4mは未実装だった。真の南口へ共通表示2段、不可視Ramp、収納状態の開放扉2枚、Lintelを新設し、西側の表示段2件を南口へ移動、Bridge西Rampを意味改名、東側へ鏡像の不可視Rampを追加した。Blenderは463 Object／438 Mesh、`VIS_*`277件、`COL_*`150件、`is_dirty=false`で保存済みである。GLBは781,556 bytes、SHA-256は`f9eb4ce3eac6a859730787a7433335ec9ab6899dc70e97e62a2181d979aebd0d`、NavMesh SHA-256は`54a053288090553a206e39bec69c5e38f8c5cb7aedbf00875fb321a7ebbc5ee4`で再生成2回の一致を確認した。NavMesh代表17経路がPASSし、T02実ブラウザでは実`playerController`が南口・Bridge西・東を各0.8 world units通過し、最高足元Y＝0.002、全フレーム境界内、例外なしで22/22 PASSした。`npm run build`とT01～T04、T03実ブラウザ24/24、T04実ブラウザ39/39、通常ゲーム読込とコンソールエラー0件を確認し、`fix: 北側校舎南口と渡り廊下の段差を修正`として`9d77952`へ独立コミットした。ユーザーによる同一地点の手動再テストは未実施である。
- 問題42（実行時例外通知・自動検証済み）: V2実ゲームのcatchを全件監査した結果、例外を空捕捉していたのはポインターロック要求のPromise rejection 1件だけで、ステージ読込、NavMesh、NPC、ビット、Survivalのcatchは資源解放後に再throwしていた。今回の無言の移動不能は例外の捕捉ではなく、`playerController`が境界外移動をフレーム開始位置へ戻して慣性をリセットする通常ロールバックだったため、外周接触まで致命エラーにしないよう維持した。初期化失敗は`console.error`後に再throw、ポインターロック失敗は`console.error`、ゲーム更新・描画失敗はRenderLoop停止、`console.error`、再throwへ統一した。T02の実プレイヤー通過検証も捕捉例外をコンソールへ転送する。`npm run build`、`npm run build:t02`、T02実ブラウザ22/22、通常ゲーム読込とコンソールエラー0件を確認し、`fix: V2実行時例外をコンソールへ通知`として`6220141`へ独立コミットした。
- 4階化監査（設計確認待ち）: 現GLBは約0.78MB、T04実ブラウザの学校読込は51.60msで、既存Materialを再利用しMeshを面・階段flight単位でまとめる低ポリblockoutなら4階化は可能である。4階はZ＝9.6～12.6m、屋上床はZ＝12.6m、`BND_Stage`上端は15.0mを候補とし、北西階段を既存shaft内で屋上へ延長する。屋上階段室はX＝-12.6～-6.6m／Y＝38.9～45.5m、男女更衣室は1階トイレ直上の同一平面寸法を候補とする。プレイヤーの屋上通行は現基盤で可能だが、NPC・ビットの複数階追跡はT04の高さ付きNavMesh後続へ残す。中央・北教室を約9.1×20mの特別教室1室へ統合するか、別々の特別教室2室にするか、および2～4階の今回通行範囲は未確定である。
- 問題36（プレイヤー制御修正・自動検証済み）: 北側校舎南出入口と体育館北出入口の中央経路は床高Z＝0.00mで連続し、現GLBと現`BND_Stage`では例外を直接再現しなかった。一方、境界巻き戻し後の`resync()`はCollider共有辺付近で最大約0.008mの水平変位を起こし、共有辺上では支持面を失う場合があった。境界外移動時は新しい接地判定を行わず、既知の境界内フレーム開始位置、接地状態、垂直速度、支持面高、支持Mesh、支持面法線を完全復元する方式へ変更した。spawn再配置の`resync()`は維持し、純垂直衝突の前後でX/Zを固定した。T03へ再同期の水平位置保持と高さ状態完全復元を追加し、実ブラウザでPASSした。渡り廊下側面の0.30m床高差は独立した不足スロープとして資産側で修正する。
- 問題37（資産修正・自動再検証済み）: 出入口は総高0.30m、各蹴上0.15m、各踏面1.00m、黄色の表示2段を共通断面とし、幅だけを主玄関5.0m、北側校舎北口3.0m、北側校舎南口・渡り廊下西側6.0m、体育館校庭側3.0mへ合わせた。移動衝突は段ごとの箱を残さず、4か所とも地面Z＝-0.30mから床Z＝0.00mへ続く単一の`COL_*Ramp`へ統一した。北側校舎南口と体育館渡り廊下側は既存の青い扉を開口外へ収納した常時開放状態で維持した。学校GLBとNavMeshを2回生成して同一SHA-256を確認し、入口形状、開放扉、校庭から北側屋外・渡り廊下・体育館への経路をT02実ブラウザ20/20で検証した。`fix: 学校出入口の共通段差とスロープを整備`として`2a71b89`へ分離コミットした。
- 問題38（資産修正・自動再検証済み）: 男女トイレの北側へ各3個室を配置した。内部仕切りは厚さ0.08m、奥行2.10m、高さ2.10mの表示壁と通常Colliderを分離し、各個室には幅0.90m・高さ1.80mの簡易扉を90度開放状態で置いた。男子側東壁沿いのX＝-2.55～-2.25m、Y＝39.0～42.2mを小便器3基の予約帯、X＝-3.55～-2.55mを立位空間として空け、中心Y＝39.6／40.6／41.6mをB03へ引き渡した。仕切りColliderを`NAV_Blocker_School1F`へ含め、男女とも通路から個室へ到達する3D NavMesh経路を確認した。T02実ブラウザは21/21、実ゲームの学校GLB読込も成功し、`feat: 学校トイレの個室ブロックアウトを追加`として`a49019d`へ分離コミットした。
- 問題39（プレイヤー制御修正・自動検証済み）: 実B02の3階段では、平地の定常水平進捗`0.010449595/frame`に対し、修正前の斜面中腹は約44%まで低下する既存経路があり、接線移動だけでも約76%に留まった。斜面衝突で短くなった実移動を水平慣性へ戻すことと、接地用の垂直lift自体が斜面方向の横滑りを起こすことが原因だった。要求X/Zと支持面高差から接線3D変位を作り、垂直lift前後のX/Zを固定してから既存の`motion.commit(actual)`へ実移動だけを戻すよう変更した。実B02の北西・北東・南西階段はactual/requested=`1.000000`、終端前後の負のY変位0frame、踊り場へ単調上昇し、北西斜面から手すり壁へ横移動した検証でも中心が正しい距離で停止して入力解除後drift 0だった。T03実ブラウザは平地比1.000、踊り場直前比1.006、全24項目PASS、console error 0件となった。Collider統合は本不具合の必須条件ではないため行わない。
- 問題40（検証更新・実ブラウザ再検証済み）: 出入口とトイレ追加後のT04は、学校GLBの厳格分類と資源再読込だけが旧期待値`VIS=261`、`COL=144`を参照して37/39項目となった。実資産契約どおり`VIS=274`、`COL=147`へ更新し、学校分類、実壁遮蔽、NavMesh経路、NPC・ビット、ビーム、資源破棄・再読込を含む39/39項目PASSを確認した。検証更新は`test: 学校資産のT04期待件数を更新`として`c64819d`へ分離コミットした。
- 第3回修正後の最終資産はBlender 457 Object／432 Mesh、GLB 434 Objectで、`.blend`は319,263 bytes・SHA-256 `8E62F099B6E0A483C8DCE5EA42132C7035E3CDDF653E83F954328D053A7E0162`、GLBは774,208 bytes・SHA-256 `C8799EE3C62438795E8153F9C006E66DD089EBCAAD7924DA1909F7E857F79BB8`、NavMeshは158,784 bytes・SHA-256 `571C6D488D174562FD6C3FE51E4856124D8F61CB0AE0B09D0A2FFD014A907744`である。GLB・NavMeshの再生成は決定的で、Blenderは現作業ブランチ側パス、`is_dirty=false`、T01 `.blend`・GLBは既知のSHA-256とGit差分なしを維持した。
- 自動検証で入口・トイレ・階段速度・境界巻き戻しの修正は完了した。最後の完了条件は、ユーザーが同じ北側校舎南口、渡り廊下側面、体育館出入口、3階段、男女トイレを実際に歩いて再テストすることである。
- 2026-07-19 00:39 JSTに作業再開時のGit状態を再確認した。現在ブランチは`codex/v2-b02-t03-gameplay-test`、HEADは最新developの`8b25790`から開始した学校テスト差分を含み、旧B01・B02 worktreeは分離されたままである。
- 手動テスト指摘の実装計画を確定し、全体のコミット判定は複数責務を含むため`SPLIT`とした。
- 学校テスト専用JSONとタイトル入口だけで`npm run build`が成功した後、`feat: B02学校テストプレイ入口を追加`としてコミット`1463f54`へ分離した。pushは行っていない。
- 問題2（修正・自動再検証済み）: B02階段ランプを上がると入力解除後も非常に遅く上昇し続け、方向転換が効かなくなった。期待結果は表示段と独立した連続ランプ上を通常の水平入力速度で滑らかに移動し、入力解除で停止することだった。原因は接地用の純垂直`moveWithCollisions`が斜面法線に沿うX/Z変位を生み、その実変位が水平慣性へ戻されていたことである。接地スナップ前のX/Zを保持して垂直補正後に復元し、接地補正を水平慣性から分離した。
- T03検証へ斜面中腹の入力解除と即時方向転換を追加した。実ブラウザは全19項目PASSで、水平ドリフト`0.000000`、逆方向進行`0.199500 / 0.2`、空中フレーム0だった。`npm run build:t03`と`npm run build`も成功し、`fix: 斜面上のプレイヤー移動を安定化`としてコミット`2c83349`へ分離した。
- 問題3（学校1階ナビゲーションを修正・自動検証済み）: 初期テストJSONは外周以外の全セルが床だったため、ビットと洗脳NPCの経路探索では校舎壁、塀、閉鎖地点、将来窓位置を通行可能と判定していた。期待結果は主玄関・各出入口・教室扉・渡り廊下・体育館入口だけを接続し、平面で表現できない階段と舞台昇降部をAI経路から除外することだった。B02 GLBの1階コライダー中心線と実開口を83列×71行へ反映し、床4,460セルが主玄関から4近傍ですべて到達可能な単一成分であること、スポーンと集合地点が床セルであることを確認した。`npm run build`と`git diff --check`も成功した。
- 問題4（ビット迂回追跡を修正・自動検証済み）: ビットの追跡は毎フレーム4近傍探索をやり直し、到達不能時には標的への直進へ戻るため、壁へ押し付けられた。また床・壁余裕・境界判定がワールド原点中央を前提としており、学校のカスタム原点では別セルを参照した。目標セル単位で最短経路をキャッシュし、到達不能時は停止して0.5秒間隔で再探索するよう変更した。アラート集合も同じ経路を使用し、カーペット爆撃は壁で編隊を解除して同じ標的への迂回追跡へ移る。カスタム原点と回転方向を持つ5×5検証グリッドで6セルの迂回経路と到達不能`null`を確認し、`npm run build`と`git diff --check`も成功した。
- 問題5（洗脳NPC迂回追跡を修正・自動検証済み）: 洗脳NPCには経路キャッシュがあったが、到達不能時に古い経路と目的地を残し、通常探索へ戻った後も以前の追跡方向へ動く可能性があった。共通4近傍・BFS・経路復元へ統一し、標的セル変更時または到着時だけ最短経路を更新するよう整理した。到達不能時は標的IDを保持しつつ現在位置で停止して次フレーム以降も再探索し、標的喪失時と離脱動作開始時は旧追跡経路を破棄する。`npm run build`、対象差分の`git diff --check`、UTF-8 BOMなしを確認した。
- 2026-07-19、問題3～5のセル実装は個別不具合を直しても3D階段、重複階、実メッシュ視線、将来の窓へ拡張できないため、完成成果として採用しないことをユーザーと確定した。履歴をresetせず、階段修正`2c83349`を保持したままセル関連コミットだけをrevertする。
- 3D空間基盤は、GLBを空間正本、Recast NavMeshバイナリをGLBから生成する派生物、TypeScriptカタログを非空間情報だけの索引とする。通常扉・階段は連続NavMesh、梯子・昇降機などだけを`LNK_*`で表す。
- Blenderは最初の監査で旧B02 worktree側が開かれdirtyだったため無変更で停止した。ユーザーが保存せず閉じて現作業側を開き直し、`bpy.data.filepath`が現worktree、`is_dirty=false`、全430オブジェクト、Mesh 407件、`VIS_`263件、`COL_`144件であることを再確認した。
- セル方式の5コミットを逆順に`git revert --no-commit`し、学校テストJSON、タイトル入口、カスタム原点補正、学校1階文字セル、ビット・NPCセルBFSだけを取り下げた。階段修正`2c83349`と未コミットの3Dビーム試作は保持した。`npm run build`と`npm run build:t03`の成功後、`revert: 学校テスト用セルナビゲーションを取り下げ`としてコミット`c94302e`へ分離した。
- NavMesh候補の`recast-detour` 1.6.4はViteのES module実行で初期化不能だったため採用しなかった。ESM対応の`recast-navigation` 0.43.1へ置き換え、セル・JSONを使わない小型3Dコースで初期化、扉迂回、連続ランプ、バイナリ往復、到達不能の5項目を実ブラウザ検証した。5/5項目PASS、warning・error 0件、生成4.40ms、NavMesh 11,116 byteだった。
- 問題6（3D NavMesh入力の前提修正・自動検証済み）: B02の校舎下段階段ランプ3件と体育館舞台ランプ2件には重複頂点由来の面積0面があり、これら5件を含む階段ランプ・上段ランプ・転落防止壁12件の閉じたMeshは面法線が内向きだった。期待結果は、表示段を変更せず、`COL_StairRamp_*`等をプレイヤー衝突と連続NavMesh生成の両方へ安定して使用できることである。5件を6頂点・5面の閉じた楔形へ整理し、12件すべてを正の符号付き体積へ正規化した。修正後は12件すべてで面積0面0、非多様体辺0、正の体積を確認した。
- Blenderは現作業ブランチ側`assets/blender/v2/B02/b02_school_blockout.blend`、`is_dirty=false`、430 Object、407 Mesh（`VIS_`263件、`COL_`144件）であることを再確認した。GLBも407 Node／407 Mesh、Material 20件、Camera・Light・Animation 0件、規約外名0件である。B02 `.blend`のSHA-256は`5ECCD2E04BDC1071D6FB2BCBA9DFD5784F26DB29C82009D505F1094C2F9215B4`、GLBは`AFC4D0C9E8FF19FFAFE55DD4B500F25D145AB6CBF2FECB720BD38551BB7B2C6F`となった。
- `npm run build`、`npm run build:t01`、`npm run build:t03`は成功した。T01の`.blend`は`A26672A7400A143F32FAB9F5DC53387CFF85C1FB22A9766F464E9BAEFAC5B16B`、GLBは`CF6CF17CF7447152D0B25219045C33EFB2E1A1821C201ACD6E51E39F468EA69B`のままで、対象パスのGit差分は0件だった。Blender連携のビューポート画像取得は既存カメラへ合わせても黒画像を返したため、表示メッシュ監査と後段の実ゲーム描画で視覚確認を補完する。
- 問題7（北側出入口の段差移動を修正・自動検証済み）: 北側出入口の`COL_NorthEntryStep`は、外側地面上面Z＝-0.30mから1階床Z＝0.00mまでを一度に上がる高さ0.30mの箱だった。T03プレイヤーの許容段差0.15mを超えるため、期待する外側から校舎内への連続移動が成立しなかった。表示用`VIS_NorthEntryStep`の8頂点・6面の箱は維持し、衝突用だけを同じX＝2.4～5.4m、Y＝45.5～47.0m、Z＝-0.3～0.0m境界内の1:5連続楔へ変更した。修正後のColliderは6頂点・5面、面積0面0、非多様体辺0、符号付き体積0.675m³、斜面法線Z＝0.980581である。
- 北側出入口修正後も430 Object、407 Mesh（`VIS_`263件、`COL_`144件）を維持し、GLBは407 Node／407 Mesh、Material 20件、Camera・Light・Animation 0件、規約外名0件だった。B02 `.blend`はSHA-256 `04CF5C0C8759737F21843BC231B0F460695D198EA1E662EA790933421BD6648E`、GLBは`1DFF477335398A62893DE048BF3F8073ECF9B19715C17BB9EE4296E67D5CCF74`である。`npm run build`と`npm run build:t03`は成功し、T01資産のGit差分は0件だった。実際の出入りは学校を通常ゲームへ再接続した後の手動確認へ残す。
- 問題8（学校3D空間正本・事前ベイクNavMeshを作成）: V2学校ではJSON文字マップとセルを空間正本にせず、同一GLB内の`META_Stage`、`MRK_*`、`VOL_*`、`BND_Stage`、`NAV_*`と、そのGLBから生成した事前ベイクRecast NavMeshを使用する。Blender監査は443 Object／418 Mesh、GLB出力対象420 Objectで、内訳は`VIS_*`263件、`COL_*`144件、`NAV_*`8件、`META_Stage`1件、`MRK_*`1件、`VOL_*`2件、`BND_Stage`1件である。制作ガイド23件は出力対象外を維持した。
- 問題9（体育館舞台へのNavMesh経路切断を修正・自動検証済み）: プレイヤースポーンから体育館舞台中央へ経路計算すると、Recastが部分経路を返し、要求終点との誤差が`0.762910`となった。東西の`VIS_GymStageStairHeadWall_*`と`COL_GymStageStairHeadWall_*`は下端Z＝2.4mだったが、舞台上面Z＝1.0mからの実開口はBlender 1.4m、Runtime 0.35mにしかならず、プレイヤー高さ約0.424mとNavMesh高さ0.425mを下回っていたことが原因である。表示壁・衝突壁双方の下端をZ＝3.4mへ上げ、舞台上面からBlender 2.4mの一致した開口を確保した。NavMeshだけをつなぐ仮connector面は不採用として削除した。
- 舞台開口修正後、主玄関スポーンから校庭、北側出入口外側、北西・北東・南西階段踊り場、体育館中央、体育倉庫、舞台中央などを含む代表10経路はすべて終点誤差`4.3e-7`以下となった。最終`.blend`は308,457 bytes、SHA-256 `42617927F6AD7AB4F4C8776CA3E8EC3999BA12E9DF90D9551342D0A97EEF5996`、GLBは739,444 bytes、SHA-256 `55A6EAE9AAA169E1CB6389C2D3924BED05D1F9530105DCF92F9F9808FCFEE381`、NavMeshは151,552 bytes、SHA-256 `C0699A5C371AB30A5B85241DEEE38A2603258DE8961A095AA5EF55031A4040E9`である。同一入力から2回ベイクしてNavMeshの容量・SHA-256が一致した。T01 `.blend`は`A26672A7400A143F32FAB9F5DC53387CFF85C1FB22A9766F464E9BAEFAC5B16B`、GLBは`CF6CF17CF7447152D0B25219045C33EFB2E1A1821C201ACD6E51E39F468EA69B`を維持した。
- `npm run bake:v2:school-navmesh`は部分経路を`success`として受理せず、代表10経路すべての要求終点との誤差`1e-5`以下を必須とする。`npm run build`、`npm run build:t01`、`npm run build:t02`、`npm run build:t03`、`npm run build:t04`はすべて成功した。
- 問題10（学校3D空間コンテキストを実装・自動検証済み）: 学校カタログはID、表示名、GLB／NavMesh URL、schema、NavMesh profile、確定SHA-256だけを保持し、空間座標を持たない。`StageSpatialContext`はGLB作者Node 420件を`VIS_*`263件、通常`COL_*`144件、`NAV_*`8件、意味Object 5件へ厳格分類し、プレイヤースポーン、NPC・ビット出現Volume、3D境界、Actor衝突、通常ビーム・視線遮蔽、事前ベイクNavMeshを同じ所有単位へまとめる。ActorOnly窓は移動だけを遮り、光線と視線を通す分類テストも追加した。
- T04実ブラウザ検証は実学校GLB／NavMeshのハッシュ照合、主玄関スポーン、境界包含、舞台までの完全経路、実壁遮蔽、資源破棄、再読込を含む12/12項目が初回・再実行ともPASSし、console warning／errorは0件だった。学校読込は約34～41msで、破棄後はMesh・Material・TransformNode件数が読込前と一致した。
- 問題11（NPCからビットへのアラート標的受け渡しを修正・自動検証済み）: 既存の`AlertRequest`は`targetId`と`blockerId`の意味が呼出箇所ごとに反転し、`applyAlertRequests`が両方を`blockerId`として`ExternalAlert`へ渡していた。このため、警報を出したNPCと追跡対象が同じIDへ潰れていた。型を`leaderId`と`targetId`へ変更し、生存NPCがプレイヤーに塞がれた場合はNPCを発信者・プレイヤーを標的、洗脳NPCが未洗脳者へ接触した場合は洗脳NPCを発信者・接触相手を標的として統一した。`npm run build`と対象差分の`git diff --check`は成功した。
- 問題12（共通3D経路追従を実装・自動検証済み）: NPC・ビットのセルBFSを置き換える共通エージェントは、NavMeshへ投影した始点・標的の完全経路をキャッシュし、標的移動閾値、定期期限、経路消費、stuck時だけ再探索する。到達不能または移動拘束失敗時は現在位置で停止し、直進へフォールバックしない。斜面を含むXYZ経路を速度×deltaで複数点跨いで進み、投影終点への到着を判定する。T04実ブラウザは20/20項目PASS、console warning／error 0件、`npm run build:t04`成功だった。
- 問題13（Volume内3Dスポーン抽選を実装・自動検証済み）: NPC・ビット出現位置をセル記号から選ぶ代わりに、GLBの`VOL_*`内で候補を生成し、NavMeshへ投影した後もVolume内にある点だけを採用する。複数個体は3D最小距離と重複排除を満たし、候補枯渇時はVolume IDと棄却理由別件数を含む例外で停止する。T04へ単一点、複数点、重複候補、NavMesh投影失敗、不正乱数の5検証を追加し、実ブラウザは既存20項目を含む25/25項目が初回・再実行ともPASS、console warning／error 0件、`npm run build:t04`成功だった。
- 問題14（学校専用V2プレイヤーランタイムを実装・自動検証済み）: 通常ゲーム入口を`src/v2/main.ts`へ切り替え、学校JSON、セル座標、既存8ステージ選択を介さず、GLB／NavMeshの`StageSpatialContext`から学校だけを読み込む。GLBの`player_spawn` world座標・前方へ足元基準で配置し、Actor衝突集合とT03の重力・接地・段差・斜面移動を再利用し、`BND_Stage`外への移動は同じフレーム開始位置へ戻して慣性と接地を再同期する。実ブラウザは`http://127.0.0.1:5175/`で読込完了、クリック開始、足元`(0.375, 0.002, 0.000)`の接地、W入力、console warning／error 0件を確認し、`npm run typecheck:v2`と`npm run build`も成功した。
- 問題15（V2アラート共有基盤を実装・自動検証済み）: NPCとビットが壁越しでも維持できるのは通常視認ではなくアラート由来の標的だけに限定するため、`leaderId`と`targetId`を期限付きで共有する基盤をセル実装から分離した。同じ通知の再発行は期限を更新し、期限切れで削除する。leaderとtargetの同一IDは厳格に拒否する。T04実ブラウザは27/27項目PASS、console warning／error 0件、`npm run build:t04`成功だった。
- 問題16（V2プレイヤー型検査の隠れたセル依存を解消）: `playerMotion.ts`は座標処理にセルを使わないが、入力軸の型だけを旧`playerAbility.ts`からimportしていたため、`GridLayout`、`FloorCell`、旧NPC型がV2型検査対象へ連鎖していた。入力軸型を汎用移動制御側へ移し、V2のコンパイル依存から旧セル型を除外した。`npm run typecheck:v2 -- --listFiles`相当の監査、`npm run build`、`npm run build:t03`は成功した。
- 問題17（V2実行依存のJSON・セル再混入をビルドで禁止）: 通常入口を学校専用V2へ変えただけでは、将来のtype-only importや補助モジュールから旧セル処理が再流入する可能性がある。通常Web入口と全V2モジュールの相対import依存を走査し、JSONファイル参照、`GridLayout`、`FloorCell`、`NavCellRef`、旧grid／stageContext／stageSelection／セルBFSを検出した時点で失敗する監査を`typecheck:v2`の必須前段へ追加した。現時点はV2モジュール8件・TypeScript依存18件でPASSし、`npm run build`も成功した。
- 問題18（通常ビームの3D壁遮蔽を実装・自動検証済み）: ビームの新位置だけを判定すると高速フレームで薄い壁を飛び越え、壁だけを先に判定すると壁より手前のActor命中を失う。前フレーム位置から新位置までの通常COL壁と球形Actorを同じ距離基準で比較し、最初の1件で停止・着弾するV2ビームシステムへ置き換えた。同距離は壁を優先し、ActorOnly窓はビームと視線を通す。T04実ブラウザはActor先行、壁先行、同距離、高速連続判定、窓透過、通常壁遮蔽、寿命失効を含む31/31項目PASS、console warning／error 0件、`npm run build:t04`成功だった。
- 問題19（洗脳NPCの3D迂回追跡を実装・自動検証済み）: NPCを`npc_spawn` VolumeからNavMesh上へ生成し、徘徊と標的追跡を共通3D経路追従へ移した。到達不能時は直進せず停止する。通常標的は距離・95度半角視野・通常COL視線遮蔽なしを毎フレーム再確認して壁で解除し、別Actorからのalert標的だけは期限中保持する。独立レビューで発信NPC自身が自分のalertを受信してvisualをalertへ変える不具合を検出し、発信者を受信対象から除外した。徘徊先も注入乱数からNavMesh投影する方式へ統一した。T04実ブラウザは32/32項目PASS、console warning／error 0件、`npm run build:t04`と`typecheck:v2`成功だった。
- 問題20（ビットの3D索敵・迂回追跡を実装・自動検証済み）: ビットを`bit_spawn` VolumeからNavMesh上へ生成し、探索・通常追跡・alert追跡を共通3D経路へ移した。通常標的は距離・100度半角視野・通常COL視線遮蔽なしのANDを取得時と保持中の毎フレームに要求し、壁または扇形外で解除する。別Actorのalertだけを期限中保持し、洗脳済みNPCは通常視認・alertの双方から除外する。独立レビューで自己alert再受信、保持時の扇形欠落、洗脳標的混入、NavMesh高を物理床高に代用、カーペットfollowerのNavMesh外移動を検出して修正した。カーペットはleader／follower候補の壁衝突またはNavMesh投影不能で編隊を解除し、leaderだけがNavMeshで扉へ迂回する。T04実ブラウザは36/36項目PASS、console warning／error 0件、`npm run build:t04`と`typecheck:v2`成功だった。
- 問題21（学校V2実ゲーム統合・自動検証済み）: 学校専用V2入口はプレイヤーだけを生成しており、個別に完成したNPC、ビット、アラート共有、通常ビームが同じ実ゲーム更新順へ未接続だった。`V2SurvivalRuntime`でプレイヤーSnapshot、NPC更新、ビット更新、警報発行、ビーム生成、連続衝突の順序と所有資源の破棄を統合した。通常Web入口はJSONやセルへ戻らず、学校GLB、事前ベイクNavMesh、3D遮蔽集合だけを使用する。`npm run typecheck:v2`はV2モジュール9件・TypeScript依存19件でJSON、セル型、旧セル実装なし、`npm run build`も成功した。実ブラウザは学校読込後にNPC 10体、ビット6体、編隊展開、視認・警報、通常ビームを同時更新し、1280×720で10秒継続してconsole warning／error 0件だった。
- 問題22（T02・T03検証の3D化・自動検証済み）: T02の旧8 JSON／共通セルローダー検証を学校GLBの作者Node、意味分類、資源所有、破棄・再読込を直接確認する14項目へ置き換えた。T03はJSON fixtureを外し、平面Meshと実GLBによる重力、接地、段差、斜面停止・逆走、資源切替を直接確認する19項目へ置き換えた。両画面とも全項目PASS、console warning／error 0件だった。
- 問題23（旧JSON・セル実装を削除・自動検証済み）: 旧8ステージJSON、`src/main.ts`、旧V2セル実装39 TypeScriptファイルを参照ゼロ確認後に削除した。依存監査は通常入口、9個のV2モジュール／19個の実行依存、T01～T04の22個の検証依存にJSON参照、旧セル型、旧セル実装がないことを確認した。
- 問題24（壁裏プレイヤーへの誤命中を修正・自動検証済み）: ビームのActor球へプレイヤー身長の半分を使うと球が壁裏からはみ出し、壁より手前のActorとして誤命中した。プレイヤー衝突幅の半分を半径へ統一し、正しい球では壁が先、旧半径ではActorが先になる回帰をT04へ追加した。
- 問題25（初期化失敗時の資源残留を修正・自動検証済み）: `StageSpatialContext`、NPC、ビット、`V2SurvivalRuntime`の生成途中で例外が起きると、一部MeshやMaterialがSceneへ残る経路があった。作成済み資源を逆順に破棄して元の例外を維持する構造へ統一し、注入例外後にScene件数が基準へ戻ることを検証した。
- 問題26（アラート期限更新順を修正・自動検証済み）: NPC更新前に期限を進めると、新規通知が生成フレーム内に減算され、NPCとビットが異なる期限Snapshotを見る可能性があった。同じSnapshotを双方へ配布した後に1回だけ減算し、新規通知が満額15秒から始まるよう更新順を確定した。
- 問題27（着弾表示を累積化・実ブラウザ確認済み）: HUDがフレームごとの壁着弾・標的命中だけを表示していたため、値がほぼ常に0へ戻った。実プレイ中の累積値へ変更し、壁着弾1件・標的命中69件が時間経過後も表示に残ることを確認した。
- 問題28（最終自動検証済み）: 2026-07-19 10:09 JSTに依存監査、通常Web＋Electron、T01～T04の全ビルドを再実行し、すべて成功した。T02 14/14、T03 19/19、T04 39/39の実ブラウザ結果、通常入口の学校読込、console warning／error 0件を維持した。`origin/develop`比111パスのうち現存テキスト58件は厳格UTF-8・BOMなし、Git差分検査はPASS、B02 `.blend`／GLB／NavMeshの確定ハッシュはカタログと一致し、T01資産は不変だった。
- 問題29（3D境界内部判定を修正・実ブラウザ検証済み）: 校庭南塀へ近づくと塀より手前で移動が巻き戻され、接地再同期後に`境界外移動の巻き戻し後の足元位置がBND_Stageの外側です。`で停止した。体育館舞台脇階段でも幅方向の一部だけ同じ巻き戻しが発生した。原因は、`BND_Stage`の閉Mesh内外判定が固定方向の単一Rayを使い、上面と南面の共有稜線をほぼ同距離で二重交差として数える帯を内部から除外していたことである。表面点は距離`1e-6`以内を包含し、それ以外は全三角形の符号付き立体角総和の絶対値が`2π`を超えるかで判定する方式へ置き換えた。修正前T02では南塀と舞台ランプ代表点が17件中1件FAILとなることを確認し、修正後は17/17、T03は19/19、T04は39/39が実ブラウザでPASSし、console warning／errorは0件だった。`npm run build`、`build:t02`、`build:t03`、`build:t04`、`git diff --check`、strict UTF-8・BOMなしも成功した。
- 問題30（学校出入口の段差を連続ランプ化・実ブラウザ検証済み）: 主玄関の0.15m×2段はプレイヤー許容段差と同値で誤差に弱く、渡り廊下接続部はTatakiとThresholdの衝突面が重なり、体育館校庭側は地面Z＝-0.30mから床Z＝0.00mまで0.30mの垂直差にランプがなかった。表示用の段と沓摺は維持し、`COL_EntryStep01/02`を2段分の連続斜面、`COL_NorthSouthEntryTataki`を床と同じ高さの平面衝突、旧Thresholdを`COL_GymCourtyardEntryRamp`へ置き換えて校庭から体育館床へ続く1:5斜面とした。`NAV_Walkable_EntryTransitions`を同じ衝突面から再生成し、校庭から体育館西側出入口への新規代表経路を含む11経路がすべて要求終点へ到達した。同一入力から2回ベイクしたGLB・NavMeshの容量とSHA-256は一致した。T02実ブラウザは17/17項目PASS、console warning／error 0件で、`npm run build`、`build:t02`、`build:t03`、`build:t04`も成功した。GLBは737,684 bytes、SHA-256 `396C9B5F88DAE9563B7B6F3C1CA7C9E44F07F6FEB0BDFAD24A737D9AA559595B`、NavMeshは152,724 bytes、SHA-256 `40983EC74208D8902051D56C0418BD6ED5E727D85436503B1DD63903F2C836A0`となった。
- 問題31（3階段・吹き抜け・階段下倉庫を再構築・実ブラウザ検証済み）: 旧階段は中間踊り場上面Z＝1.5m、1階天井下面Z＝2.9mで、Runtimeの有効高0.35に対してプレイヤーCollider高約0.424が収まらず、踊り場手前で頭が天井へ衝突した。階段下も踊り場下面Z＝1.4mのため、Blender換算2.0mの最大視点高で通常歩行できなかった。ユーザー指定により1階天井下面をZ＝3.0m、2階床面をZ＝3.6m、その間を厚さ0.6mの構造帯とし、中間踊り場を下面Z＝2.3m・上面Z＝2.4mへ変更した。北西・北東・南西の天井と2階Volumeへ階段吹き抜けを開け、3階Volume、屋根、屋上ガード、2・3階窓ガイド、屋上プールの計80 Objectを上方へ0.6m追従した。下段は16段・蹴上0.15m・勾配29.745度、上段は8段・蹴上0.15m・勾配26.565度とし、表示段と独立した連続ランプ、踊り場、斜路追従ガードを再構築した。2階以上の閉鎖Colliderは上段2段目以降に維持し、今回の歩行範囲を踊り場までに限定した。倉庫有効高は最大視点高に対して床基準0.30m、表示床上面基準0.26mの余裕があり、入口幅1.2mは直径0.8mのプレイヤーに左右各0.2mの余裕を持つ。11代表NavMesh経路はすべて到達し、踊り場終点誤差は`4.3e-7`以下だった。T02実ブラウザへ3倉庫の最大視点高、踊り場下面、踊り場上の吹き抜けを追加し、18/18項目PASS、console warning／error 0件を確認した。`npm run build`、`build:t01`、`build:t02`、`build:t03`、`build:t04`は成功した。`.blend`は312,973 bytes、SHA-256 `CB5E50F63A39A9BD7CD645D09848B48C52EC72160386CB7627AA0CD249043CB8`、GLBは757,980 bytes、SHA-256 `3B3D53171F03C7B86255A61DF80B56904F4B5146244B273BCBF69B9B264E26E2`、NavMeshは156,808 bytes、SHA-256 `9CCFE1419B150053A1E7AD85F686431A18343CBC9F7780041EE1ACDAA77063AF`となり、GLB・NavMeshは同一入力からの2回生成で一致した。
- 問題32（常時開放扉とトイレ開口を表示へ反映・実ブラウザ検証済み）: 男女トイレは壁とLintelが通行開口を持つ一方、`VIS_DoorLeaf_Toilet_M/F`だけが残って扉のないデザインと矛盾していた。北側校舎北出入口と体育館西側出入口は、扉名が`Open`でも引戸が開口内へ残り、ほぼ閉じて見えた。トイレ扉表示2件をMesh Dataごと削除し、北側引戸は開口X＝2.4～5.4mの左右へ、体育館西側引戸は開口Y＝5.0～8.0mの上下へ完全収納した。扉Colliderは元から存在せず、壁・Lintel・NavMeshの開口は変更していない。Blenderは441 Object／416 Mesh、GLB出力は418 Object（`VIS_*`261件）となった。T02実ブラウザは、トイレ扉2件の不在と開放扉4件のGLB world位置を含む19/19項目PASS、console warning／error 0件だった。NavMeshは同じ156,808 bytes、SHA-256 `9CCFE1419B150053A1E7AD85F686431A18343CBC9F7780041EE1ACDAA77063AF`を2回の再ベイクで維持した。`.blend`は312,192 bytes、SHA-256 `A31C20BD9EC5CB5095A5CF3A2D4A2137107CB165D61ABDA8D41139F81E3C0ECE`、GLBは754,904 bytes、SHA-256 `E414D302387053DB953E9422B25B85F185F2173C2885CCFEE54E7990DCDB5806`となり、`build:t02`と`build:t04`は成功した。
- 問題33（未洗脳時の照準と操作説明を修正・実ブラウザ検証済み）: V2学校ランタイムにはプレイヤーの洗脳・銃所持状態がまだない一方、開始時に照準を無条件表示していた。未洗脳状態では照準を表示しない既存仕様に合わせ、V2入口から照準表示処理を削除した。操作説明は`操作説明`、`WASD: 移動`、`Shift: ダッシュ`の3行へ戻し、マウスとEscの追加説明を削除した。通常入口を実ブラウザで開始し、表示中の照準0件、操作文の完全一致、学校読込、NPC・ビット更新、console warning／error 0件を確認した。`npm run build`も成功した。
- 問題34（最終自動・実ブラウザ検証済み）: 2026-07-19 12:28 JSTに`npm run audit:v2:dependencies`、`npm run build`、`build:t01`、`build:t02`、`build:t03`、`build:t04`を再実行し、すべて成功した。実ブラウザはT02 19/19、T03 19/19、T04 39/39項目PASS、各console warning／error 0件だった。通常ゲームは学校読込後にNPC 10体、ビット8体、索敵・警報・通常ビームを10秒継続更新し、未洗脳時の表示中照準0件、操作説明完全一致、console warning／error 0件を確認した。ブラウザ検証環境は明示的な1920×1080指定を実効1280×720へ制限したため、1080pの最終描画・性能はユーザー手動再テストへ残す。現行B02は441 Object／416 Mesh、GLB 418 Object、NavMesh 1,895 Triangleで、Blenderは現ブランチ側ファイルを開いた`is_dirty=false`、B02 `.blend1`なしだった。GLB・NavMeshの実SHA-256はカタログと一致し、T01資産は既知SHA-256を維持した。`develop...HEAD`の現存変更テキスト61件はstrict UTF-8・BOMなし、`git diff --check`とTypeScript各ビルドによる構文・括弧検査もPASSした。
- 問題35（階段踊り場の最大視点を境界内へ修正・実ブラウザ検証済み）: 最終独立監査で、中間踊り場上面Z＝2.4mへ床余裕0.05mと最大視点高2.0mを加えたZ＝4.45mが、`BND_Stage`上端Z＝3.5mを越えることを検出した。現在のプレイヤー境界判定は足元だけなので即時例外は発生しないが、プレイヤーが存在してよい3D空間を囲う資産契約に反する。水平範囲と下端を変えず上端だけをZ＝4.6mへ拡張し、3か所の踊り場で足元・最大視点がともに境界内となる回帰をT02へ追加した。T02は19/19、T03は19/19、T04は39/39項目PASS、各console warning／error 0件で、全ビルドと通常ゲーム10秒継続も成功した。`.blend`は312,287 bytes、SHA-256 `AAA4BFA111387D7DE6C52E0D25A1D9DC3A93A4F998044D642F2DCDA95561405A`、GLBは754,916 bytes、SHA-256 `ED0BAA2D2709669F58182F0BBDEDB6DDE3D66ADA2A7E3928E206B2C8CCEBC2D6`、NavMeshは不変の156,808 bytes、SHA-256 `9CCFE1419B150053A1E7AD85F686431A18343CBC9F7780041EE1ACDAA77063AF`となった。Blenderは現ブランチ側ファイル、`is_dirty=false`、`.blend1`なしで、独立再監査もP0～P2指摘なしだった。
- 後続責務は、B03が階段同色面の判別性と隙間のない表示用手すり、T04がNPC・ビット共通の階段・複数階・高さ付き経路、T05がgun所持NPC・ビットの完全戦闘状態機械と高さ付き特殊ビーム、T06がBGM・SE・VOICEと音量UIのV2接続を担当する。
- 体育館舞台階段の形状ディテールと同色面の境界表現はB03、NPC・ビット共通の複数階・高さ付きNavMesh接続はT04後続、ビット固有の飛行・戦闘統合はT05の範囲とし、今回の1階連続NavMeshへ仮リンクやセル互換を追加しない。

### 廃止済み初期JSON・セル接続の履歴

- 2026-07-18 23:29 JSTに開始確認を実施した。
- メインworktreeはクリーンな`develop`で、`develop`、`origin/develop`、HEADはいずれもPR #40取込後の`8b25790efbd9afbd151fa3081a8f0faf095b5df5`だった。
- 旧B01 worktreeは`D:/Users/draft/create/fps_survival20251226-b01`、旧B02 worktreeは`D:/Users/draft/create/fps_survival20251226-b02`に分離されている。
- 停止済みの似た名前のブランチ`codex/v2-b02-t03-testplay`は`8b25790`に存在するが、指定名とは異なるため変更していない。
- 指定名`codex/v2-b02-t03-gameplay-test`が存在しないことを確認し、最新`develop`から作成してメインworktreeで切り替えた。
- 指定された15文書をUTF-8で末尾まで読み、T01資産規約、T02共通ローダー、T03プレイヤー高さ制御、B02最終資産、B03/T04/T05/T06の未実装境界を確認した。
- 通常ゲームの`src/main.ts`は`STAGE_CATALOG`から定義を読み、`buildStageContext`、T03高さ制御、ステージ切替時のコライダー交換と高さ再同期を実運用経路として使用する。T03検証画面は同じ高さ制御を使うが通常ゲーム全体ではないため、今回の手動テスト入口には通常タイトルを使用する。
- 最小接続は`public/stage/b02_t03_school_test.json`と、`src/world/stageSelection.ts`の明示的なテスト専用項目1件だけとする。既存8 JSON、正式学校ID、専用システムID、T03検証画面は変更しない。
- B02通常ゲーム読込時に実績のある平面定義を引き継ぎ、83列×71行、1mセル、`originMeters: { x: -19, y: -12 }`、主玄関内側の`(x=21, z=12)`スポーンを使用する。NPC経路探索がグリッド外へ出ないよう、外周1セルを`noRender`の通行不可記号で閉じる。
- T06との境界として、本JSONは名称・説明・ファイル名・カタログIDのすべてをB02/T03テスト専用と明記する。高さ付きナビゲーション、正式学校ゾーン、NPC、ビット、光線、公開処刑の学校統合は追加しない。
- `npm run build`、`npm run build:t01`、`npm run build:t02`、`npm run build:t03`は最新develop基準ですべて成功した。既存のBabylonバンドル容量警告以外にエラーはない。
- `public/stage/b02_t03_school_test.json`を追加し、B02 GLB、83列×71行の平面ナビゲーション、主玄関内側スポーン、校庭上のテスト用集合領域、経路探索用の非表示外周を接続した。
- `src/world/stageSelection.ts`へ`b02_t03_school_test`を追加し、タイトル表示を`[TEST] B02学校ブロックアウト＋T03プレイヤー`とした。コメントでも正式登録はT06の責務であることを明記した。
- 初期接続はテスト専用JSON1件とカタログ項目1件だけとし、実ブラウザで見つかったNPC経路探索の原点変換を1箇所修正した。Blender資産、既存8 JSON、T01～T03検証、旧worktree、旧ブランチには変更を加えていない。
- 問題1（修正・再検証済み）: 学校を選択してタイトル画面を左クリックし、実プレイを開始すると、`npcNavigation.ts`の`hasWallOnLine`が未定義行の列23を参照して停止した。期待結果はNPCを含めて学校ステージが継続動作すること、実際の結果は開始直後にNPC経路探索の`TypeError`が発生することだった。原因は同関数だけが`layout.worldOrigin`と行・列方向を使わず、グリッドがワールド原点中心であると仮定して線分をセルへ変換していたことにある。学校JSONの原点`(-19, -12)`ではDDA開始セルが範囲外になった。`worldOrigin`、`columnDirection`、`rowDirection`に基づく連続グリッド座標へ統一し、範囲外ガードやフォールバックは追加していない。同じ操作で再開し、NPC11体を含む実プレイを20秒以上継続しても同例外が再発しないことを確認した。
- 実ブラウザで学校選択、読込完了、タイトル開始、HUD表示、B02 GLB描画を確認した。学校から既存の都心部へ切り替え、再度学校へ戻してどちらも開始待ちへ到達し、切替時の読込警告・エラーがないことも確認した。
- 自動ブラウザ操作では音声再生のユーザー操作判定と埋込み文書のポインターロック制約によるブラウザ警告が出るが、ゲーム処理は開始して継続した。通常の手動クリックではブラウザ自身のユーザー操作として開始するため、学校実装の読込エラーとは区別する。
- 修正後の`npm run build`、`npm run build:t01`、`npm run build:t02`、`npm run build:t03`はすべて成功した。既存のバンドル容量警告以外にビルドエラーはない。
- 変更4ファイルはUTF-8（BOMなし）で、学校JSONは71行×83列、スポーンセルは通行可能記号`.`だった。`git diff --check`は改行コード変換予告以外のエラーなし、B02 Blender/GLBとT01検証資産は差分なし、B02 GLBのSHA-256は`98DE5E201CBA3D93DF01ACA27DD79B050C116A761AAE84F0DE6EEAC04F289EAE`のままである。
- Web開発サーバーを`http://127.0.0.1:5175/`で起動し、学校を選択した開始待ち画面を手動テスト用に残す。
