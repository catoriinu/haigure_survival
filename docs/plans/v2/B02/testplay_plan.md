# HAIGURE SURVIVAL v2 B02＋T03 実ゲーム読込・テストプレイ 計画

更新日: 2026-07-19

## プロンプト

# HAIGURE SURVIVAL v2 B02＋T03 実ゲーム読込・テストプレイ引き継ぎ

### 手動テスト後の追加指示

- 階段を上がると速度が急落し、操作が効かないまま非常に遅く上がり続ける。表示は段のまま、移動とカメラは連続スロープとして滑らかに昇降させる。
- ビットのビームを壁で遮り、将来の窓だけは透過させる。
- ビットの通常索敵は既存の扇形視野と距離に加え、壁・障害物越しには標的を取得しない。窓越しは視認可能とする。
- アラート経由の標的共有は壁越しでも維持する例外とする。ただしビーム自体は壁で止める。
- アラート標的や窓越し標的へ直進すると障害物へ衝突する場合、最短経路で壁を回り込む。洗脳されたNPCも学校1階では同様に対応し、階段・複数階はT04へ残す。
- 階段など同色面の境界が分かりにくい点は、B03で修正予定なら今回は変更しなくてよい。
- 修正は機能単位でコミットを分け、pushは後でまとめて行う。

### 2026-07-19 3D空間基盤への方針変更

- V2ではJSON文字マップとセルナビゲーションを完全に廃止し、GLBの3D形状、3Dマーカー、3Dボリューム、NavMeshをもとにすべての処理を行う。
- スポーン、NPC・ビット移動、視線、ビーム、集合、禁止領域、境界を3D空間問い合わせへ書き換える。TypeScriptにはステージIDと資産URLなど非空間情報だけを保持する。
- 既存8ステージは一旦V2の対象外とし、互換処理を作らない。将来それぞれをGLB・NavMesh化して学校と同じ契約へ再導入する。
- 当面は学校だけを通常ゲームの対象とし、B02/T03テスト用に追加したJSON、全面床セル、セルBFSとその修正コミットを取り下げる。
- これまでT04～T06へ残していた恒久基盤の境界を見直し、本ブランチで学校の3D空間基盤と必要なNPC・ビット・通常ビーム・視線まで移行する。
- 階段の同色面表現は引き続きB03対象とし、今回の基盤移行では表示材質を変更しない。

## 目的

T03までのゲーム実装と、B02で完成・developへマージ済みの学校ブロックアウトを実ゲームへ読み込み、プレイヤーが校舎1階、校庭、渡り廊下、体育館を歩いて縮尺・衝突・階段移動をテストプレイできる状態を作る。

ユーザーが手動テストで見つけた問題は、このタスク専用ブランチ内だけで再現、修正、再検証する。2026-07-19の追加指示により、B03の表示仕上げは先取りしない一方、T04の3D空間基盤、NPC・ビットNavMesh移動、通常ビーム・視線遮蔽は本タスクへ統合する。

## Git・作業場所

- このタスクは独立worktreeを新設せず、メインworktree `D:\Users\draft\create\fps_survival20251226`を使用する。
- メインworktreeの`develop`はPR #40取込後の`8b25790efbd9afbd151fa3081a8f0faf095b5df5`までpull済みで、開始時点ではクリーンである。
- 最初に`date`、`git status`、`git branch`、`git log`、`git worktree list`を実行し、日時、既存変更、現在位置、最新develop、旧B02/T03 worktreeとの分離を確認する。
- メインworktreeがクリーンな`develop`であることを確認後、最新developから`codex/v2-b02-t03-gameplay-test`を作成して切り替える。
- 既に同名ブランチが存在する場合は勝手に削除・上書きせず、状態を確認してユーザーへ報告する。
- 旧B02ブランチ、T03旧ブランチ、Codexが先に作った停止済みテストworktree、他タスクのworktree・作業ファイルには触れない。
- T02、T03、B02のコミットをcherry-pickしない。最新developを正本とする。
- ユーザーの明示指示なしにコミット、push、Pull Request作成を行わない。

## 最初にUTF-8で最後まで読むファイル

- `docs/plan.md`
- `docs/branch_strategy.md`
- `docs/spec_stage_assets_v2.md`
- `docs/spec_stage_json.md`
- `docs/spec_technical.md`
- `docs/plans/v2/T01/plan.md`
- `docs/plans/v2/T02/plan.md`
- `docs/plans/v2/T03/plan.md`
- `docs/plans/v2/B01/approved_layout.md`
- `docs/plans/v2/B01/b02_handoff.md`
- `docs/plans/v2/B02/plan.md`
- `docs/plans/v2/B03/plan.md`
- `docs/plans/v2/T04/plan.md`
- `docs/plans/v2/T05/plan.md`
- `docs/plans/v2/T06/plan.md`

リポジトリ内の`AGENTS.md`は探さず、自動適用されたグローバル指示に従う。

## 計画管理

- 本テスト専用の計画を`docs/plans/v2/B02/testplay_plan.md`へ作成する。
- ユーザーの今回の指示をプロンプト欄へ反映する。
- 1ステップごとに計画を更新し、問題、再現手順、原因、修正、検証結果を記録する。
- B02の完成結果を書き換える必要がある場合は、B02計画にも最新結果だけを追記する。

## 確定状態

- T03までdevelopへマージ済みで、プレイヤーの高さ、重力、接地、段差、斜面、階段ランプ移動が実装済みである。
- B02はPR #40でdevelopへマージ済みである。
- B02成果物:
  - `assets/blender/v2/B02/b02_school_blockout.blend`
  - `public/stage-assets/v2/B02/b02_school_blockout.glb`
  - `docs/plans/v2/B02/images/`
- B02最終シーンはメッシュ407件（`VIS_`263件、`COL_`144件）である。
- 学校1階、校庭、渡り廊下、体育館が今回のテスト対象である。
- 2階・3階内部、B03ローポリ仕上げ、学校の恒久タイトル登録は未実装である。

## 実装・検証範囲

1. 最新developで次を基準検査する。
   - `npm run build`
   - `npm run build:t01`
   - `npm run build:t02`
   - `npm run build:t03`
2. B02 GLBを、T03の実運用プレイヤー移動で読み込める学校専用TypeScriptカタログへ接続する。
3. JSONへ空間情報を持たせず、GLB内の3Dマーカー・ボリュームと事前ベイクNavMeshを`StageSpatialContext`へ接続する。
4. 学校GLB、3D NavMesh、スポーン、ステージ境界、衝突、視線・ビーム遮蔽を接続する。
5. Web版を実際に起動し、ユーザーが手動でテストプレイできるURL、操作方法、確認観点を提示する。
6. 自動確認だけで完了にせず、ユーザーの手動プレイ結果と修正指示を待つ。
7. 指摘された問題を専用ブランチ内で修正し、同じ場所・操作で再検証する。

## 最初のテスト項目

- 主玄関付近へ正しい縮尺と床高でスポーンできる
- 主玄関、北側出入口、副玄関の段差と扉を通行できる
- 普通教室3室、特別教室、廊下、トイレ前、階段前を歩ける
- 教室の前後引き戸開口を通行でき、壁をすり抜けない
- 北西・北東・南西階段を踊り場まで上り下りできる
- 階段ランプ、踊り場、閉鎖コライダー、転落防止壁が機能する
- 階段下倉庫予定の開放部へ入れる
- 校庭、開放渡り廊下、体育館へ連続して移動できる
- 体育館舞台脇、舞台階段、舞台、体育倉庫出入口を確認できる
- 外周塀、正門、東側通用口をすり抜けない
- 壁、床、天井へカメラやプレイヤーが不自然に食い込まない
- 床抜け、浮き、急落、見えない段差、進行不能、Z-fighting、欠落壁がない
- 1080p既定設定で重大な描画低下や読込エラーがない
- 学校の破棄・再読込でGLB、NavMesh、イベント購読などの資源が残留しない

## 今回は不具合扱いしない既知の未実装

- B03のローポリ仕上げ、机、椅子、ロッカー、小物
- B03の壁の実窓開口、透明ガラス、窓枠
- T05で完成させる固定光線、トラップ光線、動的ビーム、特殊ビット編隊と全ゲームシステムの3D統合
- T06のB03完成資産との最終統合、全機能・性能・資源ライフサイクル検証
- 2階・3階内部
- 窓ガイドが現時点で平面パネルであること

ただし、B02/T03だけで成立すべきプレイヤー移動、GLB表示、縮尺、1階衝突、階段、床高の問題は今回の修正対象とする。

## Blender資産を修正する場合

- Blenderを触る前に`bpy.data.filepath`、`bpy.data.is_dirty`、オブジェクト一覧を読み取り、ユーザーへ提示する。
- 現在開いているファイルが旧B02 worktree側、または未保存変更ありの場合、上書き、保存、破棄、初期化をせず停止して確認する。
- Blender変更が必要になった場合は、最初にメインworktreeの現テストブランチ側`assets/blender/v2/B02/b02_school_blockout.blend`へ「名前を付けて保存」し、現在パスがこの作業ブランチ側へ変わったことを確認してから編集する。
- T01、旧B02ブランチ、旧B02 worktreeの`.blend`・GLBを上書きしない。
- 修正後は`.blend`、GLB、確認画像、オブジェクト監査、T01非変更を再検証する。
- 同一B02バイナリを他セッションと並行編集しない。

## 問題修正方針

- 問題ごとに、場所、再現操作、期待結果、実際の結果を記録する。
- 原因がBlender形状、GLB書き出し、3D資産分類、NavMesh、T03プレイヤー移動、`StageSpatialContext`のどこにあるか切り分ける。
- セル互換やJSONフォールバックを追加せず、学校の3D空間基盤として必要な最小責務だけを実装する。
- 修正後は該当箇所、学校全体、T01～T03のうち3D基盤に残る検証を回帰確認する。既存8ステージはV2回帰対象に含めない。
- UTF-8（BOMなし）、`git diff --check`、括弧対応、ビルド、Git差分を最終確認する。

## 最初の進め方

まずGitと指定文書を確認し、過去の学校JSON・セル接続を安全に取り下げる。GLB意味オブジェクト契約、Recast事前ベイク、`StageSpatialContext`と`NavigationWorld`を実装し、学校だけを通常ゲームへ接続する。その後、ユーザーがプレイできるURLと操作方法を提示し、手動テスト結果を待つ。

## ステップ

- [x] 日時、Git状態、ブランチ、履歴、worktree分離、最新`develop`を確認する
- [x] 最新`develop`から`codex/v2-b02-t03-gameplay-test`を作成する
- [x] 指定された15文書をUTF-8で最後まで読む
- [x] T02/T03のローダー、プレイヤー移動、検証入口とB02 GLB構造を調査し、最小接続方法とT06境界を確定する
- [x] 最新develop基準の`npm run build`、`build:t01`、`build:t02`、`build:t03`を実行する
- [x] 学校テスト専用JSON、通常ゲームのテスト限定入口、スポーン、平面ナビゲーション、境界を実装する
- [x] 学校読込、実プレイ開始、既存ステージ切替、資源破棄を自動・実ブラウザで検証し、詳細な歩行・階段確認を手動テストへ引き継ぐ
- [x] UTF-8（BOMなし）、JSON、TypeScript構文・括弧、`git diff --check`、Git差分を確認する
- [x] Web版を起動し、URL、操作方法、手動テスト項目をユーザーへ提示する
- [ ] ユーザーの手動テスト結果を待ち、問題ごとに再現・原因・修正・再検証を記録する
- [x] 手動テスト指摘を調査し、階段はT03接地補正、ビームと索敵はGLB壁未接続、AI迂回は学校ナビゲーション全面床が原因と切り分ける
- [x] 学校テスト入口を単独ビルド確認し、機能単位でコミットする
- [x] 階段斜面上の停止・逆方向入力・速度低下を修正して検証する
- [x] JSON・セル依存の限界を調査し、V2では学校の3D空間基盤へ全面移行する方針を確定する
- [x] 学校テストJSON、セルナビ、セルBFSの暫定コミットを安全なrevertコミットで取り下げる
- [x] GLB意味オブジェクト、3Dマーカー・ボリューム、NavMesh派生物の資産契約を確定する
- [x] Recast NavMeshの生成・バイナリ往復・3D経路を小型検証する
- [x] 学校の階段・舞台斜面コライダーをNavMesh入力に使える閉じた外向きMeshへ正規化する
- [x] 北側出入口の0.30m箱段差コライダーを外側地面から1階床へ続く連続スロープへ置き換える
- [x] 学校GLBへ`META_*`、`MRK_*`、`VOL_*`、`BND_*`、`NAV_*`を追加し、事前ベイクNavMeshの代表10経路を検証する
- [x] 学校用`StageSpatialContext`、`NavigationWorld`、TypeScriptカタログを実装する
- [x] セル非依存の共通3D経路追従とVolume内NavMesh生成位置を実装する
- [ ] 学校専用V2プレイヤーランタイムを新設し、通常ゲーム入口を切り替える
- [ ] プレイヤー、NPC、ビット、通常ビーム、ビット視線を3D空間基盤へ移行する
- [ ] V2実行経路からステージJSON、`GridLayout`、`FloorCell`、セル座標処理を削除する
- [ ] 全ビルド、学校手動テスト、Blender・GLB・NavMesh監査を最終確認する

## 結果

- 2026-07-19 00:39 JSTに作業再開時のGit状態を再確認した。現在ブランチは`codex/v2-b02-t03-gameplay-test`、HEADは最新developの`8b25790`から開始した学校テスト差分を含み、旧B01・B02 worktreeは分離されたままである。
- 手動テスト指摘の実装計画を確定し、全体のコミット判定は複数責務を含むため`SPLIT`とした。
- 学校テスト専用JSONとタイトル入口だけで`npm run build`が成功した後、`feat: B02学校テストプレイ入口を追加`としてコミット`1463f54`へ分離した。pushは行っていない。
- 問題2（修正・自動再検証済み）: B02階段ランプを上がると入力解除後も非常に遅く上昇し続け、方向転換が効かなくなった。期待結果は表示段と独立した連続ランプ上を通常の水平入力速度で滑らかに移動し、入力解除で停止することだった。原因は接地用の純垂直`moveWithCollisions`が斜面法線に沿うX/Z変位を生み、その実変位が水平慣性へ戻されていたことである。接地スナップ前のX/Zを保持して垂直補正後に復元し、接地補正を水平慣性から分離した。
- T03検証へ斜面中腹の入力解除と即時方向転換を追加した。実ブラウザは全19項目PASSで、水平ドリフト`0.000000`、逆方向進行`0.199500 / 0.2`、空中フレーム0だった。`npm run build:t03`と`npm run build`も成功し、`fix: 斜面上のプレイヤー移動を安定化`としてコミット`2c83349`へ分離した。
- 問題3（学校1階ナビゲーションを修正・自動検証済み）: 初期テストJSONは外周以外の全セルが床だったため、ビットと洗脳NPCの経路探索では校舎壁、塀、閉鎖地点、将来窓位置を通行可能と判定していた。期待結果は主玄関・各出入口・教室扉・渡り廊下・体育館入口だけを接続し、平面で表現できない階段と舞台昇降部をAI経路から除外することだった。B02 GLBの1階コライダー中心線と実開口を83列×71行へ反映し、床4,460セルが主玄関から4近傍ですべて到達可能な単一成分であること、スポーンと集合地点が床セルであることを確認した。`npm run build`と`git diff --check`も成功した。
- 問題4（ビット迂回追跡を修正・自動検証済み）: ビットの追跡は毎フレーム4近傍探索をやり直し、到達不能時には標的への直進へ戻るため、壁へ押し付けられた。また床・壁余裕・境界判定がワールド原点中央を前提としており、学校のカスタム原点では別セルを参照した。目標セル単位で最短経路をキャッシュし、到達不能時は停止して0.5秒間隔で再探索するよう変更した。アラート集合も同じ経路を使用し、カーペット爆撃は壁で編隊を解除して同じ標的への迂回追跡へ移る。カスタム原点と回転方向を持つ5×5検証グリッドで6セルの迂回経路と到達不能`null`を確認し、`npm run build`と`git diff --check`も成功した。
- 問題5（洗脳NPC迂回追跡を修正・自動検証済み）: 洗脳NPCには経路キャッシュがあったが、到達不能時に古い経路と目的地を残し、通常探索へ戻った後も以前の追跡方向へ動く可能性があった。共通4近傍・BFS・経路復元へ統一し、標的セル変更時または到着時だけ最短経路を更新するよう整理した。到達不能時は標的IDを保持しつつ現在位置で停止して次フレーム以降も再探索し、標的喪失時と離脱動作開始時は旧追跡経路を破棄する。`npm run build`、対象差分の`git diff --check`、UTF-8 BOMなしを確認した。
- 2026-07-19、問題3～5のセル実装は個別不具合を直しても3D階段、重複階、実メッシュ視線、将来の窓へ拡張できないため、完成成果として採用しないことをユーザーと確定した。履歴をresetせず、階段修正`2c83349`を保持したままセル関連コミットだけをrevertする。
- 3D空間基盤は、GLBを空間正本、Recast NavMeshバイナリをGLBから生成する派生物、TypeScriptカタログを非空間情報だけの索引とする。通常扉・階段は連続NavMesh、梯子・昇降機などだけを`LNK_*`で表す。
- Blenderは最初の監査で旧B02 worktree側が開かれdirtyだったため無変更で停止した。ユーザーが保存せず閉じて現作業側を開き直し、`bpy.data.filepath`が現worktree、`is_dirty=false`、全430オブジェクト、Mesh 407件、`VIS_`263件、`COL_`144件であることを再確認した。
- セル方式の5コミットを逆順に`git revert --no-commit`し、学校テストJSON、タイトル入口、カスタム原点補正、学校1階文字セル、ビット・NPCセルBFSだけを取り下げた。階段修正`2c83349`と未コミットの3Dビーム試作は保持した。`npm run build`と`npm run build:t03`の成功後、`revert: 学校テスト用セルナビゲーションを取り下げ`としてコミット`c94302e`へ分離した。
- NavMesh候補の`recast-detour` 1.6.4はViteのES module実行で初期化不能だったため採用しなかった。ESM対応の`recast-navigation` 0.43.1へ置き換え、セル・JSONを使わない小型3Dコースで初期化、扉迂回、連続ランプ、バイナリ往復、到達不能の5項目を実ブラウザ検証した。5/5項目PASS、warning・error 0件、生成4.40ms、NavMesh 11,116 byteだった。
- 問題6（3D NavMesh入力の前提修正・自動検証済み）: B02の校舎下段階段ランプ3件と体育館舞台ランプ2件には重複頂点由来の面積0面があり、これら5件を含む階段ランプ・上段ランプ・転落防止壁12件の閉じたMeshは面法線が内向きだった。期待結果は、表示段を変更せず、`COL_StairRamp_*`等をプレイヤー衝突と連続NavMesh生成の両方へ安定して使用できることである。5件を6頂点・5面の閉じた楔形へ整理し、12件すべてを正の符号付き体積へ正規化した。修正後は12件すべてで面積0面0、非多様体辺0、正の体積を確認した。
- Blenderは現作業ブランチ側`assets/blender/v2/B02/b02_school_blockout.blend`、`is_dirty=false`、430 Object、407 Mesh（`VIS_`263件、`COL_`144件）であることを再確認した。GLBも407 Node／407 Mesh、Material 20件、Camera・Light・Animation 0件、規約外名0件である。B02 `.blend`のSHA-256は`5ECCD2E04BDC1071D6FB2BCBA9DFD5784F26DB29C82009D505F1094C2F9215B4`、GLBは`AFC4D0C9E8FF19FFAFE55DD4B500F25D145AB6CBF2FECB720BD38551BB7B2C6F`となった。
- `npm run build`、`npm run build:t01`、`npm run build:t03`は成功した。T01の`.blend`は`A26672A7400A143F32FAB9F5DC53387CFF85C1FB22A9766F464E9BAEFAC5B16B`、GLBは`CF6CF17CF7447152D0B25219045C33EFB2E1A1821C201ACD6E51E39F468EA69B`のままで、対象パスのGit差分は0件だった。Blender連携のビューポート画像取得は既存カメラへ合わせても黒画像を返したため、表示メッシュ監査と後段の実ゲーム描画で視覚確認を補完する。
- 問題7（北側出入口の段差移動を修正・自動検証済み）: 北側出入口の`COL_NorthEntryStep`は、外側地面上面Z＝-0.30mから1階床Z＝0.00mまでを一度に上がる高さ0.30mの箱だった。T03プレイヤーの許容段差0.15mを超えるため、期待する外側から校舎内への連続移動が成立しなかった。表示用`VIS_NorthEntryStep`の8頂点・6面の箱は維持し、衝突用だけを同じX＝2.4～5.4m、Y＝45.5～47.0m、Z＝-0.3～0.0m境界内の1:5連続楔へ変更した。修正後のColliderは6頂点・5面、面積0面0、非多様体辺0、符号付き体積0.675m³、斜面法線Z＝0.980581である。
- 北側出入口修正後も430 Object、407 Mesh（`VIS_`263件、`COL_`144件）を維持し、GLBは407 Node／407 Mesh、Material 20件、Camera・Light・Animation 0件、規約外名0件だった。B02 `.blend`はSHA-256 `04CF5C0C8759737F21843BC231B0F460695D198EA1E662EA790933421BD6648E`、GLBは`1DFF477335398A62893DE048BF3F8073ECF9B19715C17BB9EE4296E67D5CCF74`である。`npm run build`と`npm run build:t03`は成功し、T01資産のGit差分は0件だった。実際の出入りは学校を通常ゲームへ再接続した後の手動確認へ残す。
- 問題8（学校3D空間正本・事前ベイクNavMeshを作成）: V2学校ではJSON文字マップとセルを空間正本にせず、同一GLB内の`META_Stage`、`MRK_*`、`VOL_*`、`BND_Stage`、`NAV_*`と、そのGLBから生成した事前ベイクRecast NavMeshを使用する。Blender監査は443 Object／418 Mesh、GLB出力対象420 Objectで、内訳は`VIS_*`263件、`COL_*`144件、`NAV_*`8件、`META_Stage`1件、`MRK_*`1件、`VOL_*`2件、`BND_Stage`1件である。制作ガイド23件は出力対象外を維持した。
- 問題9（体育館舞台へのNavMesh経路切断を修正・自動検証済み）: プレイヤースポーンから体育館舞台中央へ経路計算すると、Recastが部分経路を返し、要求終点との誤差が`0.762910`となった。東西の`VIS_GymStageStairHeadWall_*`と`COL_GymStageStairHeadWall_*`は下端Z＝2.4mだったが、舞台上面Z＝1.0mからの実開口はBlender 1.4m、Runtime 0.35mにしかならず、プレイヤー高さ約0.424mとNavMesh高さ0.425mを下回っていたことが原因である。表示壁・衝突壁双方の下端をZ＝3.4mへ上げ、舞台上面からBlender 2.4mの一致した開口を確保した。NavMeshだけをつなぐ仮connector面は不採用として削除した。
- 舞台開口修正後、主玄関スポーンから校庭、北側出入口外側、北西・北東・南西階段踊り場、体育館中央、体育倉庫、舞台中央などを含む代表10経路はすべて終点誤差`4.3e-7`以下となった。最終`.blend`は308,457 bytes、SHA-256 `42617927F6AD7AB4F4C8776CA3E8EC3999BA12E9DF90D9551342D0A97EEF5996`、GLBは739,444 bytes、SHA-256 `55A6EAE9AAA169E1CB6389C2D3924BED05D1F9530105DCF92F9F9808FCFEE381`、NavMeshは151,552 bytes、SHA-256 `C0699A5C371AB30A5B85241DEEE38A2603258DE8961A095AA5EF55031A4040E9`である。同一入力から2回ベイクしてNavMeshの容量・SHA-256が一致した。T01 `.blend`は`A26672A7400A143F32FAB9F5DC53387CFF85C1FB22A9766F464E9BAEFAC5B16B`、GLBは`CF6CF17CF7447152D0B25219045C33EFB2E1A1821C201ACD6E51E39F468EA69B`を維持した。
- `npm run bake:v2:school-navmesh`は部分経路を`success`として受理せず、代表10経路すべての要求終点との誤差`1e-5`以下を必須とする。`npm run build`、`npm run build:t01`、`npm run build:t02`、`npm run build:t03`、`npm run build:t04`はすべて成功した。
- 問題10（学校3D空間コンテキストを実装・自動検証済み）: 学校カタログはID、表示名、GLB／NavMesh URL、schema、NavMesh profile、確定SHA-256だけを保持し、空間座標を持たない。`StageSpatialContext`はGLB作者Node 420件を`VIS_*`263件、通常`COL_*`144件、`NAV_*`8件、意味Object 5件へ厳格分類し、プレイヤースポーン、NPC・ビット出現Volume、3D境界、Actor衝突、通常ビーム・視線遮蔽、事前ベイクNavMeshを同じ所有単位へまとめる。ActorOnly窓は移動だけを遮り、光線と視線を通す分類テストも追加した。
- T04実ブラウザ検証は実学校GLB／NavMeshのハッシュ照合、主玄関スポーン、境界包含、舞台までの完全経路、実壁遮蔽、資源破棄、再読込を含む12/12項目が初回・再実行ともPASSし、console warning／errorは0件だった。学校読込は約34～41msで、破棄後はMesh・Material・TransformNode件数が読込前と一致した。
- 問題11（NPCからビットへのアラート標的受け渡しを修正・自動検証済み）: 既存の`AlertRequest`は`targetId`と`blockerId`の意味が呼出箇所ごとに反転し、`applyAlertRequests`が両方を`blockerId`として`ExternalAlert`へ渡していた。このため、警報を出したNPCと追跡対象が同じIDへ潰れていた。型を`leaderId`と`targetId`へ変更し、生存NPCがプレイヤーに塞がれた場合はNPCを発信者・プレイヤーを標的、洗脳NPCが未洗脳者へ接触した場合は洗脳NPCを発信者・接触相手を標的として統一した。`npm run build`と対象差分の`git diff --check`は成功した。
- 問題12（共通3D経路追従を実装・自動検証済み）: NPC・ビットのセルBFSを置き換える共通エージェントは、NavMeshへ投影した始点・標的の完全経路をキャッシュし、標的移動閾値、定期期限、経路消費、stuck時だけ再探索する。到達不能または移動拘束失敗時は現在位置で停止し、直進へフォールバックしない。斜面を含むXYZ経路を速度×deltaで複数点跨いで進み、投影終点への到着を判定する。T04実ブラウザは20/20項目PASS、console warning／error 0件、`npm run build:t04`成功だった。
- 問題13（Volume内3Dスポーン抽選を実装・自動検証済み）: NPC・ビット出現位置をセル記号から選ぶ代わりに、GLBの`VOL_*`内で候補を生成し、NavMeshへ投影した後もVolume内にある点だけを採用する。複数個体は3D最小距離と重複排除を満たし、候補枯渇時はVolume IDと棄却理由別件数を含む例外で停止する。T04へ単一点、複数点、重複候補、NavMesh投影失敗、不正乱数の5検証を追加し、実ブラウザは既存20項目を含む25/25項目が初回・再実行ともPASS、console warning／error 0件、`npm run build:t04`成功だった。
- 体育館舞台階段の形状ディテールと同色面の境界表現はB03、NPC・ビットが複数階を移動する高さ付きNavMesh接続はT04の範囲とし、今回の1階連続NavMeshへ仮リンクやセル互換を追加しない。

- 2026-07-18 23:29 JSTに開始確認を実施した。
- メインworktreeはクリーンな`develop`で、`develop`、`origin/develop`、HEADはいずれもPR #40取込後の`8b25790efbd9afbd151fa3081a8f0faf095b5df5`だった。
- 旧B01 worktreeは`D:/Users/draft/create/fps_survival20251226-b01`、旧B02 worktreeは`D:/Users/draft/create/fps_survival20251226-b02`に分離されている。
- 停止済みの似た名前のブランチ`codex/v2-b02-t03-testplay`は`8b25790`に存在するが、指定名とは異なるため変更していない。
- 指定名`codex/v2-b02-t03-gameplay-test`が存在しないことを確認し、最新`develop`から作成してメインworktreeで切り替えた。
- 指定された15文書をUTF-8で末尾まで読み、T01資産規約、T02共通ローダー、T03プレイヤー高さ制御、B02最終資産、B03/T04/T05/T06の未実装境界を確認した。
- 通常ゲームの`src/main.ts`は`STAGE_CATALOG`から定義を読み、`buildStageContext`、T03高さ制御、ステージ切替時のコライダー交換と高さ再同期を実運用経路として使用する。T03検証画面は同じ高さ制御を使うが通常ゲーム全体ではないため、今回の手動テスト入口には通常タイトルを使用する。
- 最小接続は`public/stage/b02_t03_school_test.json`と、`src/world/stageSelection.ts`の明示的なテスト専用項目1件だけとする。既存8 JSON、正式学校ID、専用システムID、T03検証画面は変更しない。
- B02通常ゲーム読込時に実績のある平面定義を引き継ぎ、83列×71行、1mセル、`originMeters: { x: -19, y: -12 }`、主玄関内側の`(x=21, z=12)`スポーンを使用する。NPC経路探索がグリッド外へ出ないよう、外周1セルを`noRender`の通行不可記号で閉じる。
- T06との境界として、本JSONは名称・説明・ファイル名・カタログIDのすべてをB02/T03テスト専用と明記する。高さ付きナビゲーション、正式学校ゾーン、NPC、ビット、光線、公開処刑の学校統合は追加しない。
- `npm run build`、`npm run build:t01`、`npm run build:t02`、`npm run build:t03`は最新develop基準ですべて成功した。既存のBabylonバンドル容量警告以外にエラーはない。
- `public/stage/b02_t03_school_test.json`を追加し、B02 GLB、83列×71行の平面ナビゲーション、主玄関内側スポーン、校庭上のテスト用集合領域、経路探索用の非表示外周を接続した。
- `src/world/stageSelection.ts`へ`b02_t03_school_test`を追加し、タイトル表示を`[TEST] B02学校ブロックアウト＋T03プレイヤー`とした。コメントでも正式登録はT06の責務であることを明記した。
- 初期接続はテスト専用JSON1件とカタログ項目1件だけとし、実ブラウザで見つかったNPC経路探索の原点変換を1箇所修正した。Blender資産、既存8 JSON、T01～T03検証、旧worktree、旧ブランチには変更を加えていない。
- 問題1（修正・再検証済み）: 学校を選択してタイトル画面を左クリックし、実プレイを開始すると、`npcNavigation.ts`の`hasWallOnLine`が未定義行の列23を参照して停止した。期待結果はNPCを含めて学校ステージが継続動作すること、実際の結果は開始直後にNPC経路探索の`TypeError`が発生することだった。原因は同関数だけが`layout.worldOrigin`と行・列方向を使わず、グリッドがワールド原点中心であると仮定して線分をセルへ変換していたことにある。学校JSONの原点`(-19, -12)`ではDDA開始セルが範囲外になった。`worldOrigin`、`columnDirection`、`rowDirection`に基づく連続グリッド座標へ統一し、範囲外ガードやフォールバックは追加していない。同じ操作で再開し、NPC11体を含む実プレイを20秒以上継続しても同例外が再発しないことを確認した。
- 実ブラウザで学校選択、読込完了、タイトル開始、HUD表示、B02 GLB描画を確認した。学校から既存の都心部へ切り替え、再度学校へ戻してどちらも開始待ちへ到達し、切替時の読込警告・エラーがないことも確認した。
- 自動ブラウザ操作では音声再生のユーザー操作判定と埋込み文書のポインターロック制約によるブラウザ警告が出るが、ゲーム処理は開始して継続した。通常の手動クリックではブラウザ自身のユーザー操作として開始するため、学校実装の読込エラーとは区別する。
- 修正後の`npm run build`、`npm run build:t01`、`npm run build:t02`、`npm run build:t03`はすべて成功した。既存のバンドル容量警告以外にビルドエラーはない。
- 変更4ファイルはUTF-8（BOMなし）で、学校JSONは71行×83列、スポーンセルは通行可能記号`.`だった。`git diff --check`は改行コード変換予告以外のエラーなし、B02 Blender/GLBとT01検証資産は差分なし、B02 GLBのSHA-256は`98DE5E201CBA3D93DF01ACA27DD79B050C116A761AAE84F0DE6EEAC04F289EAE`のままである。
- Web開発サーバーを`http://127.0.0.1:5175/`で起動し、学校を選択した開始待ち画面を手動テスト用に残す。
