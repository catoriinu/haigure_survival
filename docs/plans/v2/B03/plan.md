# HAIGURE SURVIVAL v2 B03 学校ローポリ仕上げ 計画

更新日: 2026-07-20

## プロンプト

B02で承認された学校ブロックアウトを軽量ローポリとして仕上げる。教室、廊下、玄関、体育館、校庭を造形し、机、椅子、ロッカーなどを共通部品化する。窓は壁を実際に開口し、低ポリ窓枠、薄い透明ガラス表示、移動体専用の不可視衝突を分離する。マテリアル数と描画負荷を抑え、ゲームコードとNavMesh実行基盤は編集しない。`.blend`と`.glb`は本タスクの担当だけが編集する。

B02手動テストでは、階段の踏面・蹴上・踊り場が同色で境界を判別しにくく、中央側の腰壁も横から見ると段の角へ追従した上端との間に隙間が見えた。B03では衝突用の連続ガードを維持したまま、表示側を手すりとして自然に読める形状へ仕上げる。

B02追加テストでは男女トイレへ3個室ずつの薄壁・開放扉ブロックアウトを確定した。B03ではこの通路幅と個室数を維持して便器、個室前面、扉金物を仕上げ、男子側に予約済みスペースを使って小便器3基を追加する。

B02追加テストで北側・西側校舎を4階建てへ拡張し、北西階段を屋上まで接続した。屋上には常時開放扉の階段室、1階トイレ直上の男女更衣室2室、既存プールを配置している。B03では4階外壁・窓、北西上階階段、屋上施設、更衣室の内外を既存1～3階と同じ低ポリ密度で仕上げる。

B02の`VIS_WindowGuide_*`は位置・数・寸法・形状のいずれも仮であり、B03では現ガイドを確定配置として流用せず、全階の外観と室内用途を踏まえて窓構成を改めて検討する。閉じた窓は全移動体を止め、承認済みの開いた窓だけはプレイヤーとNPCを止めながらビットの固定経路通過を許可する。どちらも視線と全光線を通す。2～4階は廊下、教室区画、扉、階段を通行可能にし、家具、小物、材質、最適化はB03-2で仕上げる。

B02追加テストで、2階北側校舎の西側特別教室区画は中央仕切りで2室に分け、各室を1扉とすることを確定した。東側特別教室は1室のまま前方・後方の2扉を維持する。この平面変更は2階だけに適用し、1階の北側特別教室2室と西側統合特別教室は変更しない。

屋上プールの現水面はBlockout用の`VIS_PoolWaterPlaceholder`である。B03では水面材質と縁の見え方を仕上げ、後続ランタイムが水中判定に使う3D水Volumeの範囲をプール内形状と一致させる。

B03の各作業単位は、着手前に`docs/plans/v2/T04/plan.md`の「B03-0へのビット通過窓・屋上接近引き渡し契約」を必ず読み、同契約を窓・屋上設計の正本として扱う。

### 追加確定指示（2026-07-19）

> bit_windowは1〜4階のみ最低各階1か所、屋上は窓対象外。ただし屋上はゲーム/経路対象外ではない。屋外Aと屋上Bを結ぶビット専用・双方向必須の固定LNK kind bit_roofをB03で設計/資産化し、T05-1で実飛行、T06で統合検証。bit_windowもLNK_*でありPRT_*は使わない。どちらも自由飛行でなく端点高い方を超えない。
>
> 依存順 B03-0＋T04-2A→B03-1→B03-2→T04-2B→T05-1→T05-2→T06→T07。
>
> B03開始手順にT04 planの『B03-0へのビット通過窓・屋上接近引き渡し契約』必読。

### 2026-07-19 B03-0A追加指示

T04-2Aを待つ間に、窓・ビット侵入仕様と学校バイナリへ触れず、各部屋の内装、共通材質、不足小物、配置密度を設計する。最新`origin/develop`から`codex/v2-b03-design-freeze`と専用worktreeを作り、B03-Pの小物22種は承認済みの暫定入力として読み取る。未マージのB03-Pブランチからは派生しない。

`docs/plans/v2/B03/design_decisions.md`へ、学校全体の年代感、配色、Material・Atlas予算、各部屋の必須家具と配置密度、散乱率、追加小物、Collider・三角形予算を回答ID付きで整理する。各項目は2～3案を中立比較し、推奨案も提示する。全室を表へ収録し、普通教室、保健室・図書室、職員室、PC室・LL教室、生徒会室・放送室、理科室、美術室・家庭科室、音楽室・体育館・更衣室の8枚を簡易平面図で示す。

窓の位置、寸法、開閉状態、侵入可能分類、窓際の最終余白、ビット侵入経路、高さ付きNavMeshクリアランスは`W-*`IDへ分離し、T04-2A仕様を読むまで推測で確定しない。この段階では新しいBlender小物、学校`.blend`、GLB、NavMesh、ゲームコード、扉・手すり等の建築実装を変更しない。コミット、push、Pull Request作成は追加指示があるまで行わない。

### 2026-07-19 T04-2A引き渡し確認指示

> 今ちょうどT04-2Aのタスクから仕様が渡ってきたようです。隣のタスクを読み込み、プランを確認してください。そして、こちらのプランと整合性を取ってください。

隣接タスク`codex/v2-t04-height-nav-core`のT04計画、3D資産仕様、Runtime仕様を読み、B03の`W-*`保留と旧窓契約を監査する。衝突内容は自動採用しない。整合可能な契約はB03設計へ反映し、正確な窓位置、本数、開放／破損の見た目、屋上接近経路はB03-0のユーザー承認対象とする。

### 2026-07-19 W02・W07候補設計開始指示

> YES

T04整合後の次工程として、`W02`の通過窓密度・位置・見た目と、`W07`の屋上`bit_roof`本数・位置を2～3案で比較する。B02学校実形状は読み取りだけで確認し、候補図と回答表を作る。学校バイナリは変更しない。

### 2026-07-20 W02再設計・W07承認指示

> W07は案3がよいと思います。1か所だけだと、そこからしかビットが飛ばず、さすがに違和感があります。
>
> 問題はW02です。そもそも窓の位置、付ける位置は相談しましたか。こちらとしては、基本的にはどの部屋にも窓自体があり、廊下にもあり、校舎の外側と内側には大抵窓が付いている状態を考えています。その中でランダムに、5枚中2枚くらいが開き、5枚中3枚くらいは閉じているイメージです。破損までは今回考えなくて構いません。この前提で、どこに窓を付けるべきか考え直してください。教室、特別教室、体育館にも一周ぐるっと窓を付ける形でデザインし直してください。

窓の最終位置・総数は未承認だったため、旧W02案を撤回する。窓総数とビット通過窓数を分離し、全室、廊下、校舎外周・中庭側、体育館四周を対象にした窓配置を先に設計する。約40%の開放状態は実行時ランダムではなく、固定した複数パターンで表現する。破損窓は採用しない。W07は案3で確定する。

### 2026-07-20 W02-R1承認・体育館南面修正指示

> 案はR1でOKです。1点、体育館の南側の上部、つまり舞台側の辺には窓はなしでお願いします。閉じた窓ということではなく、完全に窓なしでお願いします。

修正版W02-R1を採用する。体育館南面（舞台側）は閉窓へ読み替えず、窓開口、窓枠、ガラス、窓Collider、窓用`LNK_*`のいずれも作らない完全な窓なしの壁面とする。体育館上部窓は北・東・西の3面だけに各3窓を置く。

### 2026-07-20 B03-1実装指示

> PLEASE IMPLEMENT THIS PLAN:
>
> B03-1「学校建築仕上げ」として、B03-0、T04-2A、B03-P小物ライブラリを引き継ぎ、承認済み設計を変更せずに学校の建築形状、窓、特殊接続、上階NavMesh生成元、天井Collider、屋上、プール、トイレを実装する。内装密度、散乱物、最終Material・Atlas調整、GLB最適化はB03-2へ残す。
>
> 校舎各階18窓と体育館北・東・西面各3窓の計81窓を実開口として作り、開33、閉48、破損0とする。体育館南面には窓関連Objectを一切作らない。閉窓は`COL_ActorOnly_Window_*`、開放窓は`COL_HumanOnly_Window_*`と33組の`bit_window`を持つ。北翼東端と西翼南端には2組の`bit_roof`を設ける。全35経路は双方向、`hs_link_radius_m=0.54`、半径0.54m包絡、`PRT_*`不使用とする。
>
> `NAV_Walkable_Interior2F/3F/4F`、`NAV_Walkable_StairsNWUpper`、`NAV_Walkable_Rooftop`と上階blocker生成元を追加し、全屋内の通常`COL_*`天井欠落を0件にする。プール内形状に一致する`VOL_PoolWater`を`hs_id="pool-water"`、新規`hs_role="water"`で追加する。B03-1では資産と仕様だけを更新し、TypeScript role登録、NavMeshベイク、カタログハッシュ、実Runtime回帰はT04-2Bへ引き渡す。
>
> 再生成、監査、1080p確認画像をスクリプト化し、`.blend`とGLBの再現性、Object名とMesh Data名、Object名と`hs_id`、Transform、法線、閉形状、single-user Mesh、Node extrasを検証する。NavMesh、B03-Pライブラリ、`src/`は変更しない。完了後は計画とロードマップを更新し、分割コミット、push、develop向けDraft Pull Request「feat: B03-1学校建築を仕上げ」を作成する。PRはマージしない。

## 教室用途メモ

| 階 | 西側通常区画 | 西側・北側の特別区画 |
| --- | --- | --- |
| 1階 | 南端の従来通常教室区画を保健室 | 西側統合特別教室を図書室、北側西特別教室を職員室、北側東特別教室を視聴覚室（パソコンルーム） |
| 2階 | 通常教室1～3を三年生教室 | 北側西を生徒会室、北側中央を放送室、北側東を理科室。西側と中央は旧西特別区画を中央仕切りで2室化し、各室1扉。東側理科室は1室・前後2扉 |
| 3階 | 通常教室1～3を二年生教室 | 北側西を美術室、北側東を家庭科室 |
| 4階 | 通常教室1～3を一年生教室 | 北側西をLL教室（多目的教室）、北側東を音楽室 |

この用途表は今回のB02追加修正では形状化せず、B03で上階内装を開放する際の平面・扉・備品配置の正本とする。

## ビット通過窓・屋上接近の確定契約

- `bit_window`は校舎1～4階だけを設置対象とし、各階へ最低1か所ずつ設ける。正確な場所、数、開いた窓／割れた窓の表示はB03-0で提示し、ユーザー承認後に固定する。
- 屋上は窓の設置対象外とする。ただし、屋上自体はプレイ範囲、NavMesh、索敵、標的追跡、戦闘の対象から外さない。
- 閉じた窓は`COL_ActorOnly_Window_*`でプレイヤー・NPC・ビットを止める。承認済みのビット通過窓は`COL_HumanOnly_Window_*`でプレイヤーとNPCだけを止め、ビット・視線・全光線を通す。
- 各ビット通過窓には、屋外の安全な待機位置Aと室内の床相対高度にある安全な待機位置Bを`LNK_<id>_A/B`として配置する。両端は`hs_link_kind="bit_window"`、`hs_bidirectional=true`とし、固定経路が窓開口中央を通るようにする。
- 屋外の安全な待機位置Aと屋上の安全な着地点Bを結ぶビット専用の`LNK_<id>_A/B`を最低1組設ける。両端は`hs_link_kind="bit_roof"`、`hs_bidirectional=true`とし、正確な位置と本数をB03-0で提示してユーザー承認後に固定する。
- `bit_window`と`bit_roof`はどちらも`LNK_*`で資産化し、`PRT_*`は使用しない。ビット専用かつ双方向必須の固定経路であり、自由飛行にはしない。経路上の最高高度はA/Bの高い方を越えない。
- 窓枠と特殊経路全体は、ビットの身体・銃口を含む半径約0.44mと安全余裕0.10mを合わせた半径0.54m以上の空きを確保する。`bit_roof`では庇、外壁、手すり、屋上構造物も同じ条件で避ける。
- カーペット3体編隊を通すために窓や屋上経路を広げない。特殊接続へ入る前の個別判定と編隊解除はT05-1が担当し、B03は通常追跡中のビット1体が安全に通れる資産寸法を保証する。
- B03-0は窓と屋上接近の候補比較・承認記録を確定し、学校バイナリを編集しない。B03-1は承認済みの開口、表示、Collider、`LNK_*`を学校資産へ追加する。B03-2は機能Objectの名前・位置・開口寸法を変えず、内装と最適化だけを行う。

## 実行単位と推奨設定

| 作業単位 | 推奨ブランチ | 推奨モデル | 推奨リーズニング | 所有範囲 |
|---|---|---|---|---|
| B03-0 設計確定 | `codex/v2-b03-design-freeze` | GPT-5.6 Sol | High | 本計画、窓・各階用途・造形密度・共通部品・マテリアルの承認資料。学校バイナリは編集しない |
| B03-1 建築仕上げ | `codex/v2-b03-architecture` | GPT-5.6 Sol | High | 学校`.blend`、GLB、窓、壁、階段、手すり、屋上、プール、トイレ、上階構造、`NAV_*`生成元、承認済み`LNK_*` |
| B03-2 内装・最適化 | `codex/v2-b03-interiors` | GPT-5.6 Sol | High | 同じ学校`.blend`、GLB、内装、共通部品、材質、容量、1080p表示。機能Objectの名前・位置・開口寸法を維持する。配置中心ならMediumでもよい |

B03-0はT04-2Aと並行できる。B03-1はB03-0とT04-2Aの両方が`develop`へマージされた後に開始し、B03-2はB03-1の後に開始する。同じ学校バイナリ資産を扱うB03-1とB03-2は直列とし、T04-2BはB03-2完了後に開始する。依存順は`B03-0＋T04-2A → B03-1 → B03-2 → T04-2B → T05-1 → T05-2 → T06 → T07`とする。各作業単位は`docs/plan.md`、`docs/plans/v2/next_tasks_plan.md`、本計画、`docs/branch_strategy.md`、およびT04計画の「B03-0へのビット通過窓・屋上接近引き渡し契約」を必ず読む。

## ステップ

### B03-0A 窓仕様待ち先行設計

- [x] 2026-07-19の日付、最新`origin/develop`、全worktree、T04-2Aの並行状態を確認する
- [x] 最新`origin/develop`の`fe88083`から`codex/v2-b03-design-freeze`と専用worktreeを作る
- [x] B02の確定間取り、部屋用途、寸法、扉位置とB03-P小物22種を読み取りで照合する
- [x] 回答ID、比較案、推奨案、全室マトリクス、追加小物優先度を設計決定資料へ保存する
- [x] 代表室8枚の簡易平面図を作る
- [x] `W-*`保留、B03-P小物22種の利用先、追加小物の寸法・Collider・Material・三角形目標を監査する
- [x] `D01`～`D10`のユーザー回答を設計決定資料と代表室図へ反映する
- [x] T04-2Aの引き渡し契約、3D資産仕様、Runtime仕様を読み、`W01`～`W06`と旧窓契約を監査する
- [x] 閉窓、ビット通過窓、`bit_window`、`bit_roof`、半径0.54m包絡、天井Colliderの契約をB03設計へ反映する
- [x] 学校バイナリ、NavMesh、ゲームコード、T04-2A不変とUTF-8・Git差分を検証する

### B03-0B ビット通過窓・屋上接近候補

- [x] B02学校を別バックグラウンドBlenderで読み取り、校舎・体育館の窓ガイド数、高さ、屋上施設・プール・空き帯を確認する
- [x] 旧`W02`へ通過窓6・10・15経路の3案を作る。この比較は窓総配置を含まないため2026-07-20に撤回する
- [x] `W07`へ東端1経路、中庭側1経路、東端＋南西端2経路の3案を作る
- [x] 窓密度比較図と屋上接近比較図を作る
- [x] 旧比較の推奨を`W02案2、W07案1`として回答表に記録する。W07はユーザー回答で案3へ変更する
- [x] W07案3を採用し、北翼東端と西翼南端の2経路へ確定する
- [x] 旧W02案を撤回し、全室・廊下と体育館上部を含む総窓配置の`W02-R1`～`R3`へ改訂する
- [x] 5窓中約2窓を開ける固定パターンと、破損窓を使わない規則を作る
- [x] ユーザー回答を受け、修正版W02-R1を採用する。校舎72窓、体育館北・東・西面9窓、全81窓、開33、閉48、開放率40.7%とし、体育館南面（舞台側）は完全な窓なしとする

### B03-1 学校建築仕上げ

- [x] 2026-07-20の日付、専用worktree、`codex/v2-b03-architecture`、Blender保存先、`origin/develop`の同一HEAD、GitHub CLI認証を確認する
- [x] 開始基準を504 Object／480 Mesh、`VIS_*`308件、`COL_*`161件、`NAV_*`8件、`VOL_*`2件、Guide 22件として記録する
- [x] 開始SHA-256を学校`.blend`=`6F68041134DEA543BDF7FA13193ECA46A729145E973BD28C5855AAEB72DDE51B`、GLB=`294F8E4370CE736726166AE14CBABEB89CBB40DE875CFD2FF40A9F9E20EB9C8C`、NavMesh=`7326422B609D810E8736B48A08EC819AA962848769A718E175463FE6191A5347`、B03-Pライブラリ=`71E461799D915B046483C9B2343C13818AC12203F94C3481E857E56DDA4F09A4`として記録する
- [x] 再生成スクリプトで上階建築、2階北翼西側2室化、階段境界、手すり、トイレ、屋上施設、プールを仕上げる
- [x] W02-R1の窓81件、閉窓48件、開放窓33件と、体育館南面窓関連0件を実装する
- [x] `bit_window` 33組と`bit_roof` 2組を実装し、双方向、半径0.54m包絡、開口中央通過、`PRT_*`不使用を検査する
- [x] 2～4階、北西上階階段、屋上の`NAV_*`生成元と上階blockerを追加し、通常NavMeshの窓越し接続を作らない
- [x] 全屋内の天井Colliderと、`hs_role="water"`を持つ`VOL_PoolWater`を実装する
- [x] 学校GLBを再出力し、Blender／GLB監査、2回生成一致、1080p確認画像、GLB単体表示を検証する
- [x] Python構文、UTF-8 BOMなし、Markdown、ローカル絶対パスなし、`git diff --check`、変更allowlist、通常・T01～T04ビルドを検証する
- [x] NavMesh、B03-Pライブラリ、`src/`が不変であることをSHA-256とGit差分で確認する
- [x] B03-1結果、B03-2とT04-2Bへの引き渡し、全体計画、次工程を記録する
- [x] 建築資産・スクリプトと文書を分割コミットし、pushしてdevelop向けDraft PRを作成する

- [x] `docs/plan.md`、`docs/plans/v2/next_tasks_plan.md`、B03計画、T04計画の引き渡し契約、`docs/spec_stage_assets_v2.md`、`docs/spec_stage_runtime_v2.md`、B03-Pカタログを読む
- [x] 造形密度、共通部品、マテリアル構成を確定する
- [x] B03-0で、2階を三年生、3階を二年生、4階を一年生として、各階用途、窓配置、造形密度、共通部品、マテリアル構成を承認する。全室・廊下・校舎外周・中庭側と体育館北・東・西面の窓総数、体育館南面完全窓なし、破損なし・約40%開放の固定配置を確定する
- [x] 校舎内外と体育館をローポリ造形する
- [x] 2～4階の廊下、教室区画、扉、階段を通行可能にし、4階外壁を1～3階と同じ意匠へ仕上げる。現行4階窓ガイド18件は確定数・確定位置として扱わない
- [x] 最終形状に一致する2～4階、北西上階階段、屋上の`NAV_*`生成元を追加する。NavMesh Runtime、NPCロジック、`.navmesh.bin`はB03で変更しない
- [x] 2階北側校舎の西側特別教室区画だけを中央仕切りで2室化し、各室へ1扉を設ける。東側特別教室は1室・前後2扉を維持し、他階へ同じ変更を複製しない
- [x] 屋上階段室の常時開放扉、男女更衣室2室、既存プール、転落防止壁を一体の屋上施設として仕上げ、更衣室へロッカー・ベンチ等を追加する場合も共通部品化する
- [x] `VIS_PoolWaterPlaceholder`を軽量な水面表示へ仕上げ、プール内形状と一致する後続用の3D水Volume範囲を確定する
- [x] 階段の踏面・蹴上・踊り場を、同系色のままでも1080p既定表示で明暗、材質差、縁取りのいずれかにより判別できるようにする
- [x] 校舎3階段、北西階段の2階～屋上区間、体育館舞台階段の表示用ガードを、横から見ても踏面・蹴上・踊り場との不自然な隙間や上端高の不連続がなく、斜路へ連続する上桟・支柱または腰壁を持つ手すり形状へ仕上げる
- [x] B02の窓ガイドを仮資料としてのみ参照し、全階の窓について位置、数、寸法、形状を改めて設計してから、壁の実開口、上下左右を分割した低ポリ壁、窓枠を作る。通過窓は有効開口の幅・高さを最低1.08mとする
- [x] ガラスを残す閉じた窓の`VIS_WindowGlass_*`を1枚面または極薄板で作り、透明ガラスマテリアルを共通化する。ビット通過窓はB03-0で承認した破損なしの通常開放状態を正本とし、開口全体を塞ぐガラス表示を置かない
- [x] 閉じた窓の開口全体へ`COL_ActorOnly_Window_*`を置き、プレイヤー・NPC・ビットの移動を遮り、視線と全光線を通す分類を維持する
- [x] 承認済みの1～4階のビット通過窓へ`COL_HumanOnly_Window_*`を置き、プレイヤーとNPCだけを遮る。屋外A・室内Bを`LNK_<id>_A/B`、`hs_link_kind="bit_window"`、`hs_bidirectional=true`で資産化し、窓開口中央を通る固定経路とする。通常歩行用NavMeshを窓越しに接続しない
- [x] 屋外A・屋上Bを`LNK_<id>_A/B`、`hs_link_kind="bit_roof"`、`hs_bidirectional=true`で資産化する。屋上を窓対象からは外すがゲーム・経路対象には含め、庇、外壁、手すり、屋上構造物との安全余裕を確保する
- [x] `bit_window`と`bit_roof`に`PRT_*`を使用せず、両方の固定経路がビット専用・双方向で、最高高度が端点A/Bの高い方を越えない配置になっていることを検査する
- [x] 各`bit_window`／`bit_roof`のA/B間全区間について、半径0.54mの移動包絡が窓枠、壁、庇、外壁、手すり、屋上構造物へ触れず、安全余裕を終端まで維持することを資産検査する。A/B待機位置と経路上へ家具・散乱物を置かない
- [x] B03-1で校舎1～4階、階段下、体育館、屋上階段室を含む全屋内空間の直上に通常`COL_*`天井があり、ビットが入り得る範囲に天井Colliderの欠落がないことを資産検査する
- [ ] 机、椅子、ロッカーなどを共通部品として配置する
- [x] 男女各3個室の確定配置を維持し、便器、個室前面、扉・金物を軽量な共通部品で仕上げる
- [x] 男子トイレのX＝-2.55～-2.25m、Y＝39.0～42.2mへ小便器3基を中心Y＝39.6／40.6／41.6mで配置し、西側X＝-3.55～-2.55mの立位空間を塞がない
- [x] 衝突メッシュを表示メッシュから分離した状態で維持する
- [x] 現行Runtimeが共有glTF meshから生成される`InstancedMesh`を作者Meshとして受理しないため、同一窓タイプの`VIS_WindowFrame_*`、`VIS_WindowGlass_*`、窓ColliderでもMesh datablockを共有せず、single-userを維持してMaterialだけを共通化する。将来共有する場合は、専用fixtureでinstance契約を更新してから採否を再検討する
- [ ] 同じ壁面・階のガラスを可能な範囲でまとめ、共通マテリアル、透明面の重なり、ドローコール、GLB容量を検査する
- [x] GLBを書き出し、命名、縮尺、マテリアル、容量を検査する
- [ ] ゲーム内表示と1080p性能の予備確認を行う
- [ ] 本計画と全体`docs/plan.md`へ結果を記録する

## 結果

### 2026-07-20 B03-1 学校建築仕上げ

- 学校資産を702 Object／608 Meshへ更新し、`EXP_Stage_school`だけを2,571,324 bytesのGLBへ出力した。`.blend`のSHA-256は`CE91EF9FF5FD8095C69D56812580D7DA7D9BF8CEC282D6DA34C7CAE0B214EBEA`、GLBは`5EC50FF09E8E2E74F9DC2A5C6402660FB4A4E5CFA084FB5E8B570B832F7BD8D9`である。
- 校舎1～4階と体育館の実開口81窓を作り、開33、閉48、破損0、体育館南面窓関連0を確認した。閉窓48件は`VIS_WindowGlass_*`と`COL_ActorOnly_Window_*`、開放窓33件は`COL_HumanOnly_Window_*`を持つ。
- `bit_window` 33組、`bit_roof` 2組を追加し、A/B pair、双方向、半径0.54m、開口中央、`PRT_*` 0件を監査した。通常NavMeshは更新せず、窓越し接続も追加していない。
- `NAV_Walkable_Interior2F/3F/4F`、`NAV_Walkable_StairsNWUpper`、`NAV_Walkable_Rooftop`、`NAV_Blocker_SchoolUpper`、全屋内天井Colliderを最終形状から作成した。`.navmesh.bin`は開始時SHA-256を維持する。
- `VIS_B03_PoolWater`と閉じた`VOL_PoolWater`を追加し、`hs_id="pool-water"`、`hs_role="water"`を資産仕様とRuntime仕様へ記録した。TypeScriptのrole登録、NavMesh再ベイク、カタログハッシュ更新、実Runtime回帰はT04-2Bへ引き渡す。
- 生成器を連続2回実行し、`.blend`とGLBの件数、容量、SHA-256一致を確認した。Blender／GLB監査、7枚の1920×1080画像、GLB単体ブラウザ表示（VIS 368／全Mesh 618、console warning・error 0件）、Python構文、UTF-8 BOMなし、Markdown表、ローカル絶対パス0件、`git diff --check`、通常・T01～T04ビルドを確認した。
- B03-PライブラリはSHA-256 `71E461799D915B046483C9B2343C13818AC12203F94C3481E857E56DDA4F09A4`、NavMeshは`7326422B609D810E8736B48A08EC819AA962848769A718E175463FE6191A5347`を維持し、`src/`の差分は0件である。
- B03-2は機能Objectの名前、位置、開口寸法、`hs_id`、Collider、`LNK_*`、`VOL_*`、`NAV_*`を維持し、家具密度、散乱物、装飾、Material・Atlas、GLB最適化、1080p性能を仕上げる。

B03全体のうちB03-0設計確定とB03-1建築仕上げを完了した。B03-2内装・最適化は未着手である。T04計画の引き渡し契約を窓・屋上設計の正本とし、B03-1の機能Objectを維持したままB03-2へ引き渡す。

2026-07-20、PR #43のT04-2A、PR #44のB03-P小物ライブラリ、B03-0が`develop`へ統合済みであることを確認してB03-1を完了した。T04-2Aの引き渡し契約は本資産へ反映済みで、B03-Pの最終カタログ22種も利用先監査と一致する。次はB03-1のPull Request統合後にB03-2を開始する。
最新の窓判定契約では、閉じた窓を`COL_ActorOnly_Window_*`、承認済みの開いた通過窓を`COL_HumanOnly_Window_*`とする。前者は全移動体、後者はプレイヤー・NPCだけを止め、両方とも視線と全光線を通す。2階は三年生、3階は二年生、4階は一年生とし、B03-1で2～4階の廊下、教室区画、扉、階段を通行可能にする。階段境界、手すり、トイレ、4階外形、北西上階階段、屋上施設、2階北側校舎、プール水面の実装はB03-1以降へ引き渡す。

B03-0Aの設計資料作成を完了した。2026-07-19、並行中のT04-2A worktreeとB03-P worktreeを変更せず、最新`origin/develop`の`fe88083`からB03-0A専用worktreeを作成した。B02学校`.blend`は別バックグラウンドBlenderで読み取るだけとし、確定床範囲、扉位置、体育館24m×36m、更衣室4.5m×7mなどを抽出した。学校バイナリの保存・出力は行っていない。

`design_decisions.md`へ`D01`～`D10`の比較と推奨、全室マトリクス、B03-P小物22種の利用先、追加小物26種のP0～P2優先度、当初の`W01`～`W06`保留を記録した。T04-2A到着後は`W01`、`W04`、`W05`を解消し、屋上接近位置を`W07`として追加した。代表室8枚の簡易平面図を同一凡例で生成し、家具、主通路、散乱候補、窓位置承認待ち保留帯を目視確認した。図面はすべて1,600×1,000pxで、再生成前後のSHA-256が一致した。

回答ID 10件、保留ID 6件、B03-P小物22種、追加小物26種、Markdown表の列数を機械監査した。学校`.blend`、GLB、NavMeshは既知のSHA-256を維持し、学校資産、ゲームコード、検証コード、T04-2A成果物のGit差分は0件である。更新範囲は本計画、設計決定資料、図面8枚、図面生成スクリプトだけで、UTF-8 BOMなし、Python構文、`git diff --check`、ローカル絶対パスなしを確認した。コミット、push、Pull Request作成は行っていない。

2026-07-19のユーザー回答により、非窓設計の`D01`～`D10`を確定した。年代感は案1を基準に、古さを必須条件とせず清掃の行き届いた現役校として経年劣化を最小限にする。基本配色は案1、階別識別は案2とし、1階を黄土、2階を深緑、3階をえんじ、4階を紺に確定した。階段境界は案1、Material・Atlas予算は案2、普通教室は30組の案2、散乱率は15～25%の案2、特別教室密度は案2、追加小物順は共通品優先の案1、体育館・屋上密度は競技設備と安全設備だけの案1を採用した。D10の確定に合わせ、体育館図からボール籠の標準配置を外した。

B03-0Aの非窓設計資料は確定済みである。続いて、T04-2Aの引き渡し契約と実装後の3D資産・Runtime仕様を読み、`W01`～`W06`を監査した。旧計画の「全窓へ`COL_ActorOnly_Window_*`」は、閉窓だけを同Colliderとし、通過窓は`COL_HumanOnly_Window_*`と`bit_window` pairを組み合わせる最新契約へ更新した。校舎1～4階へ最低各1か所、有効開口1.08m以上、特殊経路の半径0.54m包絡、通常NavMeshの窓越し非接続、全屋内の天井Collider、双方向`bit_roof`をB03-1の受入条件へ追加した。拒否を要する衝突はなかった。

T04-2AはPR #43で`develop`へ統合済みである。`W01`、`W04`、`W05`はT04契約で解消し、`W07`は案3の2経路で解消した。`W03`の通常通路実配置監査はB03-2とT04-2Bへ、`W06`はsingle-user維持の実装規則としてB03-1・B03-2へ引き渡す。B03-0が`develop`へ統合された後にB03-1へ進む。

`W02`と`W07`の承認候補を`window_link_options.md`へ作成した。保存済みB02 `.blend`をBlender 5.2.0 LTSの別バックグラウンドプロセスで読み、校舎各階18面、体育館上部12面の仮窓ガイドと、屋上施設・プール・空き帯の実境界を確認した。ガイドは確定窓として流用せず、W02を6・10・15経路、W07を1・1・2経路の各3案で比較した。推奨は、校舎各階2か所と体育館上部2か所を全て開放窓にする`W02案2`、プールと屋上施設から離れた北翼東端へ1経路を置く`W07案1`である。比較図2枚を追加し、学校バイナリの保存・出力は行っていない。両IDはユーザー回答まで未確定である。

2026-07-20、W07は「1か所だけから上昇すると不自然」というユーザー判断により案3を採用し、北翼東端と西翼南端の2経路へ確定した。W02は窓総配置の相談が未実施だったことを確認し、6・10・15経路だけを比較した旧案を撤回した。改訂案では全室・廊下・校舎外周・中庭側と体育館上部を窓対象とし、破損窓を使わず、5窓中約2窓を固定パターンで開ける案を提示した。

同日、ユーザー回答により修正版`W02-R1`を採用した。校舎は各階18窓、全72窓とし、体育館は北・東・西の上部へ各3窓、全9窓を置く。全81窓のうち33窓を開放、48窓を閉鎖し、開放率40.7%とする。体育館南面（舞台側）は閉窓ではなく、窓開口、窓枠、ガラス、窓Collider、窓用`LNK_*`を一切作らない完全な窓なしの壁面とする。これにより`D01`～`D10`、`W02`、`W07`のユーザー承認が揃い、B03-0の設計確定を完了した。学校`.blend`、GLB、NavMesh、ゲームコードは変更していない。この時点ではT04-2AとB03-0の`develop`統合をB03-1の開始条件とした。

窓配置図を更新し、体育館南面に窓マーカーがなく、舞台側が完全窓なしと明記されていることを目視確認した。屋上接近図はW07案3・2経路の採用表示を維持した。図面10枚は全て1,600×1,000pxで、再生成前後のSHA-256が一致した。B02学校`.blend`、GLB、NavMeshは既知のSHA-256を維持した。対象文書とスクリプトのUTF-8 BOMなし、Python構文、Markdown表列数、ローカル絶対パスなし、`git diff --check`、変更範囲のallowlist一致を確認した。コミット、push、Pull Request作成は行っていない。
