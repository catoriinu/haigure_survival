# HAIGURE SURVIVAL v2 T06 学校3Dステージ統合 計画

更新日: 2026-07-26

## プロンプト

T05-1A、T05-1B、T05-2、T05-2V、B03、B04の成果を統合し、JSON文字マップやセル座標を使用せず、学校のGLB、3Dマーカー、3Dボリューム、人間用NavMesh、ビット用NavMesh bundleを接続する。プレイヤー、NPC、ビット、光線、視線、集合、警報、公開処刑を学校全域で検証する。閉じた窓では実開口、透明ガラス表示、全移動体の遮断を確認する。ビット通過窓では、プレイヤー・NPCの遮断、ビット専用`aperture`遷移、視線・全光線透過を確認する。屋外4帯、校舎各階、体育館上下段、階段`surface-route`、4F屋外帯と屋上外周の`boundary`、塀外5.0mの外周BIT飛行帯を同じ共通Runtimeで統合する。

既存8ステージはV2統合対象から外し、互換アダプターを作らない。将来、各ステージをGLBとNavMeshへ移行した時点で、学校と同じ3D資産契約へ登録する。

B02手動テストではV2学校でBGM、SE、VOICEが再生されなかった。読込失敗ではなくV2入口へ再生処理が未接続のため、既存の音量設定と再生ルールを維持してT06の学校統合で接続する。

B02屋上テストではプールの水面表示を維持し、プール内へ入っている間だけプレイヤー移動速度を通常の50%にすることを確定した。水中領域はJSONやセルではなく、B03でプール形状へ一致させる3D水Volumeを正本とする。

### 旧追加指示（2026-07-19、T05-1の2026-07-25契約で置換済み）

> 屋外Aと屋上Bを結ぶビット専用・双方向必須の固定LNK kind bit_roofをB03で設計/資産化し、T05-1で実飛行、T06で統合検証。bit_windowもLNK_*でありPRT_*は使わない。どちらも自由飛行でなく端点高い方を超えない。
>
> 依存順 B03-0＋T04-2A→B03-1→B03-2→T04-2B→T05-1→T05-2→T06→T07。

### 追加確定指示（2026-07-25）

> T05-1は、ステージ非依存の飛行ゾーン・飛行帯・別ビットNavMesh・接続グラフと、飛行安全・立体挙動へ分割する。学校固有の階数、体育館、屋上を共通Runtimeへ記述しない。旧`bit_roof`は完全廃止し、学校の4F屋外帯と屋上外周は`boundary`遷移で接続する。
>
> 修正案1として、V1実形状・戦闘用被弾球0.44m＋安全余裕0.10m＝移動包絡0.54mを使用する。上下揺れは中心位置のSweepであり、揺れ幅を半径へ二重加算しない。現行片羽開放幅1.20mの58窓はこの包絡で再検証する。

### V1プレイヤーモード差分の確認指示（2026-07-25）

> ブレインウォッシュインプログレスになった後に、バージョン1ではG、H、Nのキーを押すと、プレイヤーは次のモードに移行することができましたが、プレイヤーのモード変更機能が失われています。
>
> 今後のタスクで実装する予定であればよいのですが、実装予定がなかった場合は、どのタスクで実装するのが正しいか検討して、今後のプランに反映してください。

### V1 BIT動的スポーン復元指示（2026-07-26）

> 開始時のビット数（デフォルト1機）から始まり、出現間隔秒ごとに1機出現して最大数まで増えるV1相当の機能を追加してください。出現位置はステージ内の侵入可能で壁に埋まらない位置、かつステージ資産で明示された出現禁止区域の外からランダムに選びます。

### プレイヤー開始地点の複数候補・動的出現禁止指示（2026-07-26）

> プレイヤー開始地点を複数の場所からランダムに選択してください。候補は現在の1階玄関、体育館中央、体育館舞台の演説台裏、2階・3階・4階の普通教室です。安全に開始できるなら、トイレ、各特別教室、屋上プール周辺も候補へ含めてください。
>
> 選ばれたプレイヤー開始地点の周囲には、BITと洗脳済みNPCを初期出現させないでください。開始地点が変われば出現禁止範囲も同じ選択結果に追従する、動的な仕組みにしてください。

### 学校外周・世界境界統合指示（2026-07-26）

> 塀の外側約5メートルに歩道と道路を追加し、そこを描画・BIT・光線の最終限界にしてください。プレイヤーとNPCは塀の外へ立ち入らず、全キャラクターを外周へ出現させないでください。BITは通常探索や出現では外周を使わず、追跡などの場合だけ一時的に浮遊できるようにしてください。
>
> 光線は世界境界で衝突・着弾扱いにせず自然に消し、既存の最大寿命も残してください。

## 確定方針

- 学校用ステージJSONは作成しない。
- TypeScriptカタログはステージID、表示名、GLB URL、人間用NavMesh URL、ビット用NavMesh bundle URL、各profile IDとSHA-256、空間を持たないタイトル設定だけを保持する。
- スポーン、向き、集合地点は`MRK_*`、連続遷移領域は`VOL_*`、境界は`BND_*`、離散的な狭い開口だけは`LNK_*`を正本とする。
- 通常扉と階段は連続NavMeshで接続し、セルや手書き階段リンクへ戻さない。
- 窓58組は`LNK_*`のビット専用・双方向`aperture`として扱い、プレイヤー・NPCの経路へ公開しない。`PRT_*`と旧`bit_roof`は使用しない。
- 上下移動は`vertical`、階段は`surface-route`、屋外・屋上外周は`boundary`として資産が明示し、帯の隣接をIDや高さから推測しない。
- ビットの同一帯横移動はビット用NavMesh、帯・ゾーン変更は接続グラフ、実移動の最終権威はV1物理半径0.44m＋安全余裕0.10m＝半径0.54mの3D移動包絡とする。
- 屋上は窓対象外であるだけで、プレイ範囲、NavMesh、索敵、標的追跡、戦闘の対象から外さない。
- 学校以外の旧JSONステージ切替は受入条件に含めない。学校の再読込と資源破棄を検証する。
- 学校のBGM、移動・戦闘SE、VOICEは既存の音量設定と再生条件を正本とし、学校再読込やランタイム破棄で多重再生・イベント購読・Audio資源を残さない。
- プール水中判定は3D水Volumeへの進入・滞在・退出で行い、滞在中のプレイヤー水平移動速度を通常の50%にする。退出、再配置、タイトル復帰、ステージ再読込では通常速度へ確実に戻す。
- T04-2Bで`VOL_PoolWater`の読込、内外問い合わせ、プール底へのNavMesh到達、破棄・再読込までを先行確認済みである。T06はその確定済みVolumeを使い、水平速度50%と通常速度への復帰だけをゲーム進行へ接続する。
- プレイヤー洗脳状態の正本と`selectPlayerCompletion` APIはT05-2成果を使用し、T06で別の状態機械を作らない。`brainwash-in-progress`へ入った直後から選択を解放し、`playing`終了まで完了状態間を再選択できるV1最終実装の契約を維持する。旧アーカイブ仕様の10秒待機は導入しない。
- 実ゲーム入力はG＝`brainwash-complete-gun`、N＝`brainwash-complete-no-gun`、H＝`brainwash-complete-haigure`とする。G／Nでは移動可能、Hでは移動不可、Gでは左クリック射撃、Nでは接触妨害を既存T05-2 Runtimeへ接続する。
- 選択解放後はG／N／Hの操作案内を表示し、G状態のときだけ照準と左クリック射撃案内を表示する。状態変更時はプレイヤー表示とVOICEを同期し、V1に存在しない選択専用SEは追加しない。
- G／N／Hのkeydown、左クリック、VOICE、操作案内の購読は学校Runtimeの開始・破棄単位で所有し、タイトル復帰または再読込後に重複させない。
- V1実プレイと同じく、通常設定は初期1機、既定出現間隔10秒、最大25機とする。通常BITとAlert増援は最大数へ含め、カーペット僚機は含めない。
- T05の即時stress設定は通常テスト初期20／最大20、性能受入初期50／最大50として維持し、通常ゲームの時間増援と混同しない。`bitCount`は`initialBitCount`へ改名し、Runtimeへ必須の出現間隔・最大数を渡す。旧名互換や既定fallbackは作らない。
- 出現候補は飛行帯ごとの明示`bit_spawn` Volumeから選び、ビット用NavMesh投影、0.54m安全包絡、共通遷移グラフへの到達性を満たし、`no_enemy_spawn`、`no_enemy_enter`、`hazard`、`water`を除外する。
- プレイヤー開始候補は向き付き`MRK_PlayerSpawn_*`として複数資産化し、安全な人間用NavMesh位置、初期カメラ空間、壁・家具・水域との非干渉を満たす候補から、共通seedを使って1件選ぶ。候補0件時の旧単一地点fallbackは作らない。
- 最低候補は現1階玄関、体育館中央、体育館舞台演説台裏、2階・3階・4階の普通教室とする。安全契約を満たすトイレ、特別教室、屋上プールサイドも候補へ加える。水中、閉鎖区画、扉・家具との干渉位置は候補にしない。
- 各開始候補には同じID suffixを持つ`VOL_PlayerSpawnExclusion_*`を対応付け、選択された候補のVolumeだけを動的な出現禁止範囲として有効にする。Runtimeへ学校座標や固定半径を埋め込まない。
- プレイヤー開始候補を先に確定し、その選択結果と有効な出現禁止VolumeをBIT出現と初期洗脳済みNPC出現の双方へ必須入力として渡す。未選択の候補周囲は、この動的禁止だけを理由には除外しない。
- 学校の出現可能な各飛行帯へ`VOL_BitSpawn_*`を資産化する。共通グラフから到達可能で出現を許可する帯は1件以上のVolumeで利用可能面積を覆い、Volumeがない帯を暗黙に候補から落とさない。出現させない領域は`no_enemy_spawn`、`no_enemy_enter`、`hazard`、`water`で明示する。
- NPCはBIT用Volumeを使わず、人間用NavMeshへ投影できる`VOL_NpcSpawn_*`から選ぶ。初期洗脳済みNPCを選択中の`VOL_PlayerSpawnExclusion_*`外で先に必要数生成し、残る未洗脳NPCを通常候補から生成する。全NPC間の最小距離を維持し、候補不足時に要求人数を減らすfallbackは作らず厳格エラーにする。
- ステージ全域からのランダム選択は、V1の床セル一様抽選に近い利用可能面積比例とする。帯ごと同率の校舎内優遇は採用しない。
- B04の`BND_WorldLimit`は、プレイヤー・NPC用`BND_Stage`と分離した表示・BIT・光線の最終限界として接続する。プレイヤーとNPCの再配置・領域外判定へ流用しない。
- 外周BIT飛行帯はCHASE、逃走、明示`boundary`遷移中だけ利用可能とする。通常探索、待機、総当たり探索、Alert集合先、時間増援、初期出現の候補にはしない。標的喪失、断念距離超過、追跡完了後は学校内の到達可能帯へ戻す。
- 外周へ`player_spawn`、`npc_spawn`、`bit_spawn`が0件であること、プレイヤー・NPCの人間用NavMeshが塀外へ延びていないことを厳格に検証する。
- T05-2Vの全光線は`BND_WorldLimit`退出点で約0.2秒の非着弾フェードへ入り、壁着弾、ダメージ、Alarm、反射光球が発生しないことを実学校で確認する。最大寿命20秒は維持する。
- `brainwash-in-progress`、gun、no-gun、haigureのNPC表示をV1対応スプライトセルへ分離し、状態Snapshot、衝突、射撃、接触拘束のロジックを変更しない。

## 実行単位と推奨設定

- 推奨ブランチ: `codex/v2-t06-school-integration`
- 推奨モデル: GPT-5.6 Sol
- 推奨リーズニング: Ultra
- 理由: B03最終資産、T04複数階NavMesh、T05戦闘、音声、水中、学校全域、資源ライフサイクルを一つの実ゲームへ統合するため。
- 代替: GPT-5.6 Sol / Extra High。単一の既知不具合修正だけならHighへ下げてよい。
- 開始条件: B03-2、T04-2B、T05-1、T05-2、B04、T05-2Vが`develop`へマージ済みであること。他の成果ブランチと並行実装しない。後続順は`B04＋T05-2V → T06 → T07`とする。

## ステップ

- [ ] `docs/plan.md`、`docs/plans/v2/next_tasks_plan.md`、3Dステージ資産・実行仕様、T01～T05-2V、B01～B04の計画と結果を読む
- [ ] 学校GLB、人間用NavMesh、ビット用NavMesh bundle、TypeScriptカタログの版と対応を監査する
- [x] 現B02の`MRK_*`、`VOL_*`、`BND_*`と事前ベイクNavMeshを`StageSpatialContext`へ接続する
- [ ] B03、T05-1A、T05-1B、T05-2で追加する意味Object、窓58組の`aperture`、15件の`vertical`／`surface-route`／`boundary`を`StageSpatialContext`へ接続する。`PRT_*`と旧`bit_roof`が0件であることも監査する
- [ ] プレイヤー、NPC、ビット、通常・固定・トラップ・動的ビーム、視線を学校全域で検証する
- [ ] `BND_Stage`と`BND_WorldLimit`を同時に読み、前者だけをプレイヤー・NPC領域外判定、後者だけをBIT・全光線の最終限界へ使用する
- [ ] 外周歩道・道路へプレイヤー・NPCが到達せず、全キャラクターの出現0件、通常探索・待機・総当たり探索・Alert集合先0件であることを検証する
- [ ] BITがCHASE・逃走・明示遷移中だけ外周飛行帯へ一時進入し、世界境界を越えず、追跡終了後に学校内へ戻ることを検証する
- [ ] 全発射元の光線が`BND_WorldLimit`退出点で着弾・ダメージ・Alarm・反射光球なしに0.2秒フェードし、最大寿命20秒も維持することを実学校で検証する
- [ ] 主玄関、教室扉、3階段、校庭、渡り廊下、体育館を連続移動できることを検証する
- [ ] 閉じた代表窓でプレイヤー・NPC・ビットが通過せず、視線と通常・固定・トラップ・動的ビームが透過することを検証する
- [ ] 1～4階と体育館の全58窓`aperture`で、現行片羽開放幅1.20mを半径0.54mの移動包絡が窓枠・進入角度を含めて双方向に通過でき、プレイヤー・NPCは通過せず、視線と通常・固定・トラップ・動的ビームが透過することを検証する
- [ ] 窓周囲の通常壁では視線と光線が停止し、窓開口だけが透過領域になることを検証する
- [ ] 4F屋外帯と屋上外周の`boundary`でビットだけが双方向に通過し、屋上標的へ接近して通常範囲へ戻れることを検証する。屋上帯の床上1.0～3.6mとステージ上端19.0mの安全包絡を守り、屋上で索敵・追跡・戦闘が継続することも確認する
- [ ] 探索、通常追跡、アラート追跡、戦闘接近、特殊編隊の全モードで現在帯の許可高度、直上天井、V1物理半径0.44m＋安全余裕0.10m＝移動包絡0.54mが共通適用され、上下揺れが中心Sweepとして半径へ二重加算されないことを検証する
- [ ] 校舎1～4階、階段下、低い天井、体育館、屋上階段室、窓枠、庇、外壁、手すりでビット全体が衝突前に停止または高度を下げ、全屋内の天井Collider欠落が0件であることを実学校資産で検証する
- [ ] `aperture`／`vertical`／`surface-route`／`boundary`遷移中の標的喪失で近い端点または隣接帯へ離脱することと、カーペット3体の個別安全判定失敗時に編隊が解除されることを学校実資産で検証する
- [ ] 集合、警報、公開処刑、スポーン禁止領域を3Dマーカー・ボリュームで検証する
- [ ] 通常ゲームを初期1機、10秒ごと1機、最大25機へ接続し、タイトルの初期数・出現間隔・最大数設定をV2 Runtimeへ渡す
- [ ] 1階玄関、体育館中央、舞台演説台裏、2～4階普通教室を含む`MRK_PlayerSpawn_*`候補を追加し、トイレ・特別教室・屋上プールサイドの安全な追加候補を資産監査で確定する
- [ ] 各開始候補へ対応する`VOL_PlayerSpawnExclusion_*`を追加し、プレイヤー候補選択後に対応VolumeだけをBIT・洗脳済みNPCの出現禁止へ有効化する
- [ ] 学校の全出現可能飛行帯へ面積付き`bit_spawn` Volumeを追加し、到達可能帯の被覆、NavMesh投影・0.54m安全包絡・到達性・既存禁止Volume・選択中の開始地点禁止Volume除外を検証する
- [ ] 人間用NavMesh上の`npc_spawn` Volumeを整備し、初期洗脳済みNPCを選択中の開始地点禁止Volume外、残りの未洗脳NPCを通常候補から生成して、要求人数と全NPC間最小距離を維持する
- [ ] 同一seedで開始候補、プレイヤー向き、BIT・洗脳済みNPCの初期出現を再現し、全候補で開始地点周囲への敵出現0件、候補0件・対応Volume欠落・不安全候補を厳格エラーにする
- [ ] 開始候補の一様抽選／カテゴリ重みと各禁止Volume寸法は、T06資産実装前に全候補一覧と俯瞰画像を提示して確定する
- [ ] 通常BITとAlert増援を同じ最大数へ含め、カーペット僚機を除外し、停止中・タイトル・公開処刑中・破棄後に通常増援しないことを検証する
- [ ] 通常1／10秒／25、テスト20／即時、性能50／即時を同じ必須人口契約で切り替え、再読込でタイマーと個体数を初期化する
- [ ] T05-2の選択解放状態を実ゲームのFrameまたは専用queryへ公開し、解放前のG／N／Hを無効、`brainwash-in-progress`直後から有効、`playing`終了後を無効にする
- [ ] G／N／Hのkeydownを既存`selectPlayerCompletion` APIへ接続し、`G → N → H → G`の連続再選択を検証する
- [ ] G／Nの移動、Hの移動停止、Gの左クリック射撃、Nの接触妨害、状態別プレイヤー表示を通常ゲームで検証する
- [ ] `brainwash-in-progress`、gun、no-gun、haigureのNPC表示をV1対応スプライトセルへ分離し、gunだけが射撃、no-gunだけが接触拘束を行うことを表示と挙動の両方で確認する
- [ ] 選択解放後のG／N／H操作案内、G状態だけの照準・発射案内、状態別VOICEを既存音量設定へ接続する
- [ ] B03の3D水Volumeを`StageSpatialContext`へ接続し、プール内だけプレイヤー移動速度が通常の50%になり、退出・再配置・タイトル復帰・再読込で通常速度へ戻ることを検証する
- [ ] 既存のBGM・SE・VOICE再生条件、音量設定、開始・停止・切替ライフサイクルを棚卸しし、V2学校入口へ接続する
- [ ] タイトルとゲーム内の音量UIがBGM・SE・VOICEの音量変更とミュートへ反映され、学校の破棄・再読込で停止漏れや多重再生がないことを実ブラウザとElectronで検証する
- [ ] 学校の破棄・再読込でMesh、Material、Texture、NavMesh、Crowd、G／N／Hを含むイベント購読が残留・重複しないことを検証する
- [ ] 実ブラウザとElectronで、命中から`brainwash-in-progress`、G／N／H連続切替、移動・射撃・妨害・停止、案内・照準・VOICE、タイトル復帰・再読込までを一連の受入として検証する
- [ ] 1080p既定設定、Web・Electronビルド、型検査、括弧対応、UTF-8 BOMなし、差分を検証する
- [ ] 本計画と全体`docs/plan.md`へ結果を記録する

## 結果

本体は未着手。PR #50までのT05-2は`develop`へ統合済みである。T06はB04の外周歩道・道路・`BND_WorldLimit`・外周BIT飛行帯と、T05-2VのV1光線演出・世界境界終了を実学校へ接続する。さらに、複数プレイヤー開始地点、動的出現禁止、BIT時間増援、NPC状態別スプライト、G／N／H入力・案内・照準・VOICE、音声、水中速度、学校全域、最終資源ライフサイクルを統合検証する。
