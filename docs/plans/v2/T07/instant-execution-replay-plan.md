# T07 いきなり公開処刑の操作とミッション表示 計画

更新日: 2026-09-06

## プロンプト

> ・デバッグモードかどうかの切り替え方法について、どのファイルのどこの変数を変えればいいのか、READMEに記載しておいてください。
>
> それ以外はここまでOKです。
> 次にいきなり公開処刑モードについて
>
> ・以下のボタンについて、以下の挙動になるようにしてください。
> Rは乱数以外の設定を保ったまま、タイトル画面を移さずに即リプレイ
> Enterはタイトル画面に戻る
>
> ・いきなり公開処刑モード時は右上のミッション結果表示はしない。そもそもミッションしていないので。
> サバイバルモードかつミッションありでの公開処刑や整列時のみ、ミッション結果表示してほしいです。

## 対象と完了条件

- T07の既存作業branch `codex/v2-t07-regression-docs`を継続する。開始HEADは`9dcfc0e`、開始差分なし。前回までの体力・拘束帯・HUDはユーザーのOKを受けた成果として維持する。
- READMEに`src/v2/debugMode.ts`の`V2_DEBUG_MODE`、`true`／`false`の用途と切り替え後の再読込を記載する。commit時は`false`とする。
- いきなり公開処刑では処刑中・完了後ともRで新しい乱数の動的sessionを開始する。開始時の設定と方式・人数を保ち、会場など乱数で決まる内容は新seedから再構築する。タイトルへの遷移や追加クリックを要求せず、入力受付と操作説明を一致させる。
- いきなり公開処刑ではEnterでタイトルへ戻る。サバイバルのR／Enter規則は今回の対象外とする。
- ミッション結果はサバイバルかつMission有効の整列・公開処刑・処刑完了時に表示する。通常プレイの進行中ミッション表示は維持する。いきなり公開処刑ではミッションHUDを作らない。
- 学校資産と戦闘挙動は変更せず、静的学校資源の再利用、設定保存、破棄の契約を維持する。検証はブラウザとWeb buildに限定し、ローカルcommitまで実施する。

## ステップ

- [x] 作業状態、終了操作、snapshot／seed、Mission HUDの接続を調査する。
- [x] READMEと操作・リプレイ・Mission HUDの条件を更新する。
- [x] 関連fixtureと通常ブラウザの実R／Enter・画面遷移・設定保持を検証する。
- [x] Web build、仕様・結果・公開差分を確認し、ローカルcommitする。

## 結果

READMEに`src/v2/debugMode.ts`の`V2_DEBUG_MODE`、通常の`false`と診断・無制限ダッシュの`true`、保存後の再読込とWeb配布時のbuild手順を記載した。

いきなり公開処刑は処刑中・完了後のRを新seedの即リプレイに変更した。開始snapshotの全設定と実人口を保持し、会場を含む乱数結果を新seedから作り直す。動的session再構築中もタイトルを非表示にしてPointer Lockを維持する。Enterは両phaseでタイトルへ戻る。操作説明と実入力の受理条件も一致させた。

Mission HUDは開始modeとMission有効設定から生成・表示条件を決める。サバイバルかつMission有効の通常プレイは進行中表示、整列・公開処刑・完了後は結果表示、いきなり公開処刑はHUD自体を生成しない。サバイバルの既存R／Enter規則を維持した。

通常ブラウザで処刑中と実時間での処刑完了後のR／Enterを実操作した。R前後の全設定・人口一致、新seed、全描画frameでのタイトル非表示、追加クリック不要、Pointer Lock維持、Mission HUDなしを確認した。静的学校load／GLB parseは各1回のまま、動的ownerの破棄残留は0件だった。独立contextで再構築失敗も注入し、マウスロック解除、エラータイトルの表示、設定への実クリック、Canvasクリックによる再試行・復旧を確認した。失敗注入の既知console error 2件を除き、対象ブラウザのconsole／page errorは0件。

サバイバルの正例は独立sessionで確認した。Mission有効で全員洗脳済みから実時間3秒後の整列、銃なしNPCの実captureから公開処刑、実時間での処刑完了の各状態で「ミッション結果」と結果2行が表示された。Mission無効の整列ではHUD DOMが0件だった。NPC配置は独立ブラウザcontextから既存の配置・所属更新APIだけを使い、phase・frame・DOMは書き換えていない。整列と公開処刑はRuntimeの別分岐であり、連続遷移した結果としては扱わない。

終了操作12通りとMission HUD32条件を含むT06-6Cは8項目、既存Runtime入力のT06は73項目PASS。`typecheck:t06`、`typecheck:t06-6c`、`build:renderer`内のV2依存監査・型検査・Web配布監査が通過した。buildは既存のBabylon chunk容量警告だけを出力した。Electronのbuild・起動・試験は実施していない。

対象worktreeの通常サーバーを同じポート5175で再起動し、プロセスの起動元を確認した。正本音声・Character Junctionを維持し、BGM 1件、SE 13件、VOICE 201件とCharacter画像の読込、音量5／5／5、通常モードを確認した。代表素材はHTTP 200かつ音声・画像のContent-Typeで、HTML fallbackではない。確認URLは`http://127.0.0.1:5175/`、関連fixtureは`/validation/v2/T06-6C/index.html`と`/validation/v2/T06/index.html`。`V2_DEBUG_MODE=false`でローカルcommitまで実施し、サーバーを起動したままにする。ユーザーによる今回の動作確認と、push・PR作成・mergeは未実施。
