# HAIGURE SURVIVAL v2 T07 性能・回帰試験・仕様書 計画

更新日: 2026-08-01

## プロンプト

V2の学校1ステージを回帰試験し、1080p・既定設定で60fps目標を満たすよう調整する。GLBのメッシュ、マテリアル、テクスチャ、衝突形状、NavMesh問い合わせに加え、T05-1で全ビットモードへ導入する床・直上天井・身体占有範囲の問い合わせを計測・最適化し、安全判定の結果を変えずに負荷を抑える。Web版とElectron版をビルドする。3Dステージ実行仕様、技術仕様、Blender資産仕様を最新実装へ更新し、旧JSON仕様を履歴文書として最終監査して全体計画へ結果を記録する。

### 追加確定指示（2026-07-19）

> T07は1080p 60fpsと床/天井/身体問い合わせ。
>
> T05-1の確定契約は、V1実形状・戦闘用被弾球0.44m＋安全余裕0.10m＝移動包絡0.54mとする。上下揺れは中心位置のSweepであり、揺れ幅を半径へ二重加算しない。

### 追加性能・機能回帰指示（2026-07-25）

> 最終的には最低でもNPC 99人、ビット50機、加えてプレイヤー1人を満たしたいです。この負荷へ十分滑らかに耐えられる処理内容にすることを最終目標とします。
>
> ビームのエフェクトは、バージョン1にあった光の軌跡、周囲の光の玉、光線命中エフェクトを含めて確認します。ブレインウォッシュインプログレス後のG、H、Nによるプレイヤーモード変更も今後のプランへ反映してください。
>
> 荒れ状態10、多数の動的扉、エレベーター、同時人数に上限のないFollower、洗脳済みFollowerの同期射撃を有効にした状態でも最終負荷を確認してください。

### 標的選択個性の追加回帰指示（2026-07-28）

> NPCとBITについて、取得済み標的を追い続ける個体と、最も近い可視標的へ切り替える個体を50%ずつ発生させます。両個性を有効にした最大人数で、意味と性能を最終確認してください。

## 確定方針

- 性能受入は学校の1080p・既定設定・60fps目標を正本とし、解像度や既定品質を下げた結果で代用しない。
- 定量合格値は、プレイヤー1人＋NPC 99人＋BIT 50機の描画込み総フレーム時間でp95 16.7ms以下、p99 25ms以下、最大50ms以下、50ms超Long Task 0件とする。ステージ読込完了後の開始直後10秒と定常戦闘60秒の両方へ適用する。
- 床問い合わせ、直上天井問い合わせ、V1物理半径0.44m＋安全余裕0.10m＝半径0.54mの移動包絡Sweepを個別に計測し、呼出回数、処理時間、キャッシュ・再利用の妥当性を確認する。上下揺れは中心位置を動かすSweepとして計測し、半径へ揺れ幅を二重加算しない。
- 性能最適化後も、各飛行帯の許可高度、半径0.54mの移動包絡、別ビットNavMeshと接続グラフ、標的喪失時の近端・隣接帯離脱、カーペット3体の個別判定・解除を回帰試験する。
- 窓58組の`aperture`、屋外・体育館の`vertical`、階段`surface-route`、屋上外周`boundary`をビット専用・明示接続として維持する。`PRT_*`、旧`bit_roof`、人間用NavMeshへのフォールバック、帯IDからの隣接推測へ置き換えない。
- 最終性能シナリオはプレイヤー1人＋NPC 99人＋ビット50機とし、V1相当の光線軌跡・周囲光球・壁着弾・キャラクター命中演出が実際に発生する戦闘状態を含める。演出を無効化した値だけで60fps目標の合否を決めない。
- 光線本体、軌跡光球、着弾光球、命中球、PointLightの生成数・更新時間・破棄を個別計測し、pool、instance、particle systemなどT05-2Vで選択・実装した再利用方式の上限と残留0件を回帰する。
- G／N／Hの機能実装はT06-1／T06-2で完了させ、T07では連続切替、移動、射撃、妨害、停止、案内、照準、VOICEと性能への影響を最終回帰する。
- B04の歩道・道路、`BND_WorldLimit`と、T05-2Vの世界境界終了を有効にする。プレイヤー・NPC・全スポーンの塀外到達0件、外周BIT飛行帯なし・外周BIT経路0件、全発射元の非着弾フェードを最終回帰する。
- 荒れ状態10で20室すべての荒れvariantとNavMeshタイルを有効にし、プレイヤーと全NPCが少なくとも1つの扉から室内へ到達し、倒れた机・棚を通行できることを回帰する。
- 多数の教室引き戸・トイレ開き戸について、開閉、人物衝突、ビーム・視線遮蔽、NPCの必須開扉・30%閉扉、BIT遮断を同時に回帰する。
- エレベーターはプレイヤー、未洗脳NPC、洗脳済みNPCの全組合せで利用し、合計6人、7人目拒否、降車常時許可、人物全面ゲート、光線・視線の実パネル追従、同時呼出、陣営別回避・追跡を回帰する。
- Follow人数に上限を設けず、局所分離、2秒視線猶予、Leave、haigure時間停止、被弾解除、同期射撃遅延・クールダウン・一時gun復元を最終負荷へ含める。
- T05-4の`persistent`／`nearest-visible`をNPC 99人とBIT 50機へ決定的に混在させ、最近傍個体の標的変更数、経路再計画数、視線Ray数、空間索引問い合わせ時間を分離計測する。個性を無効化した値だけで合否を決めない。

## 実行単位と推奨設定

- 推奨ブランチ: `codex/v2-t07-regression-docs`
- 推奨モデル: GPT-5.6 Sol
- 推奨リーズニング: High
- 選定理由: 完成機能を維持したまま性能計測、最適化、Web・Electron回帰、仕様書同期を一つの成果として行うため。
- 開始条件: B03-3D、T06-1、T06-2が`develop`へマージ済みであること。各タスクに含まれる全機能を最新`develop`から使用する。
- 並行可否: 単独で実施する。T07完了前にv2リリースブランチを作成しない。

## ステップ

- [ ] `docs/plan.md`、`docs/plans/v2/next_tasks_plan.md`、全個別計画の結果を読む
- [ ] 学校1ステージのゲーム進行、破棄、同一Scene再読込を回帰試験する
- [ ] 学校ステージの描画、メモリ、衝突処理、人間用NavMesh、帯別ビットNavMesh、接続グラフ処理を計測する
- [ ] ビット全モードの床、直上天井、V1物理半径0.44m＋安全余裕0.10m＝半径0.54mの移動包絡Sweepを個別に計測し、上下揺れを中心移動として扱う安全結果を維持したまま呼出回数と処理時間を最適化する
- [ ] プレイヤー1人＋NPC 99人＋ビット50機、全戦闘モード、V1相当の軌跡・着弾・命中演出を含む負荷シナリオで、AI、経路、Snapshot、光線物理、Mesh、光球、PointLightのCPU・GPU・生成数を分離計測する
- [ ] 荒れ状態10、20室の荒れvariant、多数の開閉扉、稼働中エレベーター、全陣営の利用・追跡を同じ99 NPC／50 BIT負荷で有効にする
- [ ] 無制限Followerの局所分離、Follow／Leave切替、2秒視線猶予、haigure時間停止、多人数同期射撃を負荷シナリオへ追加する
- [ ] T05-4の両標的選択個性を50%ずつ混在させ、persistent保持と既存source順、nearest-visible切替と対象ID順、遮蔽、Alarm／Alert割込み、標的変更・再計画・視線予算を回帰する
- [ ] 光線・光球・命中演出の再利用方式と生成上限を検証し、演出欠落、途中消滅、再利用時の位置・色・alpha混線、破棄後残留がないことを確認する
- [ ] 1080p・既定設定で、プレイヤー1人＋NPC 99人＋ビット50機の描画込みp95 16.7ms以下、p99 25ms以下、最大50ms以下、50ms超Long Task 0件をWeb版とElectron版実ゲームで確認する
- [ ] 最適化後も、片羽開放幅1.20mの58窓`aperture`、屋外・体育館`vertical`、階段`surface-route`、屋上外周`boundary`、各帯の許可高度、半径0.54mの移動包絡、上下揺れの中心Sweep、近端・隣接帯離脱、カーペット3体の個別判定・解除が変化していないことを回帰検証する
- [ ] G／N／Hの連続切替、状態別移動・射撃・妨害・停止、操作案内、照準、VOICE、タイトル復帰・再読込をWeb版とElectron版で回帰する
- [ ] `BND_Stage`と`BND_WorldLimit`の分離、外周5.0mの表示終端、プレイヤー・NPC・全スポーンの塀外到達0件、外周BIT飛行帯なし・外周BIT経路0件を回帰する
- [ ] 全発射元の光線が世界境界退出点で着弾・ダメージ・Alarm・反射光球なしに0.2秒フェードし、最大寿命20秒と演出poolの残留0件を維持することを回帰する
- [ ] エレベーターの全状態、合計6人、7人目拒否、降車、人物ゲート、実扉パネル遮蔽、NPC単独利用、未洗脳NPCの敵対便回避、洗脳済みNPCの階層追跡を回帰する
- [ ] F／E／Cの対象選択、扉操作、Follow／Leave、同期射撃、タイトル復帰・再読込後の入力購読・予約・Follower残留0件を回帰する
- [ ] Web版とElectron版をビルドする
- [ ] ゲーム仕様、技術仕様、3D実行仕様、Blender資産仕様を更新し、旧ステージJSON仕様が履歴と明示されていることを監査する
- [ ] Pull Request作成後、`develop`マージ前に性能・リリースゲートを実施し、定量性能、全機能回帰、Web・Electron、仕様書、成果物を独立レビューする
- [ ] 性能・リリースゲート通過後にだけT07を`develop`へマージし、最新`develop`からv2リリース準備を開始できる状態にする
- [ ] UTF-8（BOMなし）、括弧対応、差分、成果物を最終検証する
- [ ] 本計画と全体`docs/plan.md`へ最終結果を記録する

## 結果

未着手。T06-2完了後に開始する。1080p・既定設定・60fps目標と、ビットの床・直上天井・身体占有範囲、帯別NavMesh、接続グラフ問い合わせの性能および安全回帰をT07の受入対象とする。最終人数はプレイヤー1人＋NPC 99人＋ビット50機とし、T05-2VのV1光線演出、B03-3Dの荒れ状態10、T04-3の動的扉・エレベーター、T05-3の無制限Follower・同期射撃、T05-4の標的選択個性、B04の学校外周、T06-1／T06-2のF／E／C・G／N／H・時間増援・複数開始地点を有効にした実ゲームを最終計測・回帰条件とする。2026-07-28、Pull Requestの`develop`マージ前に性能・リリースゲートを追加し、通過後にだけv2リリース準備へ進む順序を確定した。同日、両標的個性を最大人数へ混在させ、標的変更・経路再計画・視線予算を最終性能条件へ追加した。2026-08-01、開始依存をT06-2へ更新し、B04最終契約の外周BIT飛行帯なし・外周BIT経路0件へ同期した。
