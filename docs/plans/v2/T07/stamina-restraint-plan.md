# T07 ダッシュ体力と拘束表示の追加 計画

更新日: 2026-09-06

## プロンプト

> まだ動作確認してないですが、次にこれをやってください。
>
> ・デバッグモードでないときは、V1同様、ダッシュ用の体力ゲージを画面左に表示して、V1同様の体力消費や回復を行ってください。
>
> ・銃なしによって動きが拘束されてるとき、視覚的にそれがわかるようにしたいです。以下の拘束表現は案です。もし簡単には実現できなかったり懸念がある場合は別案を出してください。
> NPCが拘束されているとき、キャラクターの足元から30cm分くらいに、赤く、ゆっくり明滅する帯を表示する。
> プレイヤーが拘束されているとき、画面下辺40pxくらいに、赤く、ゆっくり明滅する帯を表示する。
> この帯は、下の方はほぼ不透明で上に行くに従って透明度が増すような見た目のイメージです。
>
> ・タイトル画面、「アラーム」を「アラーム床」に変更

## 対象と完了条件

- 作業branchは既存T07の`codex/v2-t07-regression-docs`、開始HEADは`2c32d97`。前回のHUD追加分はユーザーの動作確認前であり、今回もAIによる確認とユーザー確認を区別する。
- V1の最終実装を履歴から照合し、通常モードのダッシュ体力・消費・回復・ゲージ表示へ反映する。旧Runtimeをimportせず、V2の現在の入力・移動・セッション再開始へ統合する。
- 拘束表示の正本は既存の銃なし拘束判定とし、表示目的で拘束距離・速度・戦闘状態を変えない。NPCは実空間の足元約0.3m、Playerは画面下端約40pxに赤い縦グラデーションを表示する。明滅、壁による遮蔽、既存HUDとの重なり、解除、再開始時の破棄を確認する。
- 学校のBlender／GLB／NavMeshやmetadataは変更しない。必要な帯はRuntimeの表示資源として所有・破棄する。
- タイトルの表示文言だけ「アラーム床」へ変更する。保存キー・設定schemaは維持する。
- 検証はブラウザとWeb buildに限定する。`V2_DEBUG_MODE = false`を維持し、ローカルcommit後も正本素材・ポート5175の通常ゲーム確認サーバーを継続する。

## ステップ

- [x] 対象worktree・branch・HEAD・既存差分を確認する。
- [x] V1体力仕様、V2拘束の正本、表示資源とライフサイクルを調査して実装範囲を確定する。
- [x] ダッシュ体力・ゲージ、拘束の帯、タイトル文言を実装する。
- [x] 対象fixture・通常ブラウザの実操作と実画面、Web buildを確認する。
- [x] 仕様と結果・公開差分を確認し、通常モードのままローカルcommitする。

## 結果

開始時点のworktreeは差分なし。V1最終実装（`0c8b438`）を照合し、体力150段階＝15.0、移動入力を伴うダッシュで0.1秒ごとに1段階消費、非消費時は0.2秒ごとに1段階回復とした。0でShiftと移動を保持している間は回復せず、洗脳系状態は満タン、ダッシュ倍率は2倍。通常モードで適用し、デバッグ時は従来の無制限を維持する。

拘束対象はPlayer銃なしの近接拘束と、銃なしNPCのcapture対象を合成する。被弾による停止は含めない。表示用snapshotを現在の状態で評価し、G／Hへの変更や離脱直後の古い帯を残さない。NPC接触洗脳はcaptureを作らないが、Player接触洗脳のhit-a／hit-b中は既存近接拘束が成立するため、その間は帯を表示する。

NPCの帯は幅をCharacter画像に合わせた高さ0.3mのRuntime面と共有グラデーション素材を使い、補間済み足元へ追随する。2秒周期の明滅、透明部分の深度cutoff、壁・ガラスとの前後関係を確認した。camera-facingの俯角では垂直な帯がSprite下端に隠れることを確認し、帯もSpriteと同じview平面の足元へ揃えて解消した。固定距離で手前へずらす回避策は使っていない。Playerは下辺40pxのCSS帯を使う。表示資源は動的sessionの破棄へ接続した。低い画面でのゲージと操作説明の重なりを実画面で確認したため、高さ800px以下はミニマップ右へ縮小配置した。

通常ブラウザで体力15.0→11.8の実Shift＋W移動、2.2秒休止で12.9への回復を確認した。upright／camera-facingのNPC帯を正面・左右から確認し、G切替で解除した。Playerの実capture・W停止・20秒満了解除、R再開始の15.0復帰、Enterでタイトルへ戻った際の非表示とPlayer帯DOM重複なしも確認した。1080p、1280×720、640×400で目視確認済み。隔離WebGLでも不透明壁の完全遮蔽と、前後ガラスの描画順を確認した。

最終検証はT05が331項目、T06が73項目、T06-2が26項目の計430項目PASS、対象ブラウザのconsole／page errorは0件。T05の新規2ケースは猶予解除直後のdelta=0が索敵更新を進めないfixtureの前提誤りを修正し、正tickで通過した。製品の拘束挙動は変更していない。`typecheck:t02`、`typecheck:t04`、`typecheck:t05`、`typecheck:t06`、`typecheck:t06-2`と`build:renderer`が通過し、Web配布監査もPASSした。Electronのbuild・起動・試験は実施していない。

通常サーバーを対象worktreeからポート5175で再起動した。既定設定の通常ゲームでも体力ゲージとタイトル表記を確認し、BGM 1件、SE 13件、VOICE 201件、Character画像17ディレクトリのカタログと正本素材の読込を確認した。代表BGM／SE／VOICE／CharacterはHTTP 200かつ音声・画像のContent-Typeであり、HTML fallbackではない。正本の音声・Character Junctionを維持し、配布物への混入は0件。`V2_DEBUG_MODE=false`でローカルcommitし、push・PR作成・mergeは今回の範囲に含めない。ユーザーによる実機確認は未実施として区別する。
